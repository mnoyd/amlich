---
phase: 08-sexagenary-cycle-parity-and-validators
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/almanac/sexagenary_cycle.rs
  - crates/amlich-core/src/almanac/mod.rs
autonomous: true
requirements: [SC-01, SC-02, SC-03, SC-04]
must_haves:
  truths:
    - "System converts cycle index (1-60) to canonical stem-branch pair with 1-based public contract"
    - "System converts stem-branch pair to cycle index (1-60) rejecting invalid combinations"
    - "Forward/backward progression preserves modular correctness across 10/12/60 rollover boundaries"
    - "Utilities expose deterministic helpers reusable by hour pillar and Na Am APIs"
  artifacts:
    - path: "crates/amlich-core/src/almanac/sexagenary_cycle.rs"
      provides: "60-cycle conversion/progression utilities with bounded contracts"
      exports: ["cycle_index_to_canchi", "canchi_to_cycle_index", "progress_cycle_index"]
      min_lines: 180
    - path: "crates/amlich-core/src/almanac/mod.rs"
      provides: "Module export for sexagenary_cycle"
      contains: "pub mod sexagenary_cycle"
  key_links:
    - from: "crates/amlich-core/src/almanac/sexagenary_cycle.rs"
      to: "crates/amlich-core/src/types.rs"
      via: "Canonical CAN/CHI arrays and normalize_index helper"
      pattern: "CAN|CHI|normalize_index"
    - from: "crates/amlich-core/src/almanac/sexagenary_cycle.rs"
      to: "crates/amlich-core/src/almanac/data.rs"
      via: "Reusing existing sexagenary_index computation pattern"
      pattern: "expand_sexagenary_na_am"
---

<objective>
Implement canonical sexagenary 60-cycle utilities with bounded 1-based contracts, deterministic progression, and canonical validation.

Purpose: Establish single source of truth for 60-cycle arithmetic so hour pillar and Na Am lookup can share verified conversion/progression logic without duplication.
Output: New `sexagenary_cycle` module with index-to-pair conversion, pair-to-index inversion, and signed progression helpers.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-sexagenary-cycle-parity-and-validators/08-RESEARCH.md
@crates/amlich-core/src/types.rs
@crates/amlich-core/src/almanac/data.rs

<interfaces>
From `crates/amlich-core/src/types.rs`:
```rust
pub const CAN: [&str; 10];
pub const CHI: [&str; 12];

pub fn normalize_index(value: i32, modulo: i32) -> usize;
```

From `crates/amlich-core/src/almanac/data.rs`:
```rust
fn expand_sexagenary_na_am(na_am_pairs: &[String]) -> HashMap<String, NaAmEntry>;
// Shows 60-cycle iteration pattern: for i in 0..60 { can = CAN[i % 10]; chi = CHI[i % 12] }
```
</interfaces>

# Implementation constraints:
# - Public APIs use 1-based indices (1-60) to match Vietnamese convention
# - Internal arithmetic converts to 0-based (0-59) immediately after validation
# - Use rem_euclid(60) for signed delta handling in progression
# - Reject non-canonical stem/branch pairs (odd/even mismatch) with explicit error
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add cycle index to stem-branch conversion with bounded 1-based contract</name>
  <files>crates/amlich-core/src/almanac/sexagenary_cycle.rs, crates/amlich-core/src/almanac/mod.rs</files>
  <behavior>
    - Test 1: index 1 returns "Giáp Tý", index 60 returns "Quý Hợi"
    - Test 2: Invalid indices 0 and 61 return None
    - Test 3: Module exports compile via `almanac::sexagenary_cycle`
  </behavior>
  <action>
    Create `sexagenary_cycle.rs` with:
    - `cycle_index_to_canchi(index: u8) -> Option<CanChi>` validates 1-60 bounds
    - Convert to 0-based: `zero_based = (index - 1) as usize`
    - Compute stem/branch: `can_idx = zero_based % 10`, `chi_idx = zero_based % 12`
    - Return `CanChi::new(can_idx, chi_idx)` from types.rs
    - Add unit tests for bounds checking (0, 1, 60, 61) and canonical values at edges

    Export module in `almanac/mod.rs` with `pub mod sexagenary_cycle;`.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib sexagenary_cycle::tests::cycle_index_to_canchi</automated>
  </verify>
  <done>
    Cycle index to CanChi conversion implements 1-based contract and validates bounds.
    Module is exported and compilable through the almanac namespace.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add stem-branch to cycle index conversion with canonical validation</name>
  <files>crates/amlich-core/src/almanac/sexagenary_cycle.rs</files>
  <behavior>
    - Test 1: "Giáp Tý" returns 1, "Quý Hợi" returns 60
    - Test 2: Non-canonical pairs like "Giáp Sửu" (odd/even mismatch) return None
    - Test 3: Validation uses parity check: `can_index % 2 == chi_index % 2`
  </behavior>
  <action>
    Implement:
    - `canchi_to_cycle_index(can_index: usize, chi_index: usize) -> Option<u8>`
    - Validate canonical combination: `if can_index % 2 != chi_index % 2 { return None; }`
    - Compute 60-cycle position using existing formula: `((can_index * 6) + (chi_index / 2)) % 60`
    - Convert to 1-based: `Some((zero_based + 1) as u8)`
    - Add regression tests covering all 60 valid pairs and representative invalid pairs (odd/even mismatch)
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib sexagenary_cycle::tests::canchi_to_cycle_index</automated>
  </verify>
  <done>
    Stem-branch to cycle index conversion validates canonical combinations.
    Invalid non-canonical pairs are rejected explicitly with None.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement signed progression with modular rollover correctness</name>
  <files>crates/amlich-core/src/almanac/sexagenary_cycle.rs</files>
  <behavior>
    - Test 1: Forward from 60 by +1 returns 1, backward from 1 by -1 returns 60
    - Test 2: Large deltas wrap correctly (e.g., index 1 + 125 = index 6)
    - Test 3: Invalid starting indices return None
  </behavior>
  <action>
    Implement:
    - `progress_cycle_index(index: u8, delta: i32) -> Option<u8>`
    - Validate input bounds: `if !(1..=60).contains(&index) { return None; }`
    - Convert to signed 0-based: `zero_based = (index - 1) as i32`
    - Use `rem_euclid` for signed handling: `progressed = ((zero_based + delta).rem_euclid(60)) as u8`
    - Return 1-based: `Some(progressed + 1)`
    - Add boundary tests: `progress_cycle_index(1, -1)`, `progress_cycle_index(60, 1)`, large deltas, invalid starts
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib sexagenary_cycle::tests::progress_cycle_index</automated>
  </verify>
  <done>
    Signed progression handles forward/backward deltas with correct modular rollover.
    Boundary transitions at 1 and 60 wrap correctly using rem_euclid semantics.
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --package amlich-core --lib sexagenary_cycle::tests
cargo check --package amlich-core
```
</verification>

<success_criteria>
1. SC-01 satisfied with 1-based cycle index to stem-branch conversion
2. SC-02 satisfied with stem-branch to cycle index inversion rejecting non-canonical pairs
3. SC-03 satisfied with signed progression preserving modular correctness at boundaries
4. SC-04 satisfied with reusable utility exports in dedicated sexagenary_cycle module
</success_criteria>

<output>
After completion, create `.planning/phases/08-sexagenary-cycle-parity-and-validators/08-01-SUMMARY.md`
</output>
