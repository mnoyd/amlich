---
phase: 08-sexagenary-cycle-parity-and-validators
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/tests/sexagenary_cycle_parity.rs
  - crates/amlich-core/src/almanac/sexagenary_cycle.rs
autonomous: true
requirements: [SC-05, PAR-01]
must_haves:
  truths:
    - "Full 60-entry parity validator confirms exact canonical table matching"
    - "Validator iterates over all 60 positions against baseline na_am_pairs reference"
    - "Regression tests guard cycle index normalization and invalid-input handling"
  artifacts:
    - path: "crates/amlich-core/tests/sexagenary_cycle_parity.rs"
      provides: "Full-table parity validator and regression test suite"
      contains: "validate_full_60_cycle_parity"
      min_lines: 200
    - path: "crates/amlich-core/src/almanac/sexagenary_cycle.rs"
      provides: "Public functions required by integration validator"
      exports: ["cycle_index_to_canchi", "canchi_to_cycle_index", "progress_cycle_index"]
  key_links:
    - from: "crates/amlich-core/tests/sexagenary_cycle_parity.rs"
      to: "crates/amlich-core/src/almanac/sexagenary_cycle.rs"
      via: "Integration tests call public APIs across all 60 cycle positions"
      pattern: "cycle_index_to_canchi|canchi_to_cycle_index|progress_cycle_index"
    - from: "crates/amlich-core/tests/sexagenary_cycle_parity.rs"
      to: "crates/amlich-core/data/almanac/baseline.json"
      via: "Validator loads na_am_pairs baseline for 30 entries × 2 = 60 positions"
      pattern: "na_am_pairs"
    - from: "crates/amlich-core/tests/sexagenary_cycle_parity.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "Full-table coverage mapped to SC-05 and PAR-01 expectations"
      pattern: "SC-05|PAR-01"
---

<objective>
Add full-table parity validators that lock 60-cycle behavior against baseline reference data and regression-guard boundary cases.

Purpose: Convert Phase 8 parity requirements into executable regression checks so future changes cannot reintroduce cycle arithmetic drift.
Output: Dedicated integration validator suite iterating over all 60 positions with divergence reporting.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/08-sexagenary-cycle-parity-and-validators/08-RESEARCH.md
@crates/amlich-core/src/almanac/sexagenary_cycle.rs
@crates/amlich-core/src/almanac/data.rs
@crates/amlich-core/tests/khcbppt_na_am.rs

<interfaces>
From `crates/amlich-core/src/almanac/sexagenary_cycle.rs`:
```rust
pub fn cycle_index_to_canchi(index: u8) -> Option<CanChi>;
pub fn canchi_to_cycle_index(can_index: usize, chi_index: usize) -> Option<u8>;
pub fn progress_cycle_index(index: u8, delta: i32) -> Option<u8>;
```

From `crates/amlich-core/src/almanac/data.rs`:
```rust
pub fn baseline_data() -> &'static BaselineData;
// Contains na_am_pairs: Vec<String> with 30 entries
```

From `crates/amlich-core/tests/khcbppt_na_am.rs`:
```rust
// Validator divergence reporting pattern with mismatch collection and formatted output
```
</interfaces>

# Implementation constraints:
# - Validator must iterate over all 60 positions (not sample subset)
# - Expected values must come from baseline data, not derived from function under test
# - Preserve deterministic, no-IO test design suitable for `cargo test --package amlich-core`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement full 60-cycle parity validator against baseline na_am_pairs</name>
  <files>crates/amlich-core/tests/sexagenary_cycle_parity.rs</files>
  <behavior>
    - Test 1: All 60 cycle positions (1-60) match baseline stem/branch/na_am/element expectations
    - Test 2: Each of 30 na_am_pairs covers 2 consecutive cycle positions with correct mapping
    - Test 3: Divergence report lists all mismatches if any occur
  </behavior>
  <action>
    Add a new integration test file `sexagenary_cycle_parity.rs` with:
    - `validate_full_60_cycle_parity` test that:
      - Loads baseline data: `let data = baseline_data()`
      - Iterates over `na_am_pairs` (30 entries), each expanding to 2 cycle positions
      - For each position: `cycle_index = (zero_based_index * 2 + offset) as u8 + 1`
      - Calls `cycle_index_to_canchi(cycle_index)` from sexagenary_cycle module
      - Verifies `can`, `chi`, and `na_am` match expanded baseline expectations
      - Collects mismatches in a Vec for divergence reporting with formatted messages
      - Asserts `mismatches.is_empty()` with descriptive failure message
  </action>
  <verify>
    <automated>cargo test --package amlich-core --test sexagenary_cycle_parity validate_full_60_cycle_parity -- --nocapture</automated>
  </verify>
  <done>
    Full-table validator confirms exact parity between generated cycles and baseline reference.
    All 60 positions are checked with divergence reporting for any mismatches.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add regression tests for index normalization and invalid input handling</name>
  <files>crates/amlich-core/tests/sexagenary_cycle_parity.rs, crates/amlich-core/src/almanac/sexagenary_cycle.rs</files>
  <behavior>
    - Test 1: Cycle index normalization handles all edge cases (0, 1, 60, 61, negative equivalents)
    - Test 2: Invalid stem/branch pairs (non-canonical odd/even mismatch) are consistently rejected
    - Test 3: Progression boundary cases wrap correctly at 1 and 60 for various delta values
  </behavior>
  <action>
    Extend `sexagenary_cycle_parity.rs` with regression test functions:
    - `validate_cycle_index_normalization_edges`: tests 0, 1, 60, 61 return None/expected
    - `validate_invalid_pair_rejection`: tests all 60 invalid odd/even mismatched pairs return None
    - `validate_progression_boundary_rollover`: tests `progress_cycle_index(1, -1) == 60`, `progress_cycle_index(60, 1) == 1`, large deltas
    - `validate_bidirectional_conversion_roundtrip`: converts index -> pair -> index and asserts equality for all 60 valid positions

    If needed, make minimal non-breaking adjustments in `sexagenary_cycle.rs` to expose any internal helpers required by regression tests.
    Keep validator outputs deterministic and independent from runtime locale/timezone state.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --test sexagenary_cycle_parity -- --nocapture</automated>
  </verify>
  <done>
    Regression tests guard index normalization, invalid input handling, and boundary rollover.
    Full bidirectional roundtrip validation confirms conversion consistency.
    Phase 8 validation requirements (SC-05, PAR-01) are test-backed and regression-resistant.
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --package amlich-core --test sexagenary_cycle_parity -- --nocapture
cargo test --package amlich-core --lib sexagenary_cycle::tests
```
</verification>

<success_criteria>
1. SC-05 satisfied with validation suite confirming full 60-cycle parity against canonical references
2. PAR-01 satisfied with dedicated parity validators for full 60-cycle tables
</success_criteria>

<output>
After completion, create `.planning/phases/08-sexagenary-cycle-parity-and-validators/08-02-SUMMARY.md`
</output>
