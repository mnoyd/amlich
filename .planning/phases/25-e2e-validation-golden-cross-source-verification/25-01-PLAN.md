---
phase: 25-e2e-validation-golden-cross-source-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/tests/integration_2026_smoke.rs
  - crates/amlich-core/tests/v17_baseline_guards.rs
autonomous: true
requirements:
  - INT-13

must_haves:
  truths:
    - "A reader running `cargo test --package amlich-core` observes the v1.7 E2E smoke test in `tests/integration_2026_smoke.rs` exercising IChing casting + biến quẻ + Thái Tuế cross-link + semantic-graph wiring + DaySnapshot fields together on ≥5 representative 2026 dates (Tết + 4 Sóc dates spanning distinct lunar months)"
    - "A reader running `cargo test --package amlich-core` observes the v1.7 E2E smoke test assert ALL of the following together on each representative date: (a) `enrich_day_snapshot_with_iching` populates `snapshot.iching_cast` with a non-None `IChingCastSummary` carrying 4 evidence envelopes (CRIT-6 contract: 2 primitive mai-hoa-dich-so + 1 primitive kinh-dich + 1 composite rule.composite.iching_consultation); (b) `enrich_day_snapshot_with_direction_cross_link` populates `snapshot.direction_cross_link` with a non-None `DirectionCrossLinkSummary` carrying 8 cells + dual-source provenance (KHCBPPT + HUYEN_KHONG); (c) the semantic graph built from the BOTH-fields-populated snapshot contains ≥2 `NodeConcept::Hexagram` nodes + ≥1 `EdgeConcept::Transforms` edge + ≥1 `NodeConcept::Direction` composite node"
    - "A reader running `cargo test --package amlich-core` observes the v1.7 E2E smoke test verify the IChing casting chain (Phase 22 surface) end-to-end: `cast_mai_hoa(...)` returns upper/lower trigrams + động hào; `derive_bien_que(&cast)` returns a biến King Wen index that DIFFERS from the chủ King Wen index (CRIT-4 line-flip ALWAYS changes the hexagram); `classify_the_dung(&cast)` returns a non-Dong verdict OR a Dong verdict with `cat_hung_summary == \"binh\"` (the verdict surface is exercised, not the verdict value)"
    - "A reader running `cargo test --package amlich-core` observes the v1.7 baseline guards test in `tests/v17_baseline_guards.rs` assert the cargo dependency tree is UNCHANGED from v1.6 — the production `[dependencies]` section of `crates/amlich-core/Cargo.toml` contains EXACTLY four entries with the locked names `serde`, `serde_json`, `chrono`, `unicode-normalization` (no more, no less; CI-enforced snapshot of the v1.6 baseline)"
    - "A reader running `cargo test --package amlich-core` observes the v1.7 baseline guards test assert INT-13's golden-dataset discipline holistically: `load_mai_hoa_golden()` returns a dataset with ≥10 cases (SC1 count), every case carrying ≥2 source entries with `nhantu.net` present in at least one of them (SC1 dual-source), and ≥1 `KnownDivergence` row with non-empty `our_value` + `tiebreaker` + `note` (SC1 divergences logged not silently corrected)"
    - "A caller running `cargo test --package amlich-core` observes the full test suite green with the v1.6 test files (all pre-existing v1.6-vintage test files in `crates/amlich-core/tests/` plus all `#[cfg(test)]` inline modules) continuing to pass alongside the new v1.7 tests — zero regressions versus the v1.6 baseline shape"
  artifacts:
    - path: "crates/amlich-core/tests/integration_2026_smoke.rs"
      provides: "Extended 2026 E2E smoke (Phase 25 SC2) — appends a new test `e2e_2026_smoke_v17_iching_and_cross_link_wiring_on_representative_dates` to the existing file (alongside the v1.5 INT-06 + v1.6 INT-10 tests). Exercises ALL four v1.7 surfaces (IChing casting + biến quẻ + Thái Tuế cross-link + semantic-graph wiring + DaySnapshot fields) together on Tết 2026 + 4 Sóc dates spanning distinct lunar months. Asserts: IChingCastSummary 4-envelope CRIT-6 contract; DirectionCrossLinkSummary 8-cell + dual-source contract; semantic graph Hexagram + Transforms + Direction composite nodes; CRIT-4 biến ≠ chủ King Wen; cast_mai_hoa + derive_bien_que + classify_the_dung chain end-to-end"
      contains: "e2e_2026_smoke_v17_iching_and_cross_link_wiring_on_representative_dates"
      min_lines: 130
    - path: "crates/amlich-core/tests/v17_baseline_guards.rs"
      provides: "New Phase 25 SC1 + SC4 baseline guard test file with two tests: (1) `cargo_dependency_tree_unchanged_from_v16` parses `crates/amlich-core/Cargo.toml` via `include_str!` and asserts the `[dependencies]` section contains EXACTLY four lines naming `serde`, `serde_json`, `chrono`, `unicode-normalization` — no more, no less; (2) `int13_golden_dataset_cross_source_discipline_holds` loads `mai_hoa_golden.json` via `load_mai_hoa_golden()` and re-asserts INT-13's full discipline holistically (≥10 cases, every case has ≥2 sources with `nhantu.net` present in ≥1, ≥1 KnownDivergence row with non-empty fields)"
      contains: "cargo_dependency_tree_unchanged_from_v16"
      min_lines: 80
  key_links:
    - from: "crates/amlich-core/tests/integration_2026_smoke.rs"
      to: "crates/amlich-core/src/lib.rs::enrich_day_snapshot_with_iching"
      via: "Phase 24-01 immutable enrichment helper — clone + attach IChingCastSummary"
      pattern: "enrich_day_snapshot_with_iching"
    - from: "crates/amlich-core/tests/integration_2026_smoke.rs"
      to: "crates/amlich-core/src/lib.rs::enrich_day_snapshot_with_direction_cross_link"
      via: "Phase 23-03 immutable enrichment helper — clone + attach DirectionCrossLinkSummary"
      pattern: "enrich_day_snapshot_with_direction_cross_link"
    - from: "crates/amlich-core/tests/integration_2026_smoke.rs"
      to: "crates/amlich-core/src/iching/evaluator.rs::IChingQuery::from_snapshot"
      via: "Phase 24-01 sibling-newtype query constructor (NOT ConsultationIntent::IChing variant)"
      pattern: "IChingQuery::from_snapshot"
    - from: "crates/amlich-core/tests/integration_2026_smoke.rs"
      to: "crates/amlich-core/src/iching/mai_hoa.rs::cast_mai_hoa + bien_que.rs::derive_bien_que + the_dung.rs::classify_the_dung"
      via: "Phase 22 IChing casting chain — exercise ALL three primitives together"
      pattern: "cast_mai_hoa|derive_bien_que|classify_the_dung"
    - from: "crates/amlich-core/tests/integration_2026_smoke.rs"
      to: "crates/amlich-core/src/semantic_graph/builders/day_snapshot.rs::DaySnapshotGraphBuilder"
      via: "Phase 24-02 additive builder dispatch — Hexagram + Transforms + LocatedAt + Direction composite nodes"
      pattern: "build_day_snapshot_graph"
    - from: "crates/amlich-core/tests/v17_baseline_guards.rs"
      to: "crates/amlich-core/Cargo.toml"
      via: "include_str! + ad-hoc parser — assert exactly 4 production deps with locked names"
      pattern: "include_str!"
    - from: "crates/amlich-core/tests/v17_baseline_guards.rs"
      to: "crates/amlich-core/src/iching/golden.rs::load_mai_hoa_golden"
      via: "Phase 22-02 golden dataset loader — INT-13 SC1 cross-source discipline sentinel"
      pattern: "load_mai_hoa_golden"

---

<objective>
Close INT-13 by shipping the final v1.7 E2E validation surface: extend `tests/integration_2026_smoke.rs` with a unified v1.7 smoke test exercising IChing casting + biến quẻ + Thái Tuế cross-link + semantic-graph wiring + DaySnapshot fields together on representative 2026 dates, plus a new `tests/v17_baseline_guards.rs` file locking the cargo dependency tree (SC4) and the Mai Hoa golden dataset's cross-source discipline (SC1). The Phase 22-02 golden dataset already carries 12 cases × 2 sources + 2 KnownDivergence rows (SC1 already met at the data level); this plan formally verifies and locks that discipline holistically alongside the dep-tree guard (SC4) and the unified v1.7 E2E smoke (SC2). SC3 (full crate test suite green + zero regressions on the v1.6 baseline) is verified by `cargo test --package amlich-core` at plan completion.

Purpose: This is the FINAL phase of the v1.7 milestone. Phases 20-24 shipped all production code (448-text-field corpus + Mai Hoa casting + biến quẻ + Thể/Dụng + IChingEvaluator + DirectionCrossLink + semantic-graph wiring + additive DaySnapshot fields + v1.6→v1.7 round-trip). Phase 25 adds NO new production code — it is pure validation: a unified E2E smoke proving the four v1.7 pillars compose correctly on real 2026 dates, plus two baseline guards locking the dep-tree shape and the cross-source golden discipline so they cannot silently drift in v1.8+.

Output:
- `crates/amlich-core/tests/integration_2026_smoke.rs` — append ONE new test (`e2e_2026_smoke_v17_iching_and_cross_link_wiring_on_representative_dates`) exercising all v1.7 surfaces together on Tết 2026 + 4 Sóc dates; mirrors the file's existing Phase 19 INT-10 pattern (Tết + filtered Sóc dates, dedup, ≥5 entries asserted, per-date invariants)
- `crates/amlich-core/tests/v17_baseline_guards.rs` — NEW file with TWO tests: `cargo_dependency_tree_unchanged_from_v16` (SC4) + `int13_golden_dataset_cross_source_discipline_holds` (SC1 sentinel)
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/22-mai-hoa-casting-bien-que-the-dung/22-02-SUMMARY.md
@.planning/phases/23-th-i-tu-tam-s-t-phi-tinh-cross-link/23-03-SUMMARY.md
@.planning/phases/24-iching-evaluator-semantic-graph-wiring-dto-integration/24-01-SUMMARY.md
@.planning/phases/24-iching-evaluator-semantic-graph-wiring-dto-integration/24-02-SUMMARY.md
@.planning/phases/24-iching-evaluator-semantic-graph-wiring-dto-integration/24-03-SUMMARY.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->
<!-- Executor should use these directly — no codebase exploration needed. -->

From crates/amlich-core/src/lib.rs (immutable enrichment helpers — both shipped before Phase 25):
```rust
/// Phase 23-03 (XLK-03) immutable enrichment: clone + attach DirectionCrossLinkSummary.
/// Dispatches on DATE_ONLY_BIRTH_CHI_INDEX (usize::MAX) sentinel: date-only variant.
pub fn enrich_day_snapshot_with_direction_cross_link(
    snapshot: &DaySnapshot,
    birth_chi_index: usize,
) -> Result<DaySnapshot, String>;

/// Phase 24-01 (ICH-05 + partial INT-12) immutable enrichment: clone + attach IChingCastSummary.
pub fn enrich_day_snapshot_with_iching(
    snapshot: &DaySnapshot,
    query: crate::iching::IChingQuery,
) -> Result<DaySnapshot, String>;
```

From crates/amlich-core/src/iching/evaluator.rs (Phase 24-01 sibling-newtype query):
```rust
pub struct IChingQuery {
    pub chi_hour_index: u8,
    pub question_vi: Option<String>,
    pub lunar_year_branch: u8,
    pub lunar_month: u8,
    pub lunar_day: u8,
}

impl IChingQuery {
    /// Derive lunar inputs from the snapshot's context. chi_hour_index must be 0..=11.
    pub fn from_snapshot(
        snapshot: &DaySnapshot,
        question_vi: Option<String>,
        chi_hour_index: u8,
    ) -> Result<Self, String>;

    /// Direct constructor — validates all four input ranges.
    pub fn from_lunar_inputs(
        lunar_year_branch: u8,
        lunar_month: u8,
        lunar_day: u8,
        chi_hour_index: u8,
        question_vi: Option<String>,
    ) -> Result<Self, String>;
}

pub struct IChingCastSummary {
    pub chu_king_wen_index: u8,        // accessor: chu_king_wen_index()
    pub chu_hexagram_vi_name: String,
    pub chu_hexagram_thoai_tu: String,
    pub bien_king_wen_index: u8,        // accessor: bien_king_wen_index()
    pub bien_hexagram_vi_name: String,
    pub bien_hexagram_thoai_tu: String,
    pub cat_hung_summary: String,       // 'cat' | 'binh' | 'hung'
    pub moving_line: u8,                // 1..=6 (động hào)
    pub question_vi: Option<String>,
    pub evidence: Vec<ReasoningEvidenceEnvelope>,  // exactly 4 envelopes (CRIT-6)
}
```

From crates/amlich-core/src/iching/{mai_hoa,bien_que,the_dung}.rs (Phase 22 surface):
```rust
pub fn cast_mai_hoa(
    lunar_year_branch: u8,
    lunar_month: u8,
    lunar_day: u8,
    chi_hour_index: u8,
) -> MaiHoaCast;  // upper_trigram, lower_trigram, dong_hao, chu_que: KingWenHexagram

pub fn derive_bien_que(cast: &MaiHoaCast) -> BienQue;  // king_wen: KingWenHexagram (≠ cast.chu_que per CRIT-4)

pub fn classify_the_dung(cast: &MaiHoaCast) -> TheDungClassification;  // verdict: CatHung (Cat|Binh|Hung)
```

From crates/amlich-core/src/reasoning/direction_composite.rs (Phase 23-03 DTO + constants):
```rust
pub const COMPOSITE_DIRECTION_CROSS_LINK: &str = "rule.composite.direction_cross_link";
pub const DATE_ONLY_BIRTH_CHI_INDEX: usize = usize::MAX;
pub const DIRECTION_ORDER: [Direction; 8];  // North/Northeast/East/Southeast/South/Southwest/West/Northwest

pub struct DirectionCrossLinkSummary {
    pub cross_link_kind: String,
    pub cross_link_source: String,         // typically "rule.composite.direction_cross_link"
    pub date: String,
    pub day_chi_index: u8,
    pub birth_chi_index: usize,            // usize::MAX for date-only variant
    pub cells: [DirectionCell; 8],         // EXACTLY 8 cells (one per direction)
    pub summary_vi: String,
    pub composite_severity: ReasoningNodeSeverity,
    pub evidence: Vec<ReasoningEvidenceEnvelope>,  // ≥3 envelopes: KHCBPPT + HUYEN_KHONG + composite
}
```

From crates/amlich-core/src/iching/golden.rs (Phase 22-02 dataset loader):
```rust
pub fn load_mai_hoa_golden() -> &'static MaiHoaGoldenDataset;

pub struct MaiHoaGoldenDataset {
    pub schema_version: String,            // asserted == "mai-hoa-golden-v1" at load
    pub cases: Vec<MaiHoaGoldenCase>,      // ≥10 (SC1 count)
    pub known_divergences: Vec<MaiHoaKnownDivergence>,  // ≥1 (FS-10 / AF-05)
}

pub struct MaiHoaGoldenCase {
    pub id: String,
    pub inputs: MaiHoaGoldenInputs,
    pub expected: MaiHoaGoldenExpected,
    pub sources: Vec<MaiHoaGoldenSource>,  // ≥2 per case (FS-10 dual-source)
    pub confidence: GoldenConfidence,
    pub note: String,
}

pub struct MaiHoaGoldenSource {
    pub source: String,                    // e.g. "nhantu.net", "vi.wikipedia"
    pub url_or_ref: String,
    pub value: String,
}
```

From crates/amlich-core/src/semantic_graph/*.rs (Phase 24-02 additive builder surface):
```rust
pub fn build_day_snapshot_graph(snapshot: &DaySnapshot) -> SemanticGraph;

// DaySnapshotGraphBuilder dispatches to add_iching_facts + add_direction_composite_facts
// automatically when snapshot.iching_cast / snapshot.direction_cross_link are Some.
// Emits: 2 NodeConcept::Hexagram nodes (chu + bien, role-bearing stable keys)
//      + 1 EdgeConcept::Transforms edge (chu → bien)
//      + 2 EdgeConcept::LocatedAt edges (each Hexagram → day root)
//      + 1 NodeConcept::Direction composite fact node (when direction_cross_link is Some)
```

From crates/amlich-core/src/sources.rs (registered source_id constants — always use these, never bare literals):
```rust
pub const SOURCE_KINH_DICH: &str = "kinh-dich";
pub const SOURCE_MAI_HOA_DICH_SO: &str = "mai-hoa-dich-so";
pub const SOURCE_KHCBPPT: &str = "khcbppt";
pub const SOURCE_HUYEN_KHONG: &str = "huyen-khong";
pub const COMPOSITE_ICHING_CONSULTATION: &str = "rule.composite.iching_consultation";  // via iching/mod.rs
```

From crates/amlich-core/Cargo.toml (SC4 — the locked v1.6 dependency shape):
```toml
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }
chrono = { workspace = true }
unicode-normalization = "0.1.25"

[dev-dependencies]
# (empty in v1.6 + v1.7)
```

From crates/amlich-core/tests/integration_2026_smoke.rs (Phase 19 INT-10 precedent — the pattern this plan extends):
```rust
// Existing helpers (already in the file; reuse for the new test):
fn collect_lunar_day_dates(target_day: i32) -> Vec<(i32, i32, i32)>;  // returns Sóc/Vọng dates
// Existing tests: e2e_2026_smoke_all_categories, tet_2026_is_lunar_1_1,
//                 van_boundary_8_to_9, e2e_2026_smoke_offering_wiring_on_representative_dates
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend integration_2026_smoke.rs with v1.7 E2E smoke test</name>
  <files>crates/amlich-core/tests/integration_2026_smoke.rs</files>
  <action>
APPEND a single new test function `e2e_2026_smoke_v17_iching_and_cross_link_wiring_on_representative_dates` to the EXISTING `crates/amlich-core/tests/integration_2026_smoke.rs` file. Do NOT modify or remove the existing 4 tests (e2e_2026_smoke_all_categories, tet_2026_is_lunar_1_1, van_boundary_8_to_9, e2e_2026_smoke_offering_wiring_on_representative_dates) — the new test is purely ADDITIVE, mirroring how Phase 19 appended its INT-10 offering-wiring smoke test to the v1.5 INT-06 file.

**Imports to add at the top of the new test function** (use `use` inside the function body to avoid touching the file's existing module-level imports):
```rust
use amlich_core::{
    calculate_day_snapshot, enrich_day_snapshot_with_direction_cross_link,
    enrich_day_snapshot_with_iching, build_day_snapshot_graph,
};
use amlich_core::iching::{
    cast_mai_hoa, classify_the_dung, derive_bien_que, IChingQuery,
};
use amlich_core::reasoning::DATE_ONLY_BIRTH_CHI_INDEX;
use amlich_core::semantic_graph::{EdgeConcept, NodeConcept};
use amlich_core::sources::{
    SOURCE_KINH_DICH, SOURCE_KHCBPPT, SOURCE_HUYEN_KHONG, SOURCE_MAI_HOA_DICH_SO,
};
```

**Date set construction** (mirror Phase 19 INT-10's pattern at lines 274-296):
- Push Tết 2026 (2026-02-17).
- Call the existing `collect_lunar_day_dates(1)` helper and filter to solar months `[3, 6, 9, 12]` (same filter as Phase 19; WARNING 3 in the existing file documents that this filters on SOLAR month, which is acceptable for representative-date coverage).
- Dedup via the existing HashSet retain pattern.
- Assert `dates.len() >= 5` (Tết + ≥4 Sóc dates; matches Phase 19's discipline).

**Per-date invariant block** (inside the `for &(d, m, y) in &dates` loop):
For each representative date, build a `let snap = calculate_day_snapshot(d, m, y);` then exercise ALL FIVE v1.7 surfaces together:

1. **IChingQuery construction** — `let query = IChingQuery::from_snapshot(&snap, Some("việc hôm nay".to_string()), 9).expect("...");` (chi_hour_index = 9 is the Thân hour, a valid 0..=11 value; mirrors `iching_evaluator_integration.rs` line 62).

2. **Phase 22 casting chain end-to-end** — derive lunar inputs from `snap.context.lunar` + `snap.context.canchi.year.chi_index`, then call:
   - `let cast = cast_mai_hoa(year_branch, month, day, hour);`
   - `let bien = derive_bien_que(&cast);`
   - `let the_dung = classify_the_dung(&cast);`
   - Assert `(1..=64).contains(&cast.chu_que.0)` and `(1..=64).contains(&bien.king_wen.0)`.
   - Assert `cast.chu_que != bien.king_wen` (CRIT-4 — a line flip ALWAYS changes the hexagram; this is the runtime invariant the 384-case contract test pins at the unit level; assert it here at the E2E level too).
   - Assert `(1..=6).contains(&cast.dong_hao)`.
   - Assert `matches!(the_dung.verdict, CatHung::Cat | CatHung::Binh | CatHung::Hung)` (the verdict surface is exercised, not the verdict value — different dates will produce different verdicts and that's correct).

3. **Phase 24-01 immutable IChing enrichment** — `let enriched_iching = enrich_day_snapshot_with_iching(&snap, query).expect("iching enrichment ok");` then assert:
   - `enriched_iching.iching_cast.is_some()` (the field is populated).
   - `snap.iching_cast.is_none()` (CRITICAL — input snapshot is NOT mutated; immutable-enrichment contract per Plan 24-01).
   - `let summary = enriched_iching.iching_cast.as_ref().unwrap();`
   - `summary.evidence.len() == 4` (CRIT-6 contract: exactly 4 envelopes).
   - The 4 envelopes are: 2 with `source_id == SOURCE_MAI_HOA_DICH_SO` (cast_mai_hoa + derive_bien_que methods), 1 with `source_id == SOURCE_KINH_DICH` (corpus_lookup method), 1 with `source_id == "rule.composite.iching_consultation"` (composite; method `iching_consultation`). Verify by counting: collect `source_ids` into a `Vec<&str>`, then assert counts: `mai-hoa-dich-so` appears exactly 2 times, `kinh-dich` exactly 1 time, the composite exactly 1 time. (Use the registered SOURCE_* consts for the first three; use the bare literal `"rule.composite.iching_consultation"` for the composite since it's an assertion target, not a production call-site — mirrors Plan 24-03 Test 11's discipline.)
   - `(1..=64).contains(&summary.chu_king_wen_index())` and `(1..=64).contains(&summary.bien_king_wen_index())`.
   - `summary.chu_king_wen_index() != summary.bien_king_wen_index()` (CRIT-4 echoed at the summary level).
   - `(1..=6).contains(&summary.moving_line)`.
   - `matches!(summary.cat_hung_summary.as_str(), "cat" | "binh" | "hung")`.

4. **Phase 23-03 immutable direction cross-link enrichment** — `let enriched_both = enrich_day_snapshot_with_direction_cross_link(&enriched_iching, DATE_ONLY_BIRTH_CHI_INDEX).expect("cross-link enrichment ok");` (use the date-only variant — no birth data required; mirrors the Tier-0 discipline). Assert:
   - `enriched_both.direction_cross_link.is_some()`.
   - `enriched_iching.direction_cross_link.is_none()` (immutable-enrichment contract — the input to step 4 is unchanged).
   - `enriched_both.iching_cast.is_some()` (CRITICAL — the IChing field is preserved across the cross-link enrichment; both v1.7 fields coexist).
   - `let cross = enriched_both.direction_cross_link.as_ref().unwrap();`
   - `cross.cells.len() == 8` (locked 8-direction surface per Phase 23 contract).
   - `cross.evidence.len() >= 3` (≥1 KHCBPPT primitive + ≥1 HUYEN_KHONG primitive + ≥1 composite). Collect source_ids and assert: at least one entry `== SOURCE_KHCBPPT`, at least one `== SOURCE_HUYEN_KHONG`, at least one `.starts_with("rule.composite.")`.
   - `cross.cross_link_source.starts_with("rule.composite.")` (composite envelope carries the standard prefix).

5. **Phase 24-02 semantic-graph wiring** — `let graph = build_day_snapshot_graph(&enriched_both);` then assert ALL of the following together (this is the unified v1.7 surface — Hexagram nodes + Transforms edge + Direction composite):
   - Count `NodeConcept::Hexagram` nodes: `let hex_count = graph.nodes().values().filter(|n| matches!(n.concept, NodeConcept::Hexagram)).count();` then `assert!(hex_count >= 2, ...)` (chu + bien; the role-bearing stable keys prevent collision per Plan 24-02).
   - Count `EdgeConcept::Transforms` edges: `let transforms_count = graph.edges().values().filter(|e| matches!(e.label.concept, EdgeConcept::Transforms)).count();` then `assert!(transforms_count >= 1, ...)`.
   - Count `NodeConcept::Direction` nodes: `let direction_count = graph.nodes().values().filter(|n| matches!(n.concept, NodeConcept::Direction)).count();` then `assert!(direction_count >= 1, ...)` (the composite cross-link fact node from `add_direction_composite_facts`).
   - Compare against the UNERICHED snapshot's graph: `let base_graph = build_day_snapshot_graph(&snap);` then count its Hexagram + Direction nodes. Assert the enriched graph has STRICTLY more Hexagram nodes than the base (`hex_count > base_hex_count`), proving the IChing facts are wired by the enrichment (not pre-existing). Same for Direction nodes.

**Test documentation comment** (place above the `#[test]` attribute): a section header `// ---------------------------------------------------------------------------\n// Phase 25 (INT-13) E2E smoke — v1.7 IChing + cross-link unified wiring\n// ---------------------------------------------------------------------------` plus a doc-comment block mirroring the file's existing Phase 19 INT-10 doc-comment discipline (lines 233-272 of the current file): state the surfaces exercised, the date selection rationale (Tết + 4 Sóc dates spanning distinct lunar months), and the WARNING 3 caveat (solar-month filter is a known imprecision but produces valid representative dates).

**Discipline constraints** (CRITICAL — these are non-negotiable per Phase 20 ADRs and Phase 22-24 SUMMARYs):
- Use registered SOURCE_* consts (SOURCE_KINH_DICH / SOURCE_MAI_HOA_DICH_SO / SOURCE_KHCBPPT / SOURCE_HUYEN_KHONG) for production-call-site-equivalent assertions. The ONLY bare string literals allowed are: (a) `"rule.composite.iching_consultation"` and `"rule.composite."` (composite-source-id contract assertions, not production call-sites — mirrors Plan 24-03 Test 11 discipline), and (b) test-infrastructure strings (error messages, the example `"việc hôm nay"` question).
- Do NOT add any new dependency to Cargo.toml — the test must compile against the existing v1.6-baseline 4-dep tree (serde + serde_json + chrono + unicode-normalization).
- Do NOT modify any production source file — this task touches ONLY `tests/integration_2026_smoke.rs`.
- Do NOT remove or alter the existing 4 tests in the file — purely additive.
  </action>
  <verify>
    <automated>cd /home/noy/work/amlich && cargo test --package amlich-core --test integration_2026_smoke e2e_2026_smoke_v17_iching_and_cross_link_wiring_on_representative_dates -- --exact --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    The new test `e2e_2026_smoke_v17_iching_and_cross_link_wiring_on_representative_dates` exists in `crates/amlich-core/tests/integration_2026_smoke.rs`, compiles cleanly, and passes on first run. The existing 4 tests in the file still pass (zero regressions). The test exercises ALL five v1.7 surfaces (cast_mai_hoa + derive_bien_que + classify_the_dung + enrich_day_snapshot_with_iching + enrich_day_snapshot_with_direction_cross_link + build_day_snapshot_graph) on Tết 2026 + ≥4 Sóc dates, asserts the immutable-enrichment contract on both enrichment helpers (input snapshot unchanged), asserts the CRIT-6 4-envelope evidence contract on IChingCastSummary, asserts the 8-cell + dual-source contract on DirectionCrossLinkSummary, and asserts the semantic graph carries strictly more Hexagram + Direction nodes after enrichment than the base snapshot.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add v17_baseline_guards.rs (dep-tree + golden-dataset INT-13 sentinel)</name>
  <files>crates/amlich-core/tests/v17_baseline_guards.rs</files>
  <action>
CREATE a new test file `crates/amlich-core/tests/v17_baseline_guards.rs` with TWO tests that lock Phase 25's SC1 (cross-source golden discipline) and SC4 (cargo dependency tree unchanged) as runtime-enforced invariants. The file has NO production-code dependencies and NO new external crates — it uses only `amlich_core`'s public API plus the std library.

**File-level doc-comment** (lines 1-15): describe the file's purpose — "Phase 25 baseline guards: lock the v1.6 cargo dependency tree shape (SC4) and the Mai Hoa golden dataset's cross-source discipline (SC1) as runtime-invariant tests. These guards ensure the v1.7 milestone's locked contracts cannot silently drift in v1.8+." Mention that the file is purely additive (no production code touched).

**Test 1: `cargo_dependency_tree_unchanged_from_v16`** (SC4 — locks the dep tree shape):
- Embed `crates/amlich-core/Cargo.toml` at compile time: `const CARGO_TOML: &str = include_str!("../Cargo.toml");` (the test file is in `crates/amlich-core/tests/`, so `../Cargo.toml` resolves to `crates/amlich-core/Cargo.toml`).
- Locate the `[dependencies]` section: find the byte offset of `"\n[dependencies]"` and the next `"\n["` after it (the start of the next section, e.g. `[dev-dependencies]`). Extract the substring between them as the deps section text. If either marker is missing, panic with a clear message ("Cargo.toml shape changed: [dependencies] section not found" / "Cargo.toml shape changed: unterminated [dependencies] section").
- Parse the deps section: split by newlines, filter to non-empty trimmed lines that are NOT comments (don't start with `#`) and NOT section markers (don't start with `[`). For each remaining line, extract the dependency name as the substring before the first `=` (trimmed). Collect names into a `Vec<&str>`.
- Assert EXACTLY 4 entries: `assert_eq!(names.len(), 4, "SC4 violation: Cargo.toml [dependencies] must have exactly 4 entries (v1.6 baseline); got {}: {:?}", names.len(), names);`.
- Assert the names match the locked set: sort both `names` and the expected `["chrono", "serde", "serde_json", "unicode-normalization"]`, then `assert_eq!(names_sorted, expected_sorted, "SC4 violation: Cargo.toml [dependencies] names must equal the v1.6 baseline set; got {:?}", names);`. (Sorting both sides lets the test pass regardless of declaration order in Cargo.toml — locks the SET of deps, not the order.)
- Include a brief doc-comment explaining the v1.6 baseline reference (922-test v1.6 milestone, shipped 2026-07-16; dep tree at that point: chrono + serde + serde_json + unicode-normalization) and that v1.7 added NO new deps despite shipping 448-text-field corpus + Mai Hoa + biến quẻ + Thể/Dụng + evaluator + cross-link + semantic-graph wiring.

**Test 2: `int13_golden_dataset_cross_source_discipline_holds`** (SC1 — locks the cross-source discipline):
- Call `let ds = amlich_core::iching::load_mai_hoa_golden();` (Phase 22-02 loader; the loader itself already validates ≥10 cases + ≥2 sources per case + ≥1 KnownDivergence at load time — this test re-asserts the same invariants EXPLICITLY at the INT-13 level, so a future weakening of the loader would still trip this test).
- Assert `ds.cases.len() >= 10` with an INT-13-specific message: `"INT-13 SC1 violation: need >= 10 golden cases, got {}"`.
- For each case, assert `case.sources.len() >= 2` with message `"INT-13 SC1 violation: case '{}' must have >= 2 sources (FS-10 dual-source), got {}"`.
- For each case, assert that AT LEAST ONE source entry has `source.contains("nhantu.net")` (the canonical first reference per INT-13's wording). Message: `"INT-13 SC1 violation: case '{}' must have at least one nhantu.net source entry; got sources: {:?}"`. Use `.sources.iter().any(|s| s.source.contains("nhantu.net"))`.
- Assert `!ds.known_divergences.is_empty()` with message `"INT-13 SC1 violation: known_divergences must be non-empty (divergences logged, not silently corrected per AF-05)"`.
- For each divergence, assert non-empty `our_value`, `tiebreaker`, and `note` fields (mirrors Phase 22-02's existing `golden_known_divergences_are_logged_not_corrected` test at `mai_hoa_the_dung_integration.rs:283`, but with INT-13-specific messages so a future regression points back to this milestone closure).
- Assert the dataset's `schema_version == "mai-hoa-golden-v1"` (lock the schema pin so a future v2 schema cannot silently invalidate the SC1 contract).
- Include a brief doc-comment explaining that INT-13 SC1 is met by Phase 22-02's mai_hoa_golden.json dataset (12 cases × 2 sources each + 2 KnownDivergence rows), and this test is the formal Phase 25 closure sentinel.

**File footer** (after the second test): a brief comment line `// Phase 25 (INT-13) baseline guards — closure of the v1.7 milestone. SC1 + SC4 locked as runtime invariants.`

**Discipline constraints**:
- The file imports ONLY from `amlich_core`'s public API + std. No new dependencies.
- The `include_str!` path is `"../Cargo.toml"` (relative to the test file's location at `crates/amlich-core/tests/`). Verify this resolves correctly by checking that `crates/amlich-core/Cargo.toml` exists at that relative path (it does — the `tests/` directory is a sibling of `Cargo.toml` under `crates/amlich-core/`).
- Do NOT use `std::process::Command` to invoke `cargo tree` at test time (slow + brittle across cargo versions). The Cargo.toml-parsing approach is faster and locks the same invariant.
- Do NOT modify the existing `Cargo.toml` or `mai_hoa_golden.json` — this task only ADDS a test that reads them.
  </action>
  <verify>
    <automated>cd /home/noy/work/amlich && cargo test --package amlich-core --test v17_baseline_guards -- --exact --nocapture 2>&1 | tail -20</automated>
  </verify>
  <done>
    The new file `crates/amlich-core/tests/v17_baseline_guards.rs` exists with TWO tests. `cargo_dependency_tree_unchanged_from_v16` passes (asserts exactly 4 production deps with the locked v1.6 names). `int13_golden_dataset_cross_source_discipline_holds` passes (asserts ≥10 cases, every case has ≥2 sources with nhantu.net in ≥1 of them, ≥1 KnownDivergence with non-empty fields, schema_version pinned). The file uses ONLY `amlich_core`'s public API + std (no new deps). Zero production files modified.
  </done>
</task>

</tasks>

<verification>
After both tasks complete, run the full crate test suite to confirm SC3 (full test suite green + zero regressions on v1.6 baseline):

```bash
cd /home/noy/work/amlich && cargo test --package amlich-core 2>&1 | tail -10
```

Expected: all tests pass (≥1119 tests green = 1117 baseline from end of Phase 24 + 1 new test in `integration_2026_smoke.rs` + 2 new tests in `v17_baseline_guards.rs` = 1120 total; the exact count may differ slightly if the new smoke test's date set produces variant counts, but the test count must be ≥1119 with zero failures).

Additionally, manually verify the cargo dependency tree shape one final time:
```bash
cd /home/noy/work/amlich && cargo tree -p amlich-core --depth 1
```
Expected output (exactly 4 deps, no more):
```
amlich-core v0.1.4 (/home/noy/work/amlich/crates/amlich-core)
├── chrono v0.4.44
├── serde v1.0.228
├── serde_json v1.0.149
└── unicode-normalization v0.1.25
```

Phase 25 closure checklist (all 4 SCs):
- [x] SC1 — `int13_golden_dataset_cross_source_discipline_holds` passes; mai_hoa_golden.json carries 12 cases × 2 sources + 2 KnownDivergence rows (already shipped in Phase 22-02; locked as runtime invariant by Task 2)
- [x] SC2 — `e2e_2026_smoke_v17_iching_and_cross_link_wiring_on_representative_dates` passes; exercises IChing casting + biến quẻ + Thái Tuế cross-link + semantic-graph wiring + DaySnapshot fields together on Tết 2026 + ≥4 Sóc dates (Task 1)
- [x] SC3 — `cargo test --package amlich-core` exits 0 with all tests green (zero regressions on v1.6 baseline; the v1.6 test files continue to pass alongside the new v1.7 tests)
- [x] SC4 — `cargo_dependency_tree_unchanged_from_v16` passes; `cargo tree -p amlich-core --depth 1` shows exactly the 4 locked deps (Task 2 + manual verification)
</verification>

<success_criteria>
- The new v1.7 E2E smoke test passes on Tết 2026 + ≥4 Sóc dates, exercising all five v1.7 surfaces together (IChing casting chain + immutable enrichment for both IChing + cross-link + semantic-graph wiring)
- The cargo dependency tree is locked at exactly 4 production deps with the v1.6 names (serde + serde_json + chrono + unicode-normalization) — runtime-enforced by the new baseline guard test
- The Mai Hoa golden dataset's INT-13 cross-source discipline is locked at runtime (≥10 cases, ≥2 sources per case with nhantu.net in ≥1, ≥1 KnownDivergence row, schema pinned to mai-hoa-golden-v1)
- The full crate test suite passes with zero regressions versus the v1.6 baseline shape
- INT-13 is fully closed and the v1.7 milestone is ready for `/gsd-complete-milestone`
</success_criteria>

<output>
After completion, create `.planning/phases/25-e2e-validation-golden-cross-source-verification/25-01-SUMMARY.md` documenting:
- The new test names + line counts
- The exact crate test count at plan completion (must be ≥1119, the Phase 24-end baseline + 3 new tests)
- The cargo tree output snapshot (4 deps)
- Confirmation that INT-13's four success criteria are all met (with cross-references to the specific tests that verify each)
- Any deviations from the plan (e.g. if the date set produces a different Sóc-date count than expected) with rationale
- Closure statement: "Phase 25 complete (1/1 plans). INT-13 closed. v1.7 milestone ready for `/gsd-complete-milestone`."
</output>
