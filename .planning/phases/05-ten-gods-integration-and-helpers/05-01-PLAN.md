---
phase: 05-ten-gods-integration-and-helpers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/almanac/dai_van.rs
autonomous: true
requirements: [DV-TG-01, DV-TG-02, DV-TG-03]
must_haves:
  truths:
    - "Users can request Ten Gods for a specific Dai Van pillar using birth day stem as the source"
    - "Ten Gods values are calculated only when helper APIs are called, not precomputed in base Dai Van generation"
    - "When birth day stem is unavailable (unknown birth hour path), Ten Gods helpers return None without panic or hidden defaults"
  artifacts:
    - path: "crates/amlich-core/src/almanac/dai_van.rs"
      provides: "Lazy Ten Gods adapter APIs over Dai Van pillars and ages"
      exports: ["get_ten_god_for_pillar", "get_ten_god_for_age"]
      min_lines: 430
  key_links:
    - from: "crates/amlich-core/src/almanac/dai_van.rs"
      to: "crates/amlich-core/src/almanac/thap_than.rs"
      via: "lazy adapter calls get_thap_than(birth_day_stem, pillar_stem)"
      pattern: "get_thap_than\(day_stem, pillar_stem\)"
    - from: "crates/amlich-core/src/almanac/dai_van.rs"
      to: "crates/amlich-core/src/almanac/types.rs"
      via: "pillar stem parsing into HeavenlyStem before Ten Gods correlation"
      pattern: "HeavenlyStem::try_from"
---

<objective>
Add lazy Ten Gods enrichment helpers to Dai Van so Ten Gods can be correlated per pillar/age without changing Phase 4 base result shape.

Purpose: Satisfy Phase 5 Ten Gods requirements by composing existing `thap_than` logic with Dai Van helpers while preserving deterministic and explicit unknown-birth-hour behavior.
Output: New Dai Van Ten Gods helper APIs and focused tests covering orientation, lazy behavior, and `Option` handling.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-ten-gods-integration-and-helpers/05-RESEARCH.md
@.planning/phases/04-core-dai-van-module/04-02-SUMMARY.md
@crates/amlich-core/src/almanac/dai_van.rs
@crates/amlich-core/src/almanac/thap_than.rs
@crates/amlich-core/src/almanac/types.rs

<interfaces>
From `crates/amlich-core/src/almanac/thap_than.rs`:
```rust
pub fn get_thap_than(day_can: HeavenlyStem, target_can: HeavenlyStem) -> ThapThanResult
```

From `crates/amlich-core/src/almanac/dai_van.rs`:
```rust
pub fn get_pillar_at_age(result: &DaiVanResult, age: f64) -> Option<&DaiVanPillar>
pub fn get_current_pillar(result: &DaiVanResult, age: f64) -> Option<&DaiVanPillar>
```

From `crates/amlich-core/src/almanac/types.rs`:
```rust
impl TryFrom<&str> for HeavenlyStem {
    type Error = String;
}
```
</interfaces>

# Implementation constraints:
# - Keep Ten Gods out of `DaiVanResult` struct to preserve DV-TG-02 lazy behavior
# - Always call `get_thap_than(birth_day_stem, pillar_stem)` in this direction
# - Use `Option<HeavenlyStem>` as explicit unknown-birth-hour input contract
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add lazy Ten Gods adapter APIs in Dai Van module</name>
  <files>crates/amlich-core/src/almanac/dai_van.rs</files>
  <behavior>
    - Test 1: Given valid day stem and in-range age, helper returns Ten Gods for that pillar stem
    - Test 2: Given missing day stem (`None`), helper returns `None` instead of panic/default
    - Test 3: Given out-of-range age or invalid pillar index, helper returns `None`
  </behavior>
  <action>
    Add two public helpers to `dai_van.rs`:
    - `get_ten_god_for_pillar(result, pillar_index, birth_day_stem)`
    - `get_ten_god_for_age(result, age, birth_day_stem)`

    Implement both as lazy adapters that:
    - resolve pillar only for the requested index/age
    - parse pillar stem via `HeavenlyStem::try_from(pillar.can_chi.can.as_str())`
    - call `get_thap_than(day_stem, pillar_stem)` with correct direction
    - return `Option<ThapThanResult>` for missing stem or invalid lookup paths
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib dai_van::tests::ten_gods_helpers</automated>
  </verify>
  <done>
    Dai Van exposes lazy Ten Gods query APIs for pillar index and age
    Unknown day stem and invalid lookup paths are explicit `None`
    API contract is deterministic and compile-time typed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Lock Ten Gods integration semantics with focused tests</name>
  <files>crates/amlich-core/src/almanac/dai_van.rs</files>
  <behavior>
    - Test 1: Stem orientation regression check fails if arguments are reversed
    - Test 2: Calling `calculate_dai_van` does not eagerly compute/store Ten Gods data
    - Test 3: Repeated Ten Gods helper calls with same input return stable equal outputs
  </behavior>
  <action>
    Add targeted tests under a dedicated `ten_gods_helpers` test module in `dai_van.rs` to harden integration invariants:
    - orientation check comparing known fixture pair against expected label
    - lazy behavior check ensuring base Dai Van result shape remains unchanged and Ten Gods are derived only through helper calls
    - deterministic repeated-call test for helper outputs

    Keep tests narrowly scoped to Phase 5 concerns and do not modify Ten Gods engine logic in `thap_than.rs`.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib dai_van::tests::ten_gods_helpers</automated>
  </verify>
  <done>
    Ten Gods integration risks (orientation, eager-compute drift, nondeterminism) are covered by stable tests
    Phase 5 Ten Gods requirements are fully test-backed
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --package amlich-core --lib dai_van::tests::ten_gods_helpers
```
</verification>

<success_criteria>
1. Each Dai Van pillar can be correlated to Ten Gods from birth day stem through the existing Ten Gods engine (DV-TG-01)
2. Ten Gods values are computed only on helper query paths and not embedded into base Dai Van generation (DV-TG-02)
3. Unknown birth hour/day-stem path is explicit and safe via `Option` returns (DV-TG-03)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ten-gods-integration-and-helpers/05-01-SUMMARY.md`
</output>
