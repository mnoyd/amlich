---
phase: 23-th-i-tu-tam-s-t-phi-tinh-cross-link
verified: 2026-07-16T16:05:00Z
status: passed
score: 7/7 must-haves verified
---

# Phase 23: Thái Tuế / Tam Sát ⇄ Phi Tinh Cross-Link Verification Report

**Phase Goal:** User-of-directions can find a Thái Tuế directional derivation + classical 3-direction Tam Sát module (both carrying `source_id: khcbppt` evidence with the two `evidence: None` backfills populated) AND a read-only `build_direction_cross_link` that surfaces BOTH the KHCBPPT Thái Tuế/Tam Sát directional taboos AND the `huyen-khong` Phi Tinh palace layout in one composite picture — with CRIT-3 isolation preserved.

**Verified:** 2026-07-16T16:05:00Z
**Status:** passed
**Re-verification:** No — initial verification

## Goal Achievement

### Observable Truths

| #   | Truth                                                                                                                                                                                                                                                                                                | Status     | Evidence                                                                                                                                                                                                                                                                    |
| --- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | A caller can invoke a directional Thái Tuế `pub fn` on `thai_tue.rs` mapping year-chi → `Direction8`, distinct from the existing personal-conflict-only Thái Tuế, carrying `source_id: khcbppt` evidence.                                                                                              | ✓ VERIFIED | `thai_tue.rs:185` `pub fn thai_tue_direction(year_chi_index: usize) -> ThaiTueDirectionResult` returns `Direction` (8-variant enum); evidence at `:196` is `SOURCE_KHCBPPT`. `compute_thai_tue` personal API at `:77` unchanged. 12 branch coverage + 4 cardinal direction tests pass. |
| 2   | The two 1-line `evidence: None` backfills on `thai_tue.rs` and `sat_phuong.rs` are populated.                                                                                                                                                                                                       | ✓ VERIFIED | `thai_tue.rs:134-138` returns `Some(RuleEvidence { source_id: SOURCE_KHCBPPT, method: "thai_tue_year_branch_conflict", profile: "baseline" })`; `sat_phuong.rs:53-57` similarly returns `Some(RuleEvidence { source_id: SOURCE_KHCBPPT, method: "sat_phuong_day_chi", ... })`. Backward-compat gate `tests/almanac_backfill_compat.rs` (211 lines, 6 tests) confirms v1.6 legacy JSON without `evidence` still deserializes to `None`. |
| 3   | A caller can invoke a classical 3-direction Tam Sát module (`almanac/tam_sat.rs`) returning the THREE contiguous sơn/directions per year from the Tam Hợp triad opposition, carrying `source_id: khcbppt` with a KHCBPPT-pinned citation.                                                              | ✓ VERIFIED | `tam_sat.rs:141` `pub fn tam_sat_direction(year_chi_index: usize) -> TamSatDirectionResult` returns `[Direction; 3]` + `[String; 3]` branches via tradition-ordered `TAM_SAT_ROWS` (Water/Wood/Fire/Metal). Evidence profile string carries `PendingExternalReview` marker + provenance artifact reference. `tests/tam_sat_integration.rs` (10 tests) confirms all four triad rows + all-12-branch coverage + exactly-three output lengths. |
| 4   | The existing single-direction `sat_phuong.rs` day-chi feature stays intact.                                                                                                                                                                                                                          | ✓ VERIFIED | `sat_phuong.rs:24-37` `SAT_PHUONG_BY_CHI` table unchanged; `tests/tam_sat_integration.rs::get_sat_phuong_day_chi_values_remain_unchanged` asserts the 12 day-chi values stay locked.                                                                                        |
| 5   | A caller can invoke `build_direction_cross_link(snapshot, birth_chi_index) -> PersonalFactNode` in `reasoning/direction_composite.rs` that surfaces BOTH the KHCBPPT Thái Tuế/Tam Sát directional taboos AND the `huyen-khong` Phi Tinh palace layout in one composite fact node, emitting distinct primitive `source_id` envelopes (`khcbppt` + `huyen-khong`) plus one `rule.composite.direction_cross_link` envelope. | ✓ VERIFIED | `direction_composite.rs:916` `pub fn build_direction_cross_link(...) -> Result<PersonalFactNode, String>` wraps the personal builder and returns `id: "fact.personal.direction_cross_link"` (test at `direction_cross_link_integration.rs:385`). `build_evidence` (`:582-626`) constructs exactly 3 envelopes: KHCBPPT primitive (`SOURCE_KHCBPPT`), huyen-khong primitive (`SOURCE_HUYEN_KHONG`, runtime-built method == `phi_tinh.palace_layout`), and Derived composite (`COMPOSITE_DIRECTION_CROSS_LINK`). `exactly_two_primitive_source_ids_plus_one_composite` test (`:354-375`) confirms the count distribution. |
| 6   | `build_direction_cross_link` takes only `&` references (read-only by design).                                                                                                                                                                                                                        | ✓ VERIFIED | `direction_composite.rs:833`/`:875`/`:916` all take `&DaySnapshot`; `lib.rs:304` `enrich_day_snapshot_with_direction_cross_link` is the only mutation path, and it operates on `snapshot.clone()` (`:317`). `enrichment_attaches_summary_and_leaves_input_unchanged` test asserts input byte-equal before/after. |
| 7   | CRIT-3 isolation is preserved: no `FlyingStar` reference in `interaction/direction_merge.rs` (verified by a sibling `tests/thai_tue_cross_link_crit3.rs` grep guard).                                                                                                                                | ✓ VERIFIED | `tests/thai_tue_cross_link_crit3.rs` defines `FORBIDDEN_TYPE_NAMES` (7 patterns: `almanac::fengshui`, `phi_tinh`, `compute_daily_flying_stars`, `compute_combined_overlay`, `compute_palace_aspects`, `TietKhiScanner`, `FlyingStarPeriod`) and scans exactly `SCAN_TARGETS` (2 files: `direction_merge.rs`, `direction_composite.rs`). Two defensive contract tests pin both lists. Manual grep confirms zero forbidden substrings in the two scan targets. |

**Score:** 7/7 truths verified

### Required Artifacts

| Artifact                                                                  | Expected                                                                                              | Status     | Details                                                                                                                                                                                            |
| ------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `crates/amlich-core/src/almanac/thai_tue.rs`                              | `ThaiTueDirectionResult` + `thai_tue_direction` sibling + backfilled `compute_thai_tue` evidence      | ✓ VERIFIED | 360 lines; `pub fn thai_tue_direction` at `:185`; sibling struct `:54`; `compute_thai_tue` evidence backfilled at `:134-138`; 9 inline tests cover all branches + backfill contract.              |
| `crates/amlich-core/src/almanac/tam_sat.rs`                               | Classical 3-direction Tam Sát + opposite-triad lookup                                                 | ✓ VERIFIED | 221 lines; `pub fn tam_sat_direction` at `:141`; `TAM_SAT_ROWS` tradition-ordered at `:39-80`; 5 inline tests.                                                                                     |
| `crates/amlich-core/src/almanac/sat_phuong.rs`                            | Backfilled evidence                                                                                   | ✓ VERIFIED | 117 lines; `get_sat_phuong` evidence backfilled at `:53-57`; `SAT_PHUONG_BY_CHI` table intact; `evidence_defaults_to_none` test renamed and asserts populated source.                             |
| `crates/amlich-core/tests/almanac_backfill_compat.rs`                     | v1.6 missing-evidence deserialization + v1.7 round-trip gates (≥70 lines)                             | ✓ VERIFIED | 211 lines, 6 tests (≥70 line minimum satisfied).                                                                                                                                                  |
| `crates/amlich-core/data/almanac/tam_sat_provenance.md`                   | Discoverable pending-review ledger with `PendingExternalReview` marker                                | ✓ VERIFIED | 96 lines; status line `PendingExternalReview` at `:3`; upgrade path documented `:69-78`; rule statement + 4 mapping rows + search criteria all present.                                           |
| `crates/amlich-core/src/reasoning/direction_composite.rs`                 | Read-only builders, rich cell assembly, agreement/severity, Vietnamese summary, 3-envelope provenance | ✓ VERIFIED | 1212 lines (≥260 line minimum satisfied); `pub fn build_direction_cross_link` at `:916`; `_personal` at `:833`; `_date` at `:875`; `project_to_summary` at `:931`; 18 inline tests.                |
| `crates/amlich-core/src/lib.rs`                                           | `enrich_day_snapshot_with_direction_cross_link` helper + additive `DaySnapshot.direction_cross_link`  | ✓ VERIFIED | Helper at `lib.rs:304-320`; field at `:201` with `#[serde(default, skip_serializing_if = "Option::is_none")]`; initialized to `None` at `:387`; not auto-populated by `calculate_day_snapshot_internal`. |
| `crates/amlich-core/tests/direction_cross_link_integration.rs`            | Black-box public API contract tests (≥180 lines)                                                      | ✓ VERIFIED | 522 lines, 22 tests (≥180 line minimum satisfied); covers both builders, evidence shape, agreement/severity, enrichment immutability, serde round-trip, sentinel dispatch, error path, Tam Sát count. |
| `crates/amlich-core/tests/thai_tue_cross_link_crit3.rs`                   | Sibling grep guard with locked 7 forbidden patterns and 2 scan targets                                | ✓ VERIFIED | 111 lines; `FORBIDDEN_TYPE_NAMES` (7 entries) at `:29-37`; `SCAN_TARGETS` (2 entries) at `:40-43`; 3 tests (scan + 2 defensive contract tests).                                                    |

### Key Link Verification

| From                                                                  | To                                                                              | Via                                                                                       | Status   | Details                                                                                                                                                              |
| --------------------------------------------------------------------- | ------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------- | -------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `thai_tue.rs::thai_tue_direction`                                     | `tu_menh.rs::Direction`                                                         | year-chi branch-to-eight-direction match; no new directional type                         | ✓ WIRED  | `thai_tue.rs:10` `use super::tu_menh::Direction`; `:190` calls `direction_for_year_chi` returning `Direction`; no `Direction8`/`Direction4` types minted.            |
| `tam_sat.rs::tam_sat_direction`                                       | `types.rs::RuleEvidence`                                                        | SOURCE_KHCBPPT-backed result evidence                                                     | ✓ WIRED  | `tam_sat.rs:26` `use crate::sources::SOURCE_KHCBPPT`; `:165` populates `source_id: SOURCE_KHCBPPT.to_string()`.                                                      |
| `sat_phuong.rs::get_sat_phuong`                                       | `tests/almanac_backfill_compat.rs`                                              | populated evidence remains serde-compatible with evidence omitted                         | ✓ WIRED  | `sat_phuong.rs:53` returns `Some(RuleEvidence {...})`; `tests/almanac_backfill_compat.rs` legacy-JSON tests confirm missing `evidence` deserializes to `None`.        |
| `direction_composite.rs::DIRECTION_ORDER`                             | `interaction/direction_merge.rs::ALL_DIRECTIONS`                               | same North, Northeast, East, Southeast, South, Southwest, West, Northwest order            | ✓ WIRED  | `direction_composite.rs:54-63` DIRECTION_ORDER constant matches the canonical 8-direction order; integration test `:60-82` asserts cell order equals DIRECTION_ORDER. |
| `lib.rs::FlyingStarsSummary::palace_safety_hints`                     | `lib.rs::calculate_day_snapshot_internal`                                       | annual palace safety text precomputed into the DTO                                        | ✓ WIRED  | `lib.rs:160` field declared; `:407-409` `std::array::from_fn` + `element_hint_for_palace` populates the projection inside the snapshot constructor.                  |
| `lib.rs::DaySnapshot::direction_cross_link`                           | `direction_composite.rs::DirectionCrossLinkSummary`                             | additive Option<T> field with serde default/skip attributes                               | ✓ WIRED  | `lib.rs:201` field type `crate::reasoning::DirectionCrossLinkSummary`; `:387` init to `None`; JSON omission test at `direction_cross_link_integration.rs:420-431`.    |
| `direction_composite.rs`                                              | `thai_tue.rs + tam_sat.rs + sat_phuong.rs`                                      | read-only primitive calls assemble the KHCBPPT side                                       | ✓ WIRED  | `direction_composite.rs:27-29` imports `get_sat_phuong`, `tam_sat_direction`, `compute_thai_tue, thai_tue_direction`; all four are called inside the builders.        |
| `direction_composite.rs`                                              | `lib.rs::DaySnapshot::flying_stars`                                             | DTO-only annual palace/star/safety-hint projection                                        | ✓ WIRED  | `direction_composite.rs:441-446` `snapshot.flying_stars.as_ref().ok_or_else(...)`; `:447` reads `.palace_safety_hints`; `:454` reads `.palace_overlays[i].0 as u8`. |
| `direction_composite.rs`                                              | `types.rs::ReasoningEvidenceEnvelope`                                           | two primitive source envelopes plus one composite envelope                                | ✓ WIRED  | `direction_composite.rs:36` imports `ReasoningEvidenceEnvelope`; `build_evidence` (`:582-626`) constructs exactly 3 envelopes with the locked source_id distribution. |
| `lib.rs::enrich_day_snapshot_with_direction_cross_link`               | `direction_composite::project_to_summary`                                       | explicit clone-and-attach operation                                                       | ✓ WIRED  | `lib.rs:312-318` dispatches sentinel → date builder, otherwise → personal builder, calls `project_to_summary`, then `snapshot.clone()` + `enriched.direction_cross_link = Some(summary)`. |
| `thai_tue_cross_link_crit3.rs`                                        | `src/interaction/direction_merge.rs + src/reasoning/direction_composite.rs`     | file scan with exact seven forbidden patterns                                              | ✓ WIRED  | `thai_tue_cross_link_crit3.rs:40-43` SCAN_TARGETS exact; `:48-60` reads both files via `env!("CARGO_MANIFEST_DIR")` and asserts zero violations.                      |

### Requirements Coverage

| Requirement | Source Plan          | Description                                                                                                                                                                                                                                                                                                                                                      | Status     | Evidence                                                                                                                                                                                                                       |
| ----------- | -------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| XLK-01      | 23-01-PLAN.md        | User-of-directions can find a Thái Tuế directional derivation (`pub fn` on `thai_tue.rs` mapping year-chi → `Direction8`) distinct from the existing personal-conflict-only Thái Tuế, carrying `source_id: khcbppt` evidence (the two 1-line `evidence: None` backfills on `thai_tue.rs` + `sat_phuong.rs` are populated).                                       | ✓ SATISFIED | `thai_tue.rs:185 thai_tue_direction` exists, distinct from `compute_thai_tue`. Both backfills populated (`thai_tue.rs:134`, `sat_phuong.rs:53`). `tests/almanac_backfill_compat.rs` confirms backward compatibility.            |
| XLK-02      | 23-01-PLAN.md        | User-of-directions can find a classical Tam Sát directional module (`almanac/tam_sat.rs`) returning the THREE contiguous sơn/directions per year from the Tam Hợp triad opposition (per a KHCBPPT-pinned citation), carrying `source_id: khcbppt`; the existing single-direction `sat_phuong.rs` day-chi feature stays intact. *(Decision-dependent — DEC required first.)* | ✓ SATISFIED | `tam_sat.rs:141 tam_sat_direction` returns `[Direction; 3]` per year via `TAM_SAT_ROWS` triad opposition. `source_id: SOURCE_KHCBPPT`. Pending citation ledger at `tam_sat_provenance.md`. `sat_phuong.rs` table unchanged.      |
| XLK-03      | 23-02-PLAN.md + 23-03-PLAN.md | User-of-reasoning can find a read-only `build_direction_cross_link(snapshot, birth_chi_index)` in `reasoning/direction_composite.rs` that surfaces BOTH the KHCBPPT Thái Tuế/Tam Sát directional taboos AND the `huyen-khong` Phi Tinh palace layout in one composite fact node — emitting distinct primitive `source_id` envelopes (`khcbppt` + `huyen-khong`) plus one `rule.composite.direction_cross_link` envelope, with CRIT-3 isolation preserved (no `FlyingStar` reference in `interaction/direction_merge.rs`; grep-guarded by a sibling `tests/thai_tue_cross_link_crit3.rs`). | ✓ SATISFIED | `direction_composite.rs:916 build_direction_cross_link` returns `PersonalFactNode` with id `fact.personal.direction_cross_link`. 3-envelope shape verified by integration test. Sibling guard at `tests/thai_tue_cross_link_crit3.rs`. |

**Orphaned Requirements:** None. REQUIREMENTS.md maps only XLK-01, XLK-02, XLK-03 to Phase 23; all three are claimed by plans (XLK-01 → 23-01, XLK-02 → 23-01, XLK-03 → 23-02 + 23-03) and all three are satisfied with implementation evidence.

### Anti-Patterns Found

| File                                       | Line(s)    | Pattern                                | Severity | Impact                                                                                                                                                                                                                                                                                            |
| ------------------------------------------ | ---------- | -------------------------------------- | -------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| `direction_composite.rs`                   | 683, 726   | `let placeholder = DirectionalTaboo {` | ℹ️ Info  | Not a stub — the `placeholder` local variable is a fully-populated temporary `DirectionalTaboo` struct used to compute `khcbppt_summary_vi(direction, &placeholder)` before the final `Some(DirectionalTaboo { summary_vi, ... })` is returned on the next line. Variable name is unfortunate but the implementation is complete. |
| (no other anti-patterns found)             |            |                                        |          | No `TODO`/`FIXME`/`unimplemented!`/`return null`/placeholder-text patterns in the four production files. No bare corpus source-id literals (production uses `SOURCE_KHCBPPT`/`SOURCE_HUYEN_KHONG` constants — verified by passing `tests/source_id_guard.rs`).                                       |

**Blockers:** 0
**Warnings:** 0
**Info-only:** 1 (cosmetic variable name)

### Human Verification Required

None. All Phase 23 deliverables are pure-Rust deterministic computations with no UI surface, no real-time behavior, and no external service integration. The full contract is verified programmatically by 1062 passing tests (lib + integration + sibling guards), byte-equal serde round-trip assertions, and the CRIT-3 isolation grep guard.

The one honesty-discipline item flagged in SUMMARYs — the Tam Sát KHCBPPT exact edition/page citation is honestly deferred behind a `PendingExternalReview` marker in `data/almanac/tam_sat_provenance.md` — is the documented correct behavior per ADR-0006 §5 and is not a blocker for the runtime contract.

### Gaps Summary

No gaps. All 7 observable truths verified. All 9 required artifacts present, substantive, and wired. All 12 key links confirmed connected. All 3 requirements (XLK-01, XLK-02, XLK-03) satisfied. Zero blocker anti-patterns. Zero new crate dependencies (`cargo tree -p amlich-core --depth 1` shows only `chrono`, `serde`, `serde_json`, `unicode-normalization`).

**Test suite:** `cargo test -p amlich-core` → 1062 tests pass, 0 failures, 0 regressions.

Notable quality signals:
- CRIT-3 isolation is preserved by source-discipline (runtime-built method string `phi_tinh.palace_layout` avoids writing the forbidden substring verbatim) AND grep-guarded by `tests/thai_tue_cross_link_crit3.rs` (7 patterns × 2 targets + 2 defensive contract tests pinning both lists).
- TDD discipline: each plan documents RED → GREEN commits per task; all 11 task commit hashes (`f4adf03`, `4d324cb`, `7461548`, `f67c72d`, `401b248`, `9ff695f`, `6f0d73d`, `bf680e0`, `c3e8a74`, `4b79803`, `26fd7a3`) are present in `git log`.
- The additive snapshot field is correctly absence-preserving: `calculate_day_snapshot(...).direction_cross_link.is_none()` holds and the field is omitted from JSON when None (v1.6 → v1.7 byte-equal round-trip).
- The conservative-default composite severity tiebreak is documented in source and unit-tested with 3 inline cases covering the favorable-vs-Inauspicious tie, clear majority, and multi-way tie.

Phase 23 is complete. The goal — directional Thái Tuế + classical 3-direction Tam Sát (both with `khcbppt` evidence and the two backfills populated) + read-only `build_direction_cross_link` surfacing both KHCBPPT directional taboos and the Huyền-Không Phi Tinh palace layout in one composite picture, with CRIT-3 isolation preserved — is achieved.

---

_Verified: 2026-07-16T16:05:00Z_
_Verifier: Claude (gsd-verifier)_
