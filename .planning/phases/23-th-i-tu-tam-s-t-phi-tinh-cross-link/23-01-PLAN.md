---
phase: 23-th-i-tu-tam-s-t-phi-tinh-cross-link
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/almanac/thai_tue.rs
  - crates/amlich-core/src/almanac/sat_phuong.rs
  - crates/amlich-core/tests/almanac_backfill_compat.rs
  - crates/amlich-core/src/almanac/tam_sat.rs
  - crates/amlich-core/src/almanac/mod.rs
  - crates/amlich-core/tests/tam_sat_integration.rs
  - crates/amlich-core/data/almanac/tam_sat_provenance.md
autonomous: true
requirements:
  - XLK-01
  - XLK-02
must_haves:
  truths:
    - "A caller can derive the year-branch Thái Tuế direction through a new sibling API without changing the existing personal conflict calculation."
    - "Both existing ThaiTueResult and SatPhuongResult outputs carry KHCBPPT RuleEvidence while legacy JSON without evidence still deserializes."
    - "A caller can derive exactly three Tam Sát directions from every year chi using the locked opposite Tam Hợp triad mapping, while the existing day-chi Sát Phương result remains unchanged."
    - "The Tam Sát evidence points to a discoverable provenance artifact and explicitly marks the exact KHCBPPT page citation as pending review rather than inventing a page."
  artifacts:
    - path: "crates/amlich-core/src/almanac/thai_tue.rs"
      provides: "ThaiTueDirectionResult and thai_tue_direction(year_chi_index) sibling API plus populated compute_thai_tue evidence"
      contains: "pub fn thai_tue_direction"
    - path: "crates/amlich-core/src/almanac/tam_sat.rs"
      provides: "Classical three-direction Tam Sát result and opposite-triad lookup"
      contains: "pub fn tam_sat_direction"
    - path: "crates/amlich-core/tests/almanac_backfill_compat.rs"
      provides: "v1.6 missing-evidence deserialization and v1.7 evidence round-trip gates"
      min_lines: 70
    - path: "crates/amlich-core/data/almanac/tam_sat_provenance.md"
      provides: "Discoverable Tam Sát citation/provenance ledger with explicit PendingExternalReview status"
      contains: "PendingExternalReview"
  key_links:
    - from: "crates/amlich-core/src/almanac/thai_tue.rs::thai_tue_direction"
      to: "crates/amlich-core/src/almanac/tu_menh.rs::Direction"
      via: "year-chi branch-to-eight-direction match; no new directional type"
      pattern: "Direction::"
    - from: "crates/amlich-core/src/almanac/tam_sat.rs::tam_sat_direction"
      to: "crates/amlich-core/src/almanac/types.rs::RuleEvidence"
      via: "SOURCE_KHCBPPT-backed result evidence"
      pattern: "SOURCE_KHCBPPT"
    - from: "crates/amlich-core/src/almanac/sat_phuong.rs::get_sat_phuong"
      to: "crates/amlich-core/tests/almanac_backfill_compat.rs"
      via: "populated evidence remains serde-compatible with evidence omitted"
      pattern: "evidence"
---

<objective>
Deliver the KHCBPPT directional primitives that the Phase 23 reasoning cross-link consumes: a year-chi directional Thái Tuế sibling, a distinct classical three-direction Tam Sát module, and the two locked evidence backfills.

Purpose: Establish the source-traceable almanac facts first, without rewriting personal Thái Tuế semantics or changing the existing day-chi Sát Phương behavior. The Tam Sát page citation remains honestly deferred in a discoverable data artifact.
Output: Public almanac APIs, compatibility tests, Tam Sát integration tests, and the pending-review provenance artifact.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-th-i-tu-tam-s-t-phi-tinh-cross-link/23-CONTEXT.md
@.planning/phases/23-th-i-tu-tam-s-t-phi-tinh-cross-link/23-RESEARCH.md
@.planning/adrs/0007-cross-link-crit3-carve-out.md
@crates/amlich-core/src/almanac/thai_tue.rs
@crates/amlich-core/src/almanac/sat_phuong.rs
@crates/amlich-core/src/almanac/tam_tai.rs
@crates/amlich-core/src/almanac/xung_hop.rs
@crates/amlich-core/src/almanac/tu_menh.rs
@crates/amlich-core/src/almanac/types.rs
@crates/amlich-core/src/types.rs

<interfaces>
From `crates/amlich-core/src/almanac/tu_menh.rs`:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
pub enum Direction {
    North, Northeast, East, Southeast, South, Southwest, West, Northwest,
}
```

From `crates/amlich-core/src/almanac/types.rs`:
```rust
#[derive(Debug, Clone, PartialEq, Eq, Serialize, Deserialize)]
pub struct RuleEvidence {
    pub source_id: String,
    pub method: String,
    pub profile: String,
}
```

From `crates/amlich-core/src/almanac/thai_tue.rs`:
```rust
pub fn compute_thai_tue(birth_chi_index: usize, current_year_chi_index: usize) -> ThaiTueResult;
pub struct ThaiTueResult {
    pub conflicts: Vec<ThaiTueConflict>,
    pub has_conflict: bool,
    pub evidence: Option<RuleEvidence>,
}
pub enum ThaiTueConflictKind { Truc, Xung, Hai, Hinh, Pha }
```

From `crates/amlich-core/src/almanac/sat_phuong.rs`:
```rust
pub fn get_sat_phuong(chi_index: usize) -> SatPhuongResult;
pub struct SatPhuongResult {
    pub direction: String,
    pub evidence: Option<RuleEvidence>,
}
```

From `crates/amlich-core/src/types.rs`:
```rust
pub const CHI: [&str; 12] = [/* Tý through Hợi, indexes 0..=11 */];
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add directional Thái Tuế sibling and lock the two evidence backfills</name>
  <files>crates/amlich-core/src/almanac/thai_tue.rs, crates/amlich-core/src/almanac/sat_phuong.rs, crates/amlich-core/tests/almanac_backfill_compat.rs</files>
  <behavior>
    - `thai_tue_direction(0)` returns `Direction::North`, `thai_tue_direction(3)` returns `Direction::East`, `thai_tue_direction(6)` returns `Direction::South`, and `thai_tue_direction(9)` returns `Direction::West`.
    - Intercardinal branches collapse exactly as locked: Sửu/Dần -> Northeast, Thìn/Tỵ -> Southeast, Mùi/Thân -> Southwest, Tuất/Hợi -> Northwest; all 12 indexes are covered.
    - The new `ThaiTueDirectionResult` carries the year chi index/name, the reused `Direction`, and a non-optional `RuleEvidence` whose source_id is `SOURCE_KHCBPPT`; it is a sibling and does not alter `compute_thai_tue` conflict logic.
    - `compute_thai_tue(0, 0)` and `get_sat_phuong(0)` now return `evidence: Some(...)` with source_id `SOURCE_KHCBPPT`, while their existing conflict/direction values remain unchanged.
    - A legacy JSON object omitting `evidence` deserializes for both existing result types, and each current populated result serializes -> deserializes byte-equivalently.
  </behavior>
  <action>
    Work test-first for the compatibility gate: create `tests/almanac_backfill_compat.rs` with legacy payloads that omit `evidence`, assertions that both parse to `None`, and round-trip assertions for the now-populated current outputs. Run the focused test and observe the RED failure before changing production code.

    Then implement the sibling directional API in `thai_tue.rs`:
    - Add `ThaiTueDirectionResult { year_chi_index: usize, year_chi: String, direction: Direction, evidence: RuleEvidence }` with the existing serde/debug/equality derives.
    - Add `pub fn thai_tue_direction(year_chi_index: usize) -> ThaiTueDirectionResult`, using the existing 0..=11 `CHI` indexing convention and the reused `Direction` enum. Validate the index consistently with existing almanac table APIs (out-of-range is a contract violation; do not introduce a new error type).
    - Keep `compute_thai_tue` personal-conflict-only. Only replace its `evidence: None` at the locked return site with `Some(RuleEvidence { source_id: SOURCE_KHCBPPT.to_string(), method: "thai_tue_year_branch_conflict".to_string(), profile: "baseline".to_string() })`.

    In `sat_phuong.rs`, replace only the locked `evidence: None` with the analogous KHCBPPT `RuleEvidence` (`method: "sat_phuong_day_chi"`, `profile: "baseline"`). Do not change `SAT_PHUONG_BY_CHI`, the function signature, or its day-chi mapping. Update the old inline tests that asserted `None` so they assert the populated source instead. Use the registered `SOURCE_KHCBPPT` constant in production code; do not add a bare source-id literal. Do not create `Direction8`, `Direction4`, 24-sơn types, or a `Direction::as_vn_str()` method.
  </action>
  <verify>
    <automated>cargo test -p amlich-core --test almanac_backfill_compat && cargo test -p amlich-core --lib thai_tue && cargo test -p amlich-core --lib sat_phuong</automated>
  </verify>
  <done>`ThaiTueDirectionResult` and `thai_tue_direction` are public and cover all 12 year branches with the locked eight-direction mapping; both legacy result types remain backward-deserializable; both current outputs carry `SOURCE_KHCBPPT`; the existing personal Thái Tuế and day-chi Sát Phương semantics are unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the classical opposite-triad Tam Sát module and provenance artifact</name>
  <files>crates/amlich-core/src/almanac/tam_sat.rs, crates/amlich-core/src/almanac/mod.rs, crates/amlich-core/tests/tam_sat_integration.rs, crates/amlich-core/data/almanac/tam_sat_provenance.md</files>
  <behavior>
    - `tam_sat_direction(year_chi_index)` returns exactly three directions and three corresponding opposite-triad branch names for every year chi.
    - The four locked year-triad mappings are exact: Thân/Tý/Thìn -> Dần/Ngọ/Tuất -> Đông Bắc/Nam/Tây Bắc; Hợi/Mão/Mùi -> Tỵ/Dậu/Sửu -> Đông Nam/Tây/Đông Bắc; Dần/Ngọ/Tuất -> Thân/Tý/Thìn -> Tây Nam/Bắc/Đông Nam; Tỵ/Dậu/Sửu -> Hợi/Mão/Mùi -> Tây Bắc/Đông/Tây Nam.
    - The result carries `source_id == SOURCE_KHCBPPT`, uses a method that identifies the opposite-triad derivation, and includes the provenance artifact path plus `PendingExternalReview` marker in its auditable evidence method/profile text because the exact page is unverified.
    - `get_sat_phuong` remains a separate one-direction day-chi API; all 12 existing values stay intact.
  </behavior>
  <action>
    Register `pub mod tam_sat;` in `almanac/mod.rs` and create `tam_sat.rs` with:
    - `TamSatDirectionResult { year_chi_index, year_chi, tam_hop_group: [String; 3], tam_sat_branches: [String; 3], tam_sat_directions: [Direction; 3], evidence: RuleEvidence }` plus serde/debug/equality derives.
    - `pub fn tam_sat_direction(year_chi_index: usize) -> TamSatDirectionResult`.
    - An explicit four-row Tam Hợp table ordered as Water (Thân/Tý/Thìn), Wood (Hợi/Mão/Mùi), Fire (Dần/Ngọ/Tuất), Metal (Tỵ/Dậu/Sửu), then the locked opposite branch and direction arrays. Do not use the misleading raw `year_chi_index % 4` ordering by itself: the existing `xung_hop::tam_hop` result is sorted by branch index, while the locked table order is tradition order. Map each input branch to the correct row by explicit membership and preserve the branch/direction order shown in CONTEXT.md.
    - Use the existing `Direction` enum and `CHI` table. Keep the output fixed-size (`[T; 3]`) so callers can prove the classical three-direction contract.
    - Populate evidence with `SOURCE_KHCBPPT.to_string()`, a stable opposite-triad method name, `profile: "baseline"`, and an explicit pending-review marker/reference to `data/almanac/tam_sat_provenance.md` in the method or profile text. Never invent a Quyen/trang/page number.

    Add black-box `tam_sat_integration.rs` tests through the external crate path for all four rows, all 12 input branches, exactly-three output lengths, the source evidence, the pending-review marker, and an assertion that the existing `get_sat_phuong` values remain unchanged. Keep the module independent of `tam_tai.rs`'s three-year status result even though both use opposite-triad concepts.

    Author `crates/amlich-core/data/almanac/tam_sat_provenance.md` as a discoverable, non-runtime artifact: state the locked opposite-triad rule and the four mapping rows, describe the search/review status, cite KHCBPPT by title/tradition only, and explicitly write `PendingExternalReview` for the exact edition/page pin. Do not fabricate a page citation and do not add runtime file loading.
  </action>
  <verify>
    <automated>cargo test -p amlich-core --test tam_sat_integration && cargo test -p amlich-core --lib tam_sat && cargo test -p amlich-core --test source_id_guard</automated>
  </verify>
  <done>The new `almanac::tam_sat` public module returns the exact three opposite-triad directions for all 12 year branches, emits KHCBPPT evidence with honest pending citation status, and leaves `sat_phuong.rs` day-chi behavior intact; the provenance artifact exists at the exact discoverable path.</done>
</task>

</tasks>

<verification>
- `cargo test -p amlich-core --test almanac_backfill_compat` passes the missing-evidence compatibility gate for both existing result types.
- `cargo test -p amlich-core --test tam_sat_integration` passes the four locked Tam Sát mapping rows and all-12-branch coverage.
- `cargo test -p amlich-core --test source_id_guard` passes; production provenance uses registered constants.
- `cargo test -p amlich-core` is green with no regressions.
- `crates/amlich-core/src/almanac/tam_sat.rs` contains no RNG, wall-clock, filesystem, new dependency, or personal Thái Tuế rewrite.
</verification>

<success_criteria>
- XLK-01's directional Thái Tuế sibling and both KHCBPPT evidence backfills are implemented and compatibility-tested.
- XLK-02's distinct classical Tam Sát module returns exactly three locked directions per year and has an honest, discoverable pending-review citation artifact.
- The existing `compute_thai_tue` conflict API and `get_sat_phuong` day-chi API remain behaviorally intact.
- No new crate dependencies are introduced.
</success_criteria>

<output>
After completion, create `.planning/phases/23-th-i-tu-tam-s-t-phi-tinh-cross-link/23-01-SUMMARY.md` documenting the exact mappings, evidence methods, compatibility behavior, provenance deferral, and any auto-fixed deviations.
</output>
