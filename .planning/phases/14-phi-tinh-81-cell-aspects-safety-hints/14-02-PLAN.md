---
phase: 14-phi-tinh-81-cell-aspects-safety-hints
plan: 02
type: execute
wave: 2
depends_on: [14-01]
files_modified:
  - crates/amlich-core/src/almanac/fengshui/safety.rs
  - crates/amlich-core/src/almanac/fengshui/mod.rs
  - crates/amlich-core/data/almanac/flying_stars_safety.json
autonomous: true
requirements: [FS-14, FS-15]
must_haves:
  truths:
    - "is_danger_palace(star) returns true exactly for NguHoang (5) and NhiHac (2), false for all other 7 stars"
    - "element_hint_for_palace(star) returns Some(RemedyHint) for danger/inauspicious stars and None for stars with no classical mitigation"
    - "Every RemedyHint carries element in {kim,mộc,thủy,hỏa,thổ}, source_id == \"huyen-khong\", and a non-empty classical citation"
    - "No RemedyHint.hint_text_vi contains a product/brand name or commercial call-to-action"
  artifacts:
    - path: "crates/amlich-core/src/almanac/fengshui/safety.rs"
      provides: "is_danger_palace predicate; RemedyHint type; element_hint_for_palace loader+lookup"
      min_lines: 70
      contains: "pub fn is_danger_palace"
    - path: "crates/amlich-core/data/almanac/flying_stars_safety.json"
      provides: "Per-star Ngũ-Hành mitigation hints (danger + inauspicious stars)"
      contains: "\"hints\""
    - path: "crates/amlich-core/src/almanac/fengshui/mod.rs"
      provides: "pub mod safety + re-exports"
      contains: "pub mod safety;"
  key_links:
    - from: "crates/amlich-core/src/almanac/fengshui/safety.rs"
      to: "crates/amlich-core/data/almanac/flying_stars_safety.json"
      via: "include_str! + OnceLock::get_or_init + serde_json::from_str"
      pattern: "include_str!.*flying_stars_safety.json"
    - from: "safety.rs RemedyHint reuse"
      to: "aspects::FsCitation"
      via: "use crate::almanac::fengshui::aspects::FsCitation"
      pattern: "aspects::FsCitation"
    - from: "is_danger_palace"
      to: "FlyingStar::NguHoang | FlyingStar::NhiHac"
      via: "matches! pattern"
      pattern: "matches!\\(star"
---

<objective>
Ship the advisory safety surface: the danger-palace predicate and the Ngũ-Hành element-hint lookup.

Purpose: FS-14/FS-15 — `is_danger_palace` flags stars 5 and 2; `element_hint_for_palace` returns a citation-bearing classical mitigation hint with NO product names.
Output: `safety.rs` (predicate + RemedyHint + loader), the safety JSON corpus, and `mod.rs` re-exports.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/14-phi-tinh-81-cell-aspects-safety-hints/14-RESEARCH.md

# 14-01 created FsCitation in aspects.rs — safety.rs reuses it (intra-pillar, allowed).
@.planning/phases/14-phi-tinh-81-cell-aspects-safety-hints/14-01-SUMMARY.md

<interfaces>
From crates/amlich-core/src/almanac/fengshui/types.rs (FROZEN — do NOT edit):
```rust
pub enum FlyingStar { NhatBach=1, NhiHac=2, TamBich=3, TuLuc=4, NguHoang=5, LucBach=6, ThatXich=7, BatBach=8, CuuTu=9 }
```

From crates/amlich-core/src/almanac/fengshui/aspects.rs (created in 14-01):
```rust
pub struct FsCitation { pub title: String, pub publisher: Option<String>, pub edition: Option<String>, pub page: Option<String> } // deny_unknown_fields
```

From crates/amlich-core/src/sources.rs:
```rust
pub const SOURCE_HUYEN_KHONG: &str = "huyen-khong";
```

Star auspice/element (from flying_stars.json): 1=water/auspicious, 2=earth/inauspicious, 3=wood/inauspicious, 5=earth/inauspicious(danger), 7=metal/inauspicious; 4/6/8/9 auspicious; 5 and 2 are the danger stars.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: is_danger_palace + RemedyHint + element_hint_for_palace loader</name>
  <files>crates/amlich-core/src/almanac/fengshui/safety.rs, crates/amlich-core/src/almanac/fengshui/mod.rs</files>
  <behavior>
    - is_danger_palace(NguHoang) == true; is_danger_palace(NhiHac) == true.
    - is_danger_palace returns false for the other 7 stars (NhatBach, TamBich, TuLuc, LucBach, ThatXich, BatBach, CuuTu).
    - element_hint_for_palace(NguHoang) == Some(_); element_hint_for_palace(NhiHac) == Some(_); both with non-empty hint_text_vi.
    - element_hint_for_palace(NhatBach) == None (auspicious star, no mitigation needed).
    - Every RemedyHint loaded carries source_id == "huyen-khong" and non-empty original_citation.title.
  </behavior>
  <action>
    Create `safety.rs`. Declare:
    - `pub fn is_danger_palace(star: FlyingStar) -> bool { matches!(star, FlyingStar::NguHoang | FlyingStar::NhiHac) }`. Implement as a FREE function — do NOT add a method to FlyingStar (types.rs is FROZEN; PITFALLS Pitfall 1). Doc-comment cites FS-14 / classical tradition (stars 5 and 2 are the two principal danger stars).
    - `pub struct RemedyHint { pub element: String, pub hint_text_vi: String, pub source_id: String, pub original_citation: FsCitation }` — reuse `use crate::almanac::fengshui::aspects::FsCitation;` (intra-pillar reuse; this is NOT the rituals coupling Pitfall 4 forbids). Derive Debug/Clone/PartialEq/Eq/Serialize/Deserialize; `#[serde(deny_unknown_fields)]`.
    - Internal `struct SafetyCorpus { schema_version: String, source: String, hints: Vec<RemedyHintRow> }` where `RemedyHintRow { star: u8, #[serde(flatten)] hint: RemedyHint }` OR add a `pub star: u8` field — simplest is an internal row struct `{ star: u8, element, hint_text_vi, source_id, original_citation }` that the loader maps into `(u8, RemedyHint)`. Choose whichever keeps RemedyHint free of a `star` field (the public type should not expose star since the lookup is keyed by the function argument). Recommended: internal `struct SafetyHintRow { star: u8, element: String, hint_text_vi: String, source_id: String, original_citation: FsCitation }` (Deserialize), and build `RemedyHint` from it in the lookup.
    - `const SAFETY_JSON: &str = include_str!("../../../data/almanac/flying_stars_safety.json");`
    - `static SAFETY_CORPUS: OnceLock<SafetyCorpus> = OnceLock::new();`
    - `fn load_safety_inner() -> SafetyCorpus` — parse JSON, then validate: every row's `star` in 1..=9 and unique; every `source_id == crate::sources::SOURCE_HUYEN_KHONG`; every `original_citation.title` non-empty; every `element` in the allowed set {"kim","mộc","thủy","hỏa","thổ"}. Panic on violation (mirrors stars.rs validation discipline). NEVER write the bare `"huyen-khong"` literal in .rs — use SOURCE_HUYEN_KHONG.
    - `fn safety_corpus() -> &'static SafetyCorpus { SAFETY_CORPUS.get_or_init(load_safety_inner) }`
    - `pub fn element_hint_for_palace(star: FlyingStar) -> Option<RemedyHint>` — scan corpus rows for `row.star == star as u8`, map to owned `RemedyHint`, return `Some`/`None`. Returns None when no row exists for that star.
    Add `#[cfg(test)] mod tests` encoding the behaviors: is_danger_palace truth table for all 9 stars; element_hint_for_palace Some for 2 and 5, None for 1; loaded hints carry SOURCE_HUYEN_KHONG and non-empty title. JSON does not exist until Task 2 — load-dependent tests RED until then (expected).
    Register in mod.rs: add `pub mod safety;` and `pub use safety::{element_hint_for_palace, is_danger_palace, RemedyHint};` to the re-export block.
  </action>
  <verify>
    <automated>cargo build -p amlich-core 2>&1 | grep -qE "error\[|error:" && echo FAIL || echo OK</automated>
  </verify>
  <done>safety.rs compiles, registered in mod.rs; is_danger_palace + RemedyHint + element_hint_for_palace present; reuses aspects::FsCitation; no bare "huyen-khong" literal (grep clean). Load-dependent tests may RED until Task 2 lands the JSON.</done>
</task>

<task type="auto">
  <name>Task 2: Author flying_stars_safety.json corpus</name>
  <files>crates/amlich-core/data/almanac/flying_stars_safety.json, crates/amlich-core/src/almanac/fengshui/safety.rs</files>
  <action>
    Author `flying_stars_safety.json` with shape `{ "schema_version": "safety-v1", "source": "Thẩm Thị Huyền Không Học", "hints": [ ...rows... ] }`. Provide one row per inauspicious/danger star — at minimum stars 2, 3, 5, 7 (per Research Q3; auspicious stars 1,4,6,8,9 get NO row so element_hint_for_palace returns None for them). Each row:
    - `star`: the star number (2, 3, 5, or 7).
    - `element`: the mitigating Ngũ-Hành element, one of "kim"|"mộc"|"thủy"|"hỏa"|"thổ". Classical logic: to drain/weaken an earth danger star (2,5) use metal ("kim") which earth generates; for wood disturbance (3) use fire/metal per text. Pick the classically standard mitigating element per star.
    - `hint_text_vi`: a classical Ngũ-Hành advisory in Vietnamese referencing element categories and object CLASSES ONLY (e.g. "Dùng vật phẩm thuộc hành Kim để hóa giải sát khí của sao", "Đặt vật bằng kim loại để tiết khí Thổ"). MUST NOT contain product names, brand names, URLs, prices, or commercial calls-to-action (no "mua", "đặt mua", "click", "sản phẩm", brand strings). This is enforced by the no-product-names test in plan 14-03.
    - `source_id`: "huyen-khong" (JSON data value — allowed).
    - `original_citation`: `{ "title": "Thẩm Thị Huyền Không Học", "page": "Chương hóa giải Ngũ Hành" }` (title non-empty required).
    Confirm the safety tests in safety.rs now pass (Some for 2 and 5, None for 1).
  </action>
  <verify>
    <automated>cargo test -p amlich-core --lib fengshui::safety 2>&1 | tail -5</automated>
  </verify>
  <done>flying_stars_safety.json has rows for stars 2,3,5,7; all fengshui::safety lib tests pass; element_hint_for_palace returns Some for 2/5 and None for 1; every hint element is in the allowed Ngũ-Hành set with non-empty citation.</done>
</task>

</tasks>

<verification>
- `cargo test -p amlich-core --lib fengshui::safety` — all safety unit tests pass.
- `cargo test -p amlich-core --test source_id_guard` — no bare "huyen-khong" literal in safety.rs.
- `cargo build -p amlich-core` — clean (mod.rs re-exports resolve; FsCitation import resolves).
</verification>

<success_criteria>
- FS-14: is_danger_palace true exactly for NguHoang(5) and NhiHac(2).
- FS-15: element_hint_for_palace returns Option<RemedyHint> with Ngũ-Hành element + classical citation; None for auspicious stars; no product names in corpus.
- types.rs untouched; no import from crate::rituals or crate::interaction.
</success_criteria>

<output>
After completion, create `.planning/phases/14-phi-tinh-81-cell-aspects-safety-hints/14-02-SUMMARY.md`
</output>
