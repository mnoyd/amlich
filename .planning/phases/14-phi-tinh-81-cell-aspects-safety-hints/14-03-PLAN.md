---
phase: 14-phi-tinh-81-cell-aspects-safety-hints
plan: 03
type: execute
wave: 3
depends_on: [14-01, 14-02]
files_modified:
  - crates/amlich-core/tests/fengshui_aspects.rs
autonomous: true
requirements: [FS-11, FS-12, FS-13, FS-14, FS-15]
must_haves:
  truths:
    - "An external consumer can call lookup_star_pair_aspect, compute_palace_aspects, is_danger_palace, and element_hint_for_palace through the public crate API"
    - "compute_palace_aspects(2024, 1, &scanner) returns 9 aspects whose (star_a,star_b) match the combined overlay's palace_overlays"
    - "The test suite fails RED if any RemedyHint.hint_text_vi or StarPairAspect.name contains a forbidden product/commercial term"
  artifacts:
    - path: "crates/amlich-core/tests/fengshui_aspects.rs"
      provides: "Black-box integration tests for FS-11..FS-15 + no-product-names guard"
      min_lines: 70
      contains: "no_product_names"
  key_links:
    - from: "crates/amlich-core/tests/fengshui_aspects.rs"
      to: "amlich_core::almanac::fengshui public API"
      via: "use amlich_core::... (external-consumer import surface)"
      pattern: "use amlich_core::"
    - from: "no_product_names test"
      to: "element_hint_for_palace + lookup_star_pair_aspect corpora"
      via: "runtime corpus scan against FORBIDDEN_PRODUCT_TERMS"
      pattern: "FORBIDDEN_PRODUCT_TERMS"
---

<objective>
Black-box integration tests proving the Phase 14 public API surface works for an external crate consumer, plus the no-product-names regression guard.

Purpose: Close FS-13 end-to-end (combined overlay → per-palace aspects) and FS-15's "never product names" guarantee with a CI-enforced corpus scan.
Output: `crates/amlich-core/tests/fengshui_aspects.rs`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/14-phi-tinh-81-cell-aspects-safety-hints/14-RESEARCH.md
@.planning/phases/14-phi-tinh-81-cell-aspects-safety-hints/14-01-SUMMARY.md
@.planning/phases/14-phi-tinh-81-cell-aspects-safety-hints/14-02-SUMMARY.md

<interfaces>
Public API to exercise as an EXTERNAL consumer (`use amlich_core::...`). Verify exact re-export paths from fengshui/mod.rs created in 14-01/14-02; the canonical surface is:
```rust
use amlich_core::almanac::fengshui::{
    lookup_star_pair_aspect, compute_palace_aspects, StarPairAspect, FsConfidenceTier,
    is_danger_palace, element_hint_for_palace, RemedyHint,
    compute_combined_overlay, TietKhiScanner,
};
use amlich_core::almanac::fengshui::types::FlyingStar;
```
(If a name is not re-exported at `almanac::fengshui`, import from its module path e.g. `almanac::fengshui::aspects::...` — match what 14-01/14-02 actually exported.)

Existing black-box test conventions: crates/amlich-core/tests/fengshui_invariants.rs and tests/rituals_integration.rs (external `use amlich_core::...` import style).
TietKhiScanner constructor: `TietKhiScanner::new()`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Black-box FS-11..FS-15 integration tests</name>
  <files>crates/amlich-core/tests/fengshui_aspects.rs</files>
  <action>
    Create `tests/fengshui_aspects.rs` importing the Phase 14 API via `use amlich_core::...` exactly as an external consumer would (match the re-export paths produced by 14-01/14-02; consult the SUMMARYs). Add these `#[test]` functions:
    - `all_81_pairs_lookup_ordered` (FS-11): loop star_a, star_b across all 9 FlyingStar variants, call `lookup_star_pair_aspect(a, b)`, assert returned `star_a == a as u8 && star_b == b as u8`. Assert that lookup(NhatBach, CuuTu) and lookup(CuuTu, NhatBach) differ in their (star_a, star_b) ordering (ordered, not symmetric).
    - `aspect_provenance_discipline` (FS-12): for a sample of pairs, assert `aspect.source_id == "huyen-khong"` (compare against the literal here — this is a TEST file, allowed) and `!aspect.original_citation.title.is_empty()`.
    - `compute_palace_aspects_matches_overlay` (FS-13): let scanner = TietKhiScanner::new(); let overlay = compute_combined_overlay(2024, 1, &scanner); let aspects = compute_palace_aspects(2024, 1, &scanner); assert aspects.len() == 9; for i in 0..9 assert aspects[i].star_a == overlay.palace_overlays[i].0 as u8 && aspects[i].star_b == overlay.palace_overlays[i].1 as u8.
    - `danger_palace_predicate` (FS-14): assert is_danger_palace(NguHoang) && is_danger_palace(NhiHac); assert the other 7 stars are all false.
    - `element_hint_present_for_danger_stars` (FS-15): assert element_hint_for_palace(NguHoang).is_some() and element_hint_for_palace(NhiHac).is_some(); assert each returned RemedyHint has non-empty hint_text_vi, source_id == "huyen-khong", non-empty citation title, and element in {"kim","mộc","thủy","hỏa","thổ"}; assert element_hint_for_palace(NhatBach).is_none().
    Use `FlyingStar` variants directly; build the 9-variant array once and reuse.
  </action>
  <verify>
    <automated>cargo test -p amlich-core --test fengshui_aspects 2>&1 | tail -8</automated>
  </verify>
  <done>fengshui_aspects.rs compiles against the public crate API and all FS-11..FS-15 black-box tests pass; compute_palace_aspects output is verified equal to the combined overlay pairs for 2024-01.</done>
</task>

<task type="auto">
  <name>Task 2: no-product-names corpus guard</name>
  <files>crates/amlich-core/tests/fengshui_aspects.rs</files>
  <action>
    Add a `no_product_names_in_corpora` `#[test]` to `fengshui_aspects.rs` mirroring the source_id_guard.rs regression-guard philosophy but as a RUNTIME corpus scan (per Research Q5 / Pattern 9):
    - Define `const FORBIDDEN_PRODUCT_TERMS: &[&str] = &["đặt mua", "mua ngay", "click", "sản phẩm", "giá", "khuyến mãi", "http://", "https://", "www."];` (curated commercial/product patterns; classical text never contains these).
    - Walk every RemedyHint: for each of the 9 FlyingStar variants call `element_hint_for_palace(star)`; for each Some(hint), lowercase `hint.hint_text_vi` and assert it contains none of FORBIDDEN_PRODUCT_TERMS (case-insensitive). Collect violations into a Vec and assert empty with a descriptive message listing offenders.
    - Walk every StarPairAspect name: loop all 81 ordered pairs via lookup_star_pair_aspect, lowercase `aspect.name`, assert it contains none of FORBIDDEN_PRODUCT_TERMS. Collect + assert empty.
    The test must be future-proof: if a later corpus edit introduces a product reference into hint_text_vi or aspect name, this turns RED.
  </action>
  <verify>
    <automated>cargo test -p amlich-core --test fengshui_aspects no_product_names_in_corpora 2>&1 | tail -5</automated>
  </verify>
  <done>no_product_names_in_corpora passes against the authored corpora; scans all 9 hints + all 81 aspect names against FORBIDDEN_PRODUCT_TERMS case-insensitively; turns RED on any future product-term insertion.</done>
</task>

</tasks>

<verification>
- `cargo test -p amlich-core --test fengshui_aspects` — all integration tests pass (FS-11..FS-15 + no-product-names).
- `cargo test -p amlich-core` — full crate suite green (no regressions in fengshui::* or source_id_guard).
</verification>

<success_criteria>
- FS-11..FS-15 all verified through the external public API surface (black-box).
- compute_palace_aspects output proven consistent with compute_combined_overlay for a real date.
- no-product-names guard scans the full hint + aspect-name corpora and is a standing CI regression gate.
</success_criteria>

<output>
After completion, create `.planning/phases/14-phi-tinh-81-cell-aspects-safety-hints/14-03-SUMMARY.md`
</output>
