---
phase: 16-foundation-adr-0003-confidence-closure
verified: 2026-07-15T09:00:00Z
status: passed
score: 8/8 must-haves verified
---

# Phase 16: Foundation — ADR-0003 Confidence Closure Verification Report

**Phase Goal:** Close FND-07 (ADR-0003 confidence caveat) and FND-08 (1960 Trung Nguyên divergence) — promote pre-1984 Thượng/Trung Nguyên rows to HIGH confidence AND make the 1960 split disposition machine-readable as PendingExternalReview without changing the provisional tiebreaker value.
**Verified:** 2026-07-15T09:00:00Z
**Status:** passed
**Re-verification:** No — initial verification (no prior VERIFICATION.md existed)

## Goal Achievement

### Observable Truths (8 must-haves from prompt)

| #   | Truth                                                                              | Status     | Evidence                                                                                                                                                                                                       |
| --- | ---------------------------------------------------------------------------------- | ---------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| 1   | ADR-0003a exists at `.planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md` and supersedes ADR-0003 §6 | ✓ VERIFIED | File exists; line 3 `Status: Accepted`; line 6 `Supersedes: ADR-0003 §6 (Confidence Acknowledgment)`; line 88 `Supersedes ADR-0003 §6 only. ADR-0003 §§1–5 remain in force.`                                       |
| 2   | `crates/amlich-core/src/almanac/fengshui/golden.rs` has `GoldenConfidence` enum (FND-07) AND `DeferralMarker` struct (FND-08) | ✓ VERIFIED | `pub enum GoldenConfidence { High, Medium, Low }` at line 57 with `#[serde(default)] Medium` shim; `pub struct DeferralMarker { reason, expected_review_date, assigned_to: Option<String> }` at line 80. |
| 3   | All 37 cases in `flying_stars_golden.json` have `confidence=high`; 1960 `known_divergences` row has `deferral` populated | ✓ VERIFIED | `python3` check: `case_count=37`, all 37 carry `confidence: "high"` (0 non-high); 1960 row carries `deferral: { reason, expected_review_date: "2026-12-31", assigned_to: "external-huyen-khong-reviewer" }`; `our_value=5` preserved (NOT silently corrected to 6). |
| 4   | `tests/fengshui_invariants.rs` has Test F (`test_f_golden_pre_1984_confidence_is_high`) AND Test G (`test_g_1960_divergence_deferred`) | ✓ VERIFIED | Test F at line 369 imports public `GoldenConfidence`, asserts all `kind="annual" && year<1984` cases equal `GoldenConfidence::High`; Test G at line 429 locates `case == "annual 1960"`, asserts `deferral.is_some()`, populated `reason`, `expected_review_date == "2026-12-31"`, `assigned_to` is `Some` with non-empty content, `note` contains literal `PendingExternalReview`, and `our_value == 5`. |
| 5   | `crates/amlich-core/src/almanac/fengshui/annual.rs` evidence note reports `confidence=high` for pre-1984 years | ✓ VERIFIED | Line 171: `let note = format!("year={year};center={center};polarity={polarity:?};confidence=high");` — unconditional for all annual years. `grep "confidence=medium"` returns 0 hits. The obsolete pre-1984 MEDIUM branch was removed (per Plan 16-01 deviation #1 fix in SUMMARY). |
| 6   | `cargo test -p amlich-core --test fengshui_invariants` (must be 11/11 pass) | ✓ VERIFIED | All 11 tests pass: `test_a_lo_shu_invariants_all_vans`, `test_b_van_boundary_lap_xuan_2024`, `test_b_van_boundary_mid_van8_and_van7`, `test_c_golden_annual_coverage`, `test_d_golden_monthly_cases`, `test_d_golden_period_cases`, `test_e_combined_overlay_smoke_2024_m1`, `test_e_combined_overlay_annual_center_2024`, `test_e_combined_overlay_mirrors_components`, **`test_f_golden_pre_1984_confidence_is_high`**, **`test_g_1960_divergence_deferred`**. `test result: ok. 11 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out`. |
| 7   | `cargo test -p amlich-core` (must be 888/888 pass) | ✓ VERIFIED | Aggregate across all test targets: `Total passed: 888; Total failed: 0; Total ignored: 7` (the 7 ignored are pre-existing `#[ignore]` doc tests in `na_am.rs` and `sexagenary_cycle.rs` that were ignored on master and remain so — not new from Phase 16). |
| 8   | FND-07 and FND-08 in REQUIREMENTS.md both show Complete status                | ✓ VERIFIED | REQUIREMENTS.md line 13-14: both FND-07 and FND-08 marked `[x]`. Traceability table lines 51-52: FND-07 = `**Complete** (2026-07-15, 16-01)`; FND-08 = `**Complete** (2026-07-15, 16-02)`. ROADMAP.md line 43 / 52-53 / 118 confirms Phase 16 marked complete. |

**Score:** 8/8 must-haves verified

### Required Artifacts

| Artifact                                                                                          | Expected                                                                                                                | Status     | Details                                                                                                                                                                                                                                                                                                                                                            |
| ------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------- | ---------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `.planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md`                                | Accepted superseding decision; supersedes ADR-0003 §6 only; HIGH polarity-row confidence; 1960 PendingExternalReview narrative                                | ✓ VERIFIED | 90 lines. Sections: Status/Date/Deciders/Supersedes, Context, Decision (5 subsections), Consequences (FND-07 / FND-08 / Backward Compatibility). All required strings present: `Status: Accepted`, `Supersedes: ADR-0003 §6`, `independent secondary`, `phongthuycaivan.org`, `lasotuvi.com`, `phongthuyso.vn`, `HIGH`, `PendingExternalReview`, `ADR-0003 §§1–5`, `2026-12-31`, `external-huyen-khong-reviewer`, `our_value=5`, `expected_center=5`. `'second classical' not in adr.lower()` = True. |
| `crates/amlich-core/src/almanac/fengshui/golden.rs`                                               | Typed `GoldenConfidence` enum (FND-07) + typed `DeferralMarker` struct (FND-08) + additive fields with serde defaults | ✓ VERIFIED | 353 lines. `pub enum GoldenConfidence { High, Medium(#[default]), Low }` at L57 with `#[serde(rename_all = "lowercase")]` and `#[derive(Default)]`. `pub struct DeferralMarker { reason: String, expected_review_date: String, assigned_to: Option<String> }` at L80 with `#[derive(Default)]`. `pub confidence: GoldenConfidence` on `PhiTinhGoldenCase` at L156 with `#[serde(default)]`. `pub deferral: Option<DeferralMarker>` on `KnownDivergence` at L118 with `#[serde(default, skip_serializing_if = "Option::is_none")]`. Module docstring L17-20 references ADR-0003a. |
| `crates/amlich-core/data/almanac/flying_stars_golden.json`                                       | All 37 cases `confidence=high`; 1960 `known_divergences` row carries populated `deferral`; `our_value=5` / `expected_center=5` preserved       | ✓ VERIFIED | 548 lines. `case_count=37` matches `len(cases)`. All 37 cases carry `"confidence": "high"`. `metadata.description` references ADR-0003a. 1920 and 1960 case `tiebreaker`/`note` reference ADR-0003a and dual-source independent secondary modern verification. 1960 `known_divergences` row carries `deferral: { reason, expected_review_date: "2026-12-31", assigned_to: "external-huyen-khong-reviewer" }`; `our_value: 5`, both source values (5 and 6) preserved. |
| `crates/amlich-core/src/almanac/fengshui/annual.rs`                                               | `compute_yearly_flying_stars` evidence note emits `confidence=high` for pre-1984 AND post-1984 years (FND-07 runtime parity) | ✓ VERIFIED | L171: `format!("year={year};center={center};polarity={polarity:?};confidence=high")` — unconditional. Unit test `test_compute_yearly_pre_1984_high_confidence` at L342 asserts `note.contains("confidence=high")` for 1960. Companion test `test_compute_yearly_post_1984_high_confidence` at L351 asserts the same for 2024. `confidence=medium` no longer present. |
| `crates/amlich-core/src/almanac/fengshui/mod.rs`                                                  | Re-export `GoldenConfidence` and `DeferralMarker` from `almanac::fengshui` for external-crate test access                | ✓ VERIFIED | L29: `pub use golden::{load_flying_stars_golden, DeferralMarker, GoldenConfidence, KnownDivergence, PhiTinhGoldenCase, PhiTinhGoldenDataset};` — both new types re-exported.                                                                                                                                                                                            |
| `crates/amlich-core/tests/fengshui_invariants.rs`                                                 | Test F (FND-07 gate) + Test G (FND-08 gate) external-crate black-box regression gates                                    | ✓ VERIFIED | 492 lines. Test F at L369 imports public `GoldenConfidence`, asserts all pre-1984 annual cases are `GoldenConfidence::High`, requires both canonical IDs (`annual-thuong-nguyen-1920`, `annual-trung-nguyen-1960`) to be present. Test G at L429 locates `case == "annual 1960"`, asserts `our_value == 5`, asserts `deferral.is_some()` with `expected_review_date == "2026-12-31"`, asserts `assigned_to` is `Some` with non-empty content, asserts `note` contains literal `PendingExternalReview`. Test C now relies on `KnownDivergence` import (L142) — wired. |

### Key Link Verification

| From                                                                                | To                                                                                | Via                                                                                                | Status   | Details                                                                                                                            |
| ----------------------------------------------------------------------------------- | --------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------- | -------- | ---------------------------------------------------------------------------------------------------------------------------------- |
| `tests/fengshui_invariants.rs` (Test F)                                             | `crates/amlich-core/src/almanac/fengshui/golden.rs`                               | Public `GoldenConfidence` re-export + `load_flying_stars_golden`                                    | ✓ WIRED  | L15: `use amlich_core::almanac::fengshui::{... GoldenConfidence, ...}`; L370: `load_flying_stars_golden()`; L397: `case.confidence == GoldenConfidence::High`. |
| `tests/fengshui_invariants.rs` (Test G)                                             | `crates/amlich-core/data/almanac/flying_stars_golden.json`                        | `load_flying_stars_golden` finds `case == "annual 1960"` and unwraps its typed `deferral`            | ✓ WIRED  | L436: `ds.known_divergences.iter().find(|d| d.case == "annual 1960")`; L451: `div.deferral.as_ref().expect(...)`; L459: `deferral.expected_review_date == "2026-12-31"`. |
| `tests/fengshui_invariants.rs` (Test C)                                             | `crates/amlich-core/src/almanac/fengshui/golden.rs` (KnownDivergence)             | Public `KnownDivergence` re-export for divergence-year lookup                                       | ✓ WIRED  | L19: `use amlich_core::almanac::fengshui::KnownDivergence;`; L142: iterates `ds.known_divergences` and parses "annual YYYY" case names. |
| `golden.rs`                                                                         | `flying_stars_golden.json`                                                        | `include_str!` loader + serde `Option<DeferralMarker>` field with `#[serde(default)]`                | ✓ WIRED  | L31: `const FLYING_STARS_GOLDEN_JSON: &str = include_str!("../../../data/almanac/flying_stars_golden.json");` L189: `serde_json::from_str(...)`; L118: `deferral: Option<DeferralMarker>` with backward-compatible serde. |
| `annual.rs` (runtime evidence)                                                     | ADR-0003a (decision document)                                                     | Evidence note `confidence=high` mirrors the accepted confidence upgrade                              | ✓ WIRED  | L171: `confidence=high` in note format string. L93-98: `yuan_of_year` docstring references ADR-0003a. The runtime parity eliminates the prior `confidence=medium` divergence between dataset and evidence. |
| ADR-0003a                                                                           | `flying_stars_golden.json` (1960 row)                                             | `## Consequences > FND-08` names the same marker, date, assignee, and provisional tiebreaker value    | ✓ WIRED  | ADR-0003a L74-78 records `expected_review_date = "2026-12-31"`, `assigned_to = "external-huyen-khong-reviewer"`, `our_value=5`, `expected_center=5`. JSON L541-545 carries the matching `deferral` object. Both reference the literal `PendingExternalReview`. |
| ADR-0003a                                                                           | ADR-0003 (original)                                                               | `Supersedes: ADR-0003 §6 (Confidence Acknowledgment)` — §§1–5 remain authoritative                  | ✓ WIRED  | ADR-0003a L6 declares supersession scope; L55-58 (`### 5. Backward Compatibility`) and L82 reaffirm `ADR-0003 §§1–5 remain authoritative`. |

### Requirements Coverage

| Requirement | Source Plan | Description (abbreviated)                                                                                                            | Status      | Evidence                                                                                                                                                                                                                                                              |
| ----------- | ----------- | ------------------------------------------------------------------------------------------------------------------------------------- | ----------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| FND-07      | 16-01       | Pre-1984 Thượng/Trung Nguyên polarity rows promoted MEDIUM → HIGH after external cross-check                                          | ✓ SATISFIED | ADR-0003a accepted with `Supersedes: ADR-0003 §6`; cross-check trail cites `phongthuycaivan.org` + `lasotuvi.com` / `phongthuyso.vn` (independent secondary modern verification, not additional classical authority). Typed `GoldenConfidence` enum on `PhiTinhGoldenCase`; all 37 cases explicitly `high`. Test F gates pre-1984 cases via public enum. Runtime parity: `annual.rs` emits `confidence=high` for all annual years. |
| FND-08      | 16-02       | 1960 Trung Nguyên `KnownDivergence` explicitly logged as `PendingExternalReview` with reason + tiebreaker decision; ADR-0003 narrative updated | ✓ SATISFIED | Typed `DeferralMarker { reason, expected_review_date, assigned_to }` re-exported; additive `Option<DeferralMarker>` on `KnownDivergence` with `#[serde(default, skip_serializing_if = "Option::is_none")]` (backward compatible). 1960 row's `deferral` populated with `expected_review_date: "2026-12-31"`, `assigned_to: "external-huyen-khong-reviewer"`, and a non-empty reason. `our_value: 5` and `expected_center: 5` preserved verbatim — NOT silently corrected. Note carries literal `PendingExternalReview`. Test G gates all of the above. ADR-0003a Consequences has dedicated `### FND-08 (closed in Plan 16-02)` subsection matching the JSON payload. REQUIREMENTS.md traceability row marked **Complete**. |

### Anti-Patterns Found

| File                                                              | Line | Pattern | Severity | Impact |
| ----------------------------------------------------------------- | ---- | ------- | -------- | ------ |
| (none)                                                            | —    | —       | —        | —      |

**Notes on anti-pattern scan:**
- `crates/amlich-core/src/almanac/fengshui/golden.rs` — no `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER`/`second classical` matches.
- `crates/amlich-core/src/almanac/fengshui/annual.rs` — no `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER` matches.
- `.planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md` — no `TODO`/`FIXME`/`XXX`/`HACK`/`PLACEHOLDER`/`second classical` matches (the latter is the documented 16-RESEARCH.md pitfall that the SUMMARY explicitly guarded against via rephrasing).
- `tests/fengshui_invariants.rs` — no anti-patterns; Tests F and G are real black-box assertions, not placeholders.
- No `return null` / `return {}` / `return []` / `=> {}` stubs detected in any modified file.
- No console.log-only implementations detected (production code is Rust; no console.log idiom).

### Human Verification Required

None — all automated checks pass. The phase is editorial/schema work: the runtime evidence-note parity, the typed enum, the typed deferral, the JSON annotations, and the regression gates are all programmatically verifiable and have been verified. The remaining unverifiable qualities (cross-source web reference authenticity, reviewer identity assignment, future review-execution outcome) are explicitly OUT OF SCOPE for this verification — they are domain-expert activities, not code-level deliverables. The disposition language (independent secondary modern sources rather than additional classical authority; HIGH polarity-row confidence distinct from unresolved 1960 center value) is the auditable, code-level record of that boundary.

### Gaps Summary

No gaps. All 8 must-haves from the prompt verified; all artifacts exist + substantive + wired; all key links confirmed; both requirement IDs (FND-07 and FND-08) marked Complete in REQUIREMENTS.md; ADR-0003a accepted with the locked cross-check trail; the 1960 row carries the typed PendingExternalReview deferral with our_value=5 preserved verbatim.

### Pre-existing Tech Debt (Out of Scope)

The Phase 16-01 SUMMARY (`## Deviations from Plan > Auto-fixed Issues > 2. [Rule 3 - Blocking]`) documents that the codebase has ~96 pre-existing `cargo clippy` warnings and `cargo fmt` issues on master that predate Phase 16. Phase 16 introduces zero new clippy/fmt issues (96 before → 96 after). This pre-existing debt is logged in `.planning/phases/16-foundation-adr-0003-confidence-closure/deferred-items.md` and recommended for a dedicated cleanup phase. It does NOT block Phase 16 goal achievement and was explicitly out of scope per the deviation rule SCOPE BOUNDARY.

---

_Verified: 2026-07-15T09:00:00Z_
_Verifier: Claude (gsd-verifier)_
