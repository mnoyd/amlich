---
phase: 16-foundation-adr-0003-confidence-closure
plan: 02
type: execute
wave: 2
depends_on: ["16-01"]
files_modified:
  - .planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md
  - crates/amlich-core/src/almanac/fengshui/golden.rs
  - crates/amlich-core/src/almanac/fengshui/mod.rs
  - crates/amlich-core/data/almanac/flying_stars_golden.json
  - crates/amlich-core/tests/fengshui_invariants.rs
autonomous: true
requirements: [FND-08]
must_haves:
  truths:
    - "The annual 1960 KnownDivergence remains unresolved and carries an explicit typed PendingExternalReview deferral with populated reason, expected_review_date, and assigned_to fields."
    - "The deferred row preserves our_value=5 and expected_center=5 as the provisional Thẩm Thị tiebreaker; no source disagreement is silently corrected or presented as resolved."
    - "KnownDivergence remains backward compatible because the new Option<DeferralMarker> field uses #[serde(default)] and is omitted when absent."
    - "ADR-0003a Consequences records the exact deferred disposition, review timing/assignment, and the distinction between HIGH polarity confidence and unresolved 1960 center value."
    - "The 9 pre-existing fengshui_invariants.rs tests, Plan 16-01 Test F, and the 886-test v1.5 baseline remain green."
  artifacts:
    - path: "crates/amlich-core/src/almanac/fengshui/golden.rs"
      provides: "DeferralMarker and additive KnownDivergence.deferral contract"
      contains: "pub struct DeferralMarker"
    - path: "crates/amlich-core/data/almanac/flying_stars_golden.json"
      provides: "1960 PendingExternalReview audit payload"
      contains: "PendingExternalReview"
    - path: "crates/amlich-core/tests/fengshui_invariants.rs"
      provides: "FND-08 external-consumer regression gate"
      contains: "test_g_1960_divergence_deferred"
    - path: ".planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md"
      provides: "Final narrative consequences for the deferred divergence"
      contains: "PendingExternalReview"
  key_links:
    - from: "crates/amlich-core/tests/fengshui_invariants.rs"
      to: "crates/amlich-core/data/almanac/flying_stars_golden.json"
      via: "load_flying_stars_golden finds annual 1960 and unwraps its typed deferral"
      pattern: "annual 1960|deferral"
    - from: "crates/amlich-core/src/almanac/fengshui/golden.rs"
      to: "crates/amlich-core/data/almanac/flying_stars_golden.json"
      via: "serde maps the optional deferral object to DeferralMarker"
      pattern: "Option<DeferralMarker>"
    - from: ".planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md"
      to: "crates/amlich-core/data/almanac/flying_stars_golden.json"
      via: "Consequences names the same marker, provisional value, review date, and assignee as the ledger row"
      pattern: "PendingExternalReview"
---

<objective>
Close FND-08 by giving the unresolved 1960 Trung Nguyên source split a typed, populated `PendingExternalReview` deferral and finalizing ADR-0003a's consequences without changing the provisional tiebreaker value.

Purpose: Make deferral explicit and queryable while preserving the v1.5 `KnownDivergence` evidence trail and the locked decision not to resolve the disagreement in this phase.
Output: Additive deferral schema, populated 1960 ledger marker, Test G, and synchronized ADR consequences.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/16-foundation-adr-0003-confidence-closure/16-RESEARCH.md
@.planning/phases/16-foundation-adr-0003-confidence-closure/16-01-PLAN.md
@.planning/phases/16-foundation-adr-0003-confidence-closure/16-01-SUMMARY.md
@.planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md
@.planning/milestones/v1.5-MILESTONE-AUDIT.md
@crates/amlich-core/src/almanac/fengshui/golden.rs
@crates/amlich-core/src/almanac/fengshui/mod.rs
@crates/amlich-core/data/almanac/flying_stars_golden.json
@crates/amlich-core/tests/fengshui_invariants.rs

<interfaces>
Plan 16-01 provides this additive case contract and public enum:
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize, Default)]
#[serde(rename_all = "lowercase")]
pub enum GoldenConfidence {
    High,
    #[default]
    Medium,
    Low,
}

pub struct PhiTinhGoldenCase {
    // existing fields unchanged
    #[serde(default)]
    pub confidence: GoldenConfidence,
}
```

Current divergence contract before this plan:
```rust
pub struct KnownDivergence {
    pub case: String,
    pub our_value: u8,
    pub source_values: Vec<SourceValue>,
    pub tiebreaker: String,
    pub note: String,
}
```

Target additive deferral contract:
```rust
#[derive(Debug, Clone, Serialize, Deserialize, Default)]
pub struct DeferralMarker {
    pub reason: String,
    pub expected_review_date: String,
    pub assigned_to: Option<String>,
}

pub struct KnownDivergence {
    // existing fields unchanged
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deferral: Option<DeferralMarker>,
}
```

The presence of `deferral` is the typed `PendingExternalReview` marker. Keep the literal disposition name in the 1960 JSON note and ADR so human readers can find it; do not add a parallel status enum or boolean. Re-export `DeferralMarker` from `almanac::fengshui` for public schema completeness.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Gate the 1960 PendingExternalReview marker RED→GREEN</name>
  <files>crates/amlich-core/src/almanac/fengshui/golden.rs, crates/amlich-core/src/almanac/fengshui/mod.rs, crates/amlich-core/data/almanac/flying_stars_golden.json, crates/amlich-core/tests/fengshui_invariants.rs</files>
  <action>
Use one production-test RED→GREEN commit. Add `DeferralMarker` and `#[serde(default, skip_serializing_if = "Option::is_none")] pub deferral: Option<DeferralMarker>` exactly as the target interface specifies, without changing existing fields. Re-export `DeferralMarker` from `fengshui/mod.rs`.

Add the external-consumer test `test_g_1960_divergence_deferred`. It must load the canonical dataset, locate `KnownDivergence.case == "annual 1960"`, require `deferral.as_ref()` to be present, and assert: `reason.trim()` is non-empty; `expected_review_date == "2026-12-31"`; `assigned_to` is `Some` with non-empty content; the note contains literal `PendingExternalReview`; and `our_value == 5`. Run this test before changing the JSON and confirm RED because the backward-compatible missing field deserializes as `None`; do not commit the intermediate RED state.

Make GREEN by adding to only the 1960 `known_divergences` row:
```json
"deferral": {
  "reason": "phongthuycaivan.org=5 and lasotuvi.com=6 remain unresolved; independent secondary verification does not settle the center-star encoding",
  "expected_review_date": "2026-12-31",
  "assigned_to": "external-huyen-khong-reviewer"
}
```
Update that row's note to use the literal `PendingExternalReview` and its tiebreaker to say that center 5 remains the provisional *Thẩm Thị Huyền Không Học* operational value pending review. Preserve both source values, `our_value: 5`, the annual case's `expected_center: 5`, and its Plan 16-01 `confidence: "high"`. Do not resolve the split, change 5 to 6, remove the `KnownDivergence`, or imply that dual-source modern verification settled this case.

Run Test G GREEN and then all 11 `fengshui_invariants` tests. The new optional field must let payloads without `deferral` deserialize unchanged.
  </action>
  <verify>
    <automated>cargo test -p amlich-core --test fengshui_invariants test_g_1960_divergence_deferred -- --exact</automated>
  </verify>
  <done>The 1960 divergence has a typed, fully populated deferral marker, preserves provisional value 5 and both source values, and Test G proves the locked deferred disposition.</done>
</task>

<task type="auto">
  <name>Task 2: Finalize ADR-0003a Consequences for the deferred disposition</name>
  <files>.planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md</files>
  <action>
Update ADR-0003a's `Consequences` section to match the shipped ledger exactly. State that the 1960 Trung Nguyên `KnownDivergence` is `PendingExternalReview`, why phongthuycaivan.org=5 versus lasotuvi.com=6 remains unresolved, that the expected review date is `2026-12-31`, and that responsibility is assigned to `external-huyen-khong-reviewer`.

Record the tiebreaker decision precisely: `our_value=5` and `expected_center=5` remain the provisional operational value from *Thẩm Thị Huyền Không Học* while review is pending; this is not a resolved disposition. Explain that HIGH confidence applies to the Thượng/Trung polarity matrix after independent-secondary modern cross-check, not to consensus on the disputed 1960 center value. Retain the Backward Compatibility statement that ADR-0003 §§1–5 remain authoritative and only §6 is superseded. Keep source wording as independent secondary/modern, never a fabricated second classical reference.
  </action>
  <verify>
    <automated>python -c "from pathlib import Path; s=Path('.planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md').read_text(); required=('## Consequences','PendingExternalReview','2026-12-31','external-huyen-khong-reviewer','our_value=5','expected_center=5','independent secondary','ADR-0003 §§1–5'); assert all(x in s for x in required)"</automated>
  </verify>
  <done>ADR-0003a Consequences and the golden divergence ledger state the same deferred status, reason, date, assignee, provisional tiebreaker, and matrix-versus-case confidence distinction.</done>
</task>

</tasks>

<verification>
- `cargo fmt --all --check`
- `cargo clippy -p amlich-core --all-targets -- -D warnings`
- `cargo test -p amlich-core --test fengshui_invariants` reports 11 passing tests: all 9 v1.5 tests plus Tests F and G.
- `cargo test -p amlich-core` keeps the complete 886-test v1.5 baseline green and passes both Phase 16 additions.
- `cargo build -p amlich-core --release`
- Confirm the 1960 case and divergence still use value 5, retain both source values, carry explicit HIGH case confidence from Plan 16-01, and contain the populated deferral object.
</verification>

<success_criteria>
- FND-08 is traceable from the typed optional schema to the 1960 JSON row, Test G, and ADR-0003a Consequences.
- The disposition is visibly deferred, not silently corrected or accidentally described as resolved.
- The deferral marker is additive and absent markers remain deserializable.
- All 9 prior fengshui invariant tests, Test F, and all 886 v1.5 tests remain green.
</success_criteria>

<output>
After completion, create `.planning/phases/16-foundation-adr-0003-confidence-closure/16-02-SUMMARY.md`.
</output>
