---
phase: 07-hour-pillar-parity-core
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/almanac/hour_pillar.rs
  - crates/amlich-core/src/almanac/mod.rs
autonomous: true
requirements: [HP-01, HP-02, HP-04]
must_haves:
  truths:
    - "System computes a deterministic hour pillar from day stem plus local hour/minute inputs"
    - "Day-stem group seed mapping (Giáp/Kỷ, Ất/Canh, Bính/Tân, Đinh/Nhâm, Mậu/Quý) drives correct hour-stem progression"
    - "Hour pillar output includes RuleEvidence-aligned metadata for provenance"
  artifacts:
    - path: "crates/amlich-core/src/almanac/hour_pillar.rs"
      provides: "Core hour-pillar domain types, seed/slot mapping functions, and deterministic calculator"
      exports: ["HourPillarResult", "HourBranchSlot", "compute_hour_pillar", "resolve_hour_branch_slot"]
      min_lines: 220
    - path: "crates/amlich-core/src/almanac/mod.rs"
      provides: "Module export for hour_pillar"
      contains: "pub mod hour_pillar"
  key_links:
    - from: "crates/amlich-core/src/almanac/hour_pillar.rs"
      to: "crates/amlich-core/src/almanac/types.rs"
      via: "Typed day stem input and RuleEvidence metadata output"
      pattern: "HeavenlyStem|RuleEvidence"
    - from: "crates/amlich-core/src/almanac/hour_pillar.rs"
      to: "crates/amlich-core/src/types.rs"
      via: "Canonical CanChi construction and modular index progression"
      pattern: "CanChi::new|normalize_index"
---

<objective>
Implement the hour-pillar parity core in amlich-core as a deterministic, table-driven module.

Purpose: Establish the canonical phase-7 foundation for hour stem/branch derivation so boundary fixtures and parity validators can build on stable typed APIs.
Output: New `hour_pillar` module with seed-group rules, slot mapping, and evidence-carrying result types.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-hour-pillar-parity-core/07-RESEARCH.md
@crates/amlich-core/src/almanac/types.rs
@crates/amlich-core/src/types.rs
@crates/amlich-core/src/gio_hoang_dao.rs

<interfaces>
From `crates/amlich-core/src/almanac/types.rs`:
```rust
pub enum HeavenlyStem {
    Giap, At, Binh, Dinh, Mau, Ky, Canh, Tan, Nham, Quy,
}

pub struct RuleEvidence {
    pub source_id: String,
    pub method: String,
    pub profile: String,
}
```

From `crates/amlich-core/src/types.rs`:
```rust
pub struct CanChi {
    pub can_index: usize,
    pub chi_index: usize,
    pub can: String,
    pub chi: String,
    pub full: String,
    pub sexagenary_index: usize,
}

pub fn normalize_index(value: i32, modulo: i32) -> usize;
```

From `crates/amlich-core/src/gio_hoang_dao.rs`:
```rust
pub fn get_hour_time_range(chi_index: usize) -> &'static str;
```
</interfaces>

# Implementation constraints:
# - No timezone conversion logic in this module; inputs are already local time
# - Use fixed 12 two-hour branch windows with explicit Tý split handling (23:00-00:59)
# - Keep mapping deterministic and index-based (no stringly-typed branch arithmetic)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed hour-pillar domain contracts and seed-mapping helpers</name>
  <files>crates/amlich-core/src/almanac/hour_pillar.rs, crates/amlich-core/src/almanac/mod.rs</files>
  <behavior>
    - Test 1: Each day-stem group resolves to the expected Tý-hour stem seed index
    - Test 2: Stem progression increments by one per hour slot with modulo-10 rollover
    - Test 3: Module exports compile and are accessible via `almanac::hour_pillar`
  </behavior>
  <action>
    Create `hour_pillar.rs` with public types and helpers:
    - `HourBranchSlot` (slot index 0-11 plus branch metadata)
    - `HourPillarResult` carrying `can_chi`, `slot`, and `evidence`
    - private helper for day-stem group -> Tý seed stem index mapping:
      - Giáp/Kỷ -> 0, Ất/Canh -> 2, Bính/Tân -> 4, Đinh/Nhâm -> 6, Mậu/Quý -> 8
    - helper that derives hour stem index from `(seed + slot_index) mod 10`

    Add focused unit tests in `hour_pillar.rs` for seed mapping and progression invariants.
    Export module by updating `almanac/mod.rs` with `pub mod hour_pillar;`.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib hour_pillar::tests::seed_mapping</automated>
  </verify>
  <done>
    Core hour-pillar contracts and seed-group mapping are implemented and test-backed.
    Module is exported and compilable through the almanac namespace.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic hour-slot resolution and compute_hour_pillar API with evidence</name>
  <files>crates/amlich-core/src/almanac/hour_pillar.rs</files>
  <behavior>
    - Test 1: Boundary minute pairs at every transition map to exactly one adjacent slot with no overlap/gap
    - Test 2: `compute_hour_pillar` returns expected CanChi for representative cases across all five day-stem groups
    - Test 3: Returned result includes non-empty RuleEvidence fields with stable method/profile tokens
  </behavior>
  <action>
    Implement:
    - `resolve_hour_branch_slot(local_hour: u8, local_minute: u8) -> Option<HourBranchSlot>`
    - `compute_hour_pillar(day_stem: HeavenlyStem, local_hour: u8, local_minute: u8) -> Option<HourPillarResult>`

    Rules:
    - 12 fixed windows: Tý(23-00), Sửu(01-02), ..., Hợi(21-22)
    - Validate hour in 0..=23 and minute in 0..=59
    - Branch index equals slot index (Tý=0 ... Hợi=11)
    - Hour stem index = `(seed_index + slot_index) mod 10`
    - Build `CanChi::new(hour_stem_index, branch_index)`
    - Attach evidence using project conventions (`source_id`, `method`, `profile`)

    Add unit tests that explicitly assert transition boundaries:
    - `00:59 -> Tý`, `01:00 -> Sửu`
    - `22:59 -> Hợi`, `23:00 -> Tý`
    - all odd-hour transitions around `xx:59/xx+1:00`
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib hour_pillar::tests</automated>
  </verify>
  <done>
    Hour-slot mapping and hour-pillar computation are deterministic and boundary-safe.
    Results include evidence metadata aligned with existing RuleEvidence conventions.
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --package amlich-core --lib hour_pillar::tests
cargo check --package amlich-core
```
</verification>

<success_criteria>
1. HP-01 satisfied with deterministic day-stem + local-time hour-pillar computation API
2. HP-02 satisfied with explicit five-group stem-seed mapping and slot progression tests
3. HP-04 satisfied with evidence metadata embedded in hour-pillar results
</success_criteria>

<output>
After completion, create `.planning/phases/07-hour-pillar-parity-core/07-01-SUMMARY.md`
</output>
