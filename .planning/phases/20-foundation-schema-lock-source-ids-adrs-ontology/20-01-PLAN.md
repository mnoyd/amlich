---
phase: 20-foundation-schema-lock-source-ids-adrs-ontology
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/sources.rs
  - crates/amlich-core/tests/source_id_guard.rs
  - .planning/adrs/0005-hexagram-entry-schema-v1.md
  - .planning/adrs/0006-mai-hoa-casting-convention.md
  - .planning/adrs/0007-cross-link-crit3-carve-out.md
  - .planning/MILESTONES.md
autonomous: true
requirements: [FND-09, FND-10]

must_haves:
  truths:
    - "A reader can find SOURCE_KINH_DICH = \"kinh-dich\" and SOURCE_MAI_HOA_DICH_SO = \"mai-hoa-dich-so\" as `pub const` in sources.rs"
    - "A reader can find the source_id_guard.rs FORBIDDEN_LITERALS array extended so bare \"kinh-dich\" / \"mai-hoa-dich-so\" literals fail CI at provenance call-sites"
    - "A reader can find three accepted ADRs in .planning/adrs/: ADR-0005, ADR-0006, ADR-0007 (Nygard short-form: Title/Status/Context/Decision/Consequences)"
    - "A reader can find three new DEC-0026/0027/0028 rows in MILESTONES.md cross-referencing the three ADRs"
  artifacts:
    - path: "crates/amlich-core/src/sources.rs"
      provides: "SOURCE_KINH_DICH + SOURCE_MAI_HOA_DICH_SO pub const"
      contains: "SOURCE_KINH_DICH"
    - path: "crates/amlich-core/tests/source_id_guard.rs"
      provides: "FORBIDDEN_LITERALS extended with two new source ids"
      contains: "kinh-dich"
    - path: ".planning/adrs/0005-hexagram-entry-schema-v1.md"
      provides: "Accepted HexagramEntry schema v1 decision"
      contains: "Status"
    - path: ".planning/adrs/0006-mai-hoa-casting-convention.md"
      provides: "Accepted Mai Hoa casting convention decision"
      contains: "Thiệu Khang Tiết"
    - path: ".planning/adrs/0007-cross-link-crit3-carve-out.md"
      provides: "Accepted cross-link CRIT-3 carve-out decision"
      contains: "direction_cross_link"
    - path: ".planning/MILESTONES.md"
      provides: "DEC-0026/0027/0028 ADR cross-reference rows"
      contains: "DEC-0026"
  key_links:
    - from: "crates/amlich-core/src/sources.rs"
      to: "crates/amlich-core/tests/source_id_guard.rs"
      via: "FORBIDDEN_LITERALS mirrors every SOURCE_* const value"
      pattern: "kinh-dich.*mai-hoa-dich-so"
    - from: ".planning/adrs/0005-hexagram-entry-schema-v1.md"
      to: ".planning/MILESTONES.md"
      via: "DEC-0026 row cross-references the ADR"
      pattern: "DEC-0026.*0005"
---

<objective>
Register the two v1.7 source IDs (FND-09) and accept the three foundational ADRs (FND-10) that lock the schema, casting convention, and cross-link carve-out BEFORE any corpus or algorithm code is written.

Purpose: This is the "decision registration" foundation. Source IDs + CI guard make provenance call-sites auditable from day one. The three ADRs lock the contracts that Plans 20-02 (schema) and 20-03 (ontology) implement, and that Phases 21-25 build upon. Schema-lock-first (CRIT-1 × 7 prevention).

Output: 2 registered `pub const` source IDs, an extended CI guard, 3 accepted Nygard-form ADRs, and 3 MILESTONES.md DEC cross-reference rows.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation-schema-lock-source-ids-adrs-ontology/20-CONTEXT.md
@.planning/phases/20-foundation-schema-lock-source-ids-adrs-ontology/20-RESEARCH.md

<interfaces>
<!-- Existing source-id discipline the executor will extend. Verified line numbers. -->

From crates/amlich-core/src/sources.rs (the canonical home — append new consts after SOURCE_HUYEN_KHONG at line 26):
```rust
pub const SOURCE_KHCBPPT: &str = "khcbppt";
pub const SOURCE_NGOC_HAP_KY: &str = "ngoc-hap-ky";
pub const SOURCE_VN_FOLK: &str = "vn-folk";
pub const SOURCE_CUU_DIEU: &str = "cuu-dieu";
pub const SOURCE_TAM_MENH_THONG_HOI: &str = "tam-menh-thong-hoi";
pub const SOURCE_VN_FOLK_RITUAL: &str = "vn-folk-ritual";
pub const SOURCE_HUYEN_KHONG: &str = "huyen-khong";
// ← append SOURCE_KINH_DICH + SOURCE_MAI_HOA_DICH_SO here

// Existing test to extend (lines 47-56):
#[cfg(test)]
fn all_constants_have_expected_values() {
    assert_eq!(SOURCE_KHCBPPT, "khcbppt");
    // ... 7 existing asserts — append 2 more for the new consts
}
```

From crates/amlich-core/tests/source_id_guard.rs:13-21 (escaped-quote format; guard skips sources.rs + #[cfg(test)] + // comments):
```rust
const FORBIDDEN_LITERALS: &[&str] = &[
    "\"khcbppt\"",
    "\"vn-folk\"",
    // ... 7 existing entries — append:
    "\"kinh-dich\"",           // NEW v1.7
    "\"mai-hoa-dich-so\"",     // NEW v1.7
];
```

ADR template (Nygard short-form, from .planning/adrs/0001-ritual-schema-v1.md + 0004-daily-phi-tinh-starting-star-convention.md):
```markdown
# ADR-00NN: Title

- **Status:** Accepted
- **Date:** 2026-07-16
## Context
## Decision
## Consequences
```

MILESTONES.md ADR Cross-References table (lines ~275-279; highest registered = DEC-0025; next safe = DEC-0026/0027/0028).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Register SOURCE_KINH_DICH + SOURCE_MAI_HOA_DICH_SO + extend CI guard (FND-09)</name>
  <files>crates/amlich-core/src/sources.rs, crates/amlich-core/tests/source_id_guard.rs</files>
  <behavior>
    - Test 1: `all_constants_have_expected_values` asserts SOURCE_KINH_DICH == "kinh-dich" AND SOURCE_MAI_HOA_DICH_SO == "mai-hoa-dich-so"
    - Test 2: source_id_guard.rs rejects any bare `"kinh-dich"` / `"mai-hoa-dich-so"` string literal in src/ outside sources.rs + #[cfg(test)] + comments (existing guard test, now covering 9 literals)
  </behavior>
  <action>
Append two `pub const` after `SOURCE_HUYEN_KHONG` (sources.rs ~line 26), matching the existing doc-comment style exactly:
```rust
/// Kinh Dịch (I-Ching) — Ngô Tất Tố hexagram text corpus (new in v1.7).
pub const SOURCE_KINH_DICH: &str = "kinh-dich";

/// Mai Hoa Dịch Số — Thiệu Khang Tiết casting algorithm (new in v1.7).
pub const SOURCE_MAI_HOA_DICH_SO: &str = "mai-hoa-dich-so";
```
Extend the `all_constants_have_expected_values` test (sources.rs lines ~47-56) with two `assert_eq!` lines for the new constants.

In `tests/source_id_guard.rs:13-21`, append `"\"kinh-dich\""` and `"\"mai-hoa-dich-so\""` to the `FORBIDDEN_LITERALS` array (escaped-quote format matching the 7 existing entries). The guard already skips `sources.rs` itself, `#[cfg(test)]` blocks, and `//`-comments, so no existing fixtures break. Do NOT add the literal strings anywhere outside `sources.rs` + `#[cfg(test)]` test blocks.
  </action>
  <verify>
    <automated>cargo test -p amlich-core sources --quiet && cargo test -p amlich-core --test source_id_guard --quiet</automated>
  </verify>
  <done>SOURCE_KINH_DICH + SOURCE_MAI_HOA_DICH_SO registered as pub const; source_id_guard extended and passing; no new crate deps.</done>
</task>

<task type="auto">
  <name>Task 2: Author ADR-0005 (HexagramEntry schema) + ADR-0006 (Mai Hoa casting convention) + ADR-0007 (cross-link CRIT-3 carve-out) + MILESTONES DEC rows (FND-10)</name>
  <files>.planning/adrs/0005-hexagram-entry-schema-v1.md, .planning/adrs/0006-mai-hoa-casting-convention.md, .planning/adrs/0007-cross-link-crit3-carve-out.md, .planning/MILESTONES.md</files>
  <action>
Author three ADRs in Nygard short-form (Title/Status/Context/Decision/Consequences), mirroring the length + tone of `.planning/adrs/0001-ritual-schema-v1.md` and `.planning/adrs/0004-daily-phi-tinh-starting-star-convention.md`. All three: **Status: Accepted**, **Date: 2026-07-16**.

**ADR-0005 — HexagramEntry schema v1.** Decision locks:
- The full `HexagramEntry` field set per CONTEXT.md (king_wen_index, vi_name, vi_name_en: Option, upper_trigram/lower_trigram as HauThienTrigram, thoai_tu, thoai_tu_en: Option, hao_tu: Vec (6 entries; 7 for hexagrams 1 & 2's dụng cửu/dụng lục), hao_tu_en: Option, cat_hung, reviewer: String, pending_review: Option&lt;DeferralMarker&gt;) with `#[serde(deny_unknown_fields)]`.
- `#[serde(default, skip_serializing_if = "Option::is_none")]` on all reserved `*_en` + `pending_review` fields (additive, mirrors RIT-13 body_en).
- Reuse `DeferralMarker` from `almanac/fengshui/golden.rs` verbatim for `pending_review`.
- The `hao_tu` length rule (6 for #3..64; 7 for #1 & #2) is a loader invariant (enforced in Phase 21); ADR-0005 MUST mention the rule.
- The naming-convention divergence: `vi_name` (language marker at FRONT for content) vs `thoai_tu`/`hao_tu`/`cat_hung` (romanized VN technical terms unmarked) DIFFERS from rituals' `body`/`body_en` suffix pattern — ADR body MUST explicitly document this divergence so a future maintainer does not "fix" it.
- The `reviewer: String` free-text field using the `ExternalReviewPending(reason="..."; expected_review_date="..."; assigned_to="...")` marker shape from `data/rituals/provenance_audit.md` (NOT a typed struct — survives reviewer-name change without schema migration).
- Pin the `HauThienTrigram` encoding to Lo Shu palace numbers (Khảm=1, Khôn=2, Chấn=3, Tốn=4, Kiền=6, Đoài=7, Cấn=8, Ly=9 — skip 5/center) — same assignment as the existing `Palace` enum. Pre-empts the Hậu Thiên sub-school variance.

**ADR-0006 — Mai Hoa casting convention.** Decision locks (two-source pin from day 1):
- Inputs are LUNAR (not solar): `lunar_year_branch`, `lunar_month`, `lunar_day`, `chi_hour_index`.
- The `((n-1) % k) + 1` remainder-zero convention is canonical (CRIT-2 prevention).
- Two sources cited: **Thiều Khang Tiết** (*Mai Hoa Dịch số*, dịch giả Văn Tùng, NXB Văn Hoá Thông tin, Hà Nội, 2002) as classical authority for the Tiên Thiên arrangement + **nhantu.net** as modern Vietnamese practitioner reference (concrete URL + accessed-date).
- INCLUDE the worked boundary example IN THE ADR BODY verbatim from 20-RESEARCH.md §"Code Examples > ADR-0006 worked boundary example" — the `month=8/day=8/hour=8 → Tiên Thiên 8 (Khôn), NOT 1 (Kiền)` derivation with the explicit `((8-1)%8)+1 = 8` arithmetic inline. This is the CRIT-2 prevention proof; a reader must verify by inspection.
- Defer exact Rust parameter encoding (chi as u8 vs typed enum) to Phase 22.
- Best-effort page citation; add an explicit `PendingExternalReview` page-deferral note if a page number is unavailable (mirrors ADR-0004's chapter+verse discipline).

**ADR-0007 — Cross-link CRIT-3 carve-out.** Decision locks:
- The cross-link lives in read-only `reasoning/direction_composite.rs::build_direction_cross_link` (Phase 23 populates; Phase 20 documents the placement contract).
- Composite envelope pattern: emits distinct primitive `source_id` envelopes (`khcbppt` + `huyen-khong`) PLUS one composite `rule.composite.direction_cross_link` envelope (the only pattern compatible with the CRIT-3 grep guard).
- The CRIT-3 grep guard sibling `tests/thai_tue_cross_link_crit3.rs` (Phase 23 authors) forbids `FlyingStar` references in `interaction/direction_merge.rs`.
- `build_direction_cross_link` takes only `&` references (read-only by design — does not mutate Thái Tuế/Phi Tinh).

**MILESTONES.md:** append three rows to the ADR Cross-References table: DEC-0026 → ADR-0005, DEC-0027 → ADR-0006, DEC-0028 → ADR-0007 (DEC-0025 is highest registered; confirm no collision with `rg "DEC-002[6789]"` before writing). Do NOT backfill the v1.6 ADR-0003a/0004 gap (separate cleanup).
  </action>
  <verify>
    <automated>test -f .planning/adrs/0005-hexagram-entry-schema-v1.md && test -f .planning/adrs/0006-mai-hoa-casting-convention.md && test -f .planning/adrs/0007-cross-link-crit3-carve-out.md && rg -q "DEC-0026" .planning/MILESTONES.md && rg -q "Thiệu Khang Tiết" .planning/adrs/0006-mai-hoa-casting-convention.md && rg -q "deny_unknown_fields" .planning/adrs/0005-hexagram-entry-schema-v1.md && rg -q "direction_cross_link" .planning/adrs/0007-cross-link-crit3-carve-out.md</automated>
  </verify>
  <done>Three ADRs exist with Status: Accepted, each in Nygard short-form; ADR-0006 contains the worked Khôn boundary example; MILESTONES.md has DEC-0026/0027/0028 rows; no DEC collision.</done>
</task>

</tasks>

<verification>
- `cargo test -p amlich-core sources --quiet` — source const test passes (9 consts).
- `cargo test -p amlich-core --test source_id_guard --quiet` — guard extended, still passing.
- All three ADR files exist with `Status: Accepted` + the Nygard sections.
- ADR-0005 mentions `deny_unknown_fields`, the `hao_tu` length rule, the naming divergence, and the Lo Shu pin.
- ADR-0006 cites both Thiệu Khang Tiết + nhantu.net AND contains the `((8-1)%8)+1 = 8` worked example.
- ADR-0007 names `build_direction_cross_link` + `rule.composite.direction_cross_link` + the CRIT-3 guard.
- MILESTONES.md has DEC-0026/0027/0028 with no collision.
</verification>

<success_criteria>
- A reader finds `SOURCE_KINH_DICH` + `SOURCE_MAI_HOA_DICH_SO` in sources.rs and the guard rejects their bare literals at call-sites.
- A reader finds three accepted ADRs locking schema, casting convention, and cross-link placement.
- The CRIT-2 prevention proof (worked Khôn boundary) is self-contained in ADR-0006.
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-schema-lock-source-ids-adrs-ontology/20-01-SUMMARY.md`
</output>
