---
phase: v1.1-foundation-extensions
verified: 2026-03-02T07:45:19Z
status: partial
score: 17/23 requirements complete (XH+TC complete, TK blocked)
gaps:
  - "TK-01..TK-06 blocked by failing tietkhi tests in full package gate"
---

# Phase v1.1: Foundation Extensions Verification Report

**Phase Goal:** Every XH/TC/TK requirement has requirement-level implementation evidence and fresh command-backed test evidence in one canonical artifact.
**Verified:** 2026-03-02T07:45:19Z
**Status:** PARTIAL (XH+TC complete, TK blocked)
**Re-verification:** Yes — fresh evidence run during v1.1.1 gap closure

## Goal Achievement

### Observable Truths

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | XH-01..XH-08 have explicit file-level + fresh test evidence | ✓ VERIFIED | `cargo test --package amlich-core xung_hop` passed (20/20 filtered xung_hop tests, plus xung_hop validator integration) |
| 2 | TC-01..TC-09 have explicit file-level + fresh test evidence | ✓ VERIFIED | `cargo test --package amlich-core tang_can` passed (8/8 filtered tang_can tests) |
| 3 | Full milestone acceptance truth is preserved, not over-claimed | ✓ VERIFIED | `cargo test --package amlich-core` failed with 5 tietkhi tests; TK rows remain blocked |

## Fresh Evidence Snapshot

Date: **2026-03-02**

### Command 1 — Xung Hợp targeted evidence

`cargo test --package amlich-core xung_hop`

- Result: **PASS**
- Key output: `20 passed; 0 failed` in `almanac::xung_hop` unit scope
- Integration evidence: `validate_xung_hop_against_golden ... ok`

### Command 2 — Tàng Can targeted evidence

`cargo test --package amlich-core tang_can`

- Result: **PASS**
- Key output: `8 passed; 0 failed` in tang_can/types filtered scope

### Command 3 — Full acceptance gate snapshot

`cargo test --package amlich-core`

- Result: **FAIL (expected for current milestone truth)**
- Summary: `183 passed; 5 failed`
- Failing tests (tietkhi):
  - `test_after_returns_positive`
  - `test_before_returns_negative`
  - `test_equidistant_prefers_after`
  - `test_exact_match_returns_zero`
  - `test_term_positions_in_year`

## Requirement Coverage Matrix

| Requirement | Implementation Evidence | Fresh Test Evidence | Status | Notes |
|-------------|-------------------------|---------------------|--------|-------|
| XH-01 | `crates/amlich-core/src/almanac/xung_hop.rs` (`LIUHE`, `get_liu_he`) | `cargo test --package amlich-core xung_hop` | complete | Lục Hợp pairs implemented and verified |
| XH-02 | `crates/amlich-core/src/almanac/xung_hop.rs` (`XIANGHAI`, `get_xiang_hai`) | `cargo test --package amlich-core xung_hop` | complete | Tương Hại pairs implemented and verified |
| XH-03 | `crates/amlich-core/src/almanac/xung_hop.rs` (`XIANGXING`, `get_xiang_xing`) | `cargo test --package amlich-core xung_hop` | complete | Tương Hình groups and Ngọ self-punishment case verified |
| XH-04 | `crates/amlich-core/src/almanac/types.rs` (`XungHopResult` fields) | `cargo test --package amlich-core xung_hop` | complete | Struct includes `liu_he`, `xiang_hai`, `xiang_xing` |
| XH-05 | `crates/amlich-core/src/almanac/xung_hop.rs` (new constant tables) | `cargo test --package amlich-core xung_hop` | complete | Constants wired to runtime functions |
| XH-06 | `crates/amlich-core/src/almanac/xung_hop.rs` (`get_xung_hop`) | `cargo test --package amlich-core xung_hop` | complete | `get_xung_hop()` now emits all six relationship types |
| XH-07 | `crates/amlich-core/src/almanac/xung_hop.rs` tests (`test_liu_he_*`, `test_xiang_hai_*`, `test_xiang_xing_*`) | `cargo test --package amlich-core xung_hop` | complete | New relationship tests pass |
| XH-08 | `crates/amlich-core/src/almanac/calc.rs`, `crates/amlich-core/src/almanac/types.rs` | `cargo test --package amlich-core xung_hop` and `cargo test --package amlich-core` snapshot | complete | DayFortune/serialization integration present; no xung_hop regressions |
| TC-01 | `crates/amlich-core/src/almanac/types.rs` (`TangCan` struct) | `cargo test --package amlich-core tang_can` | complete | Struct fields verified via type tests |
| TC-02 | `crates/amlich-core/src/almanac/tang_can.rs` (`CANGAN_STRENGTH`) | `cargo test --package amlich-core tang_can` | complete | Strength table values covered by tests |
| TC-03 | `crates/amlich-core/src/almanac/tang_can.rs` (`CANGAN`) | `cargo test --package amlich-core tang_can` | complete | 12-branch hidden stem table covered |
| TC-04 | `crates/amlich-core/src/almanac/mod.rs`, `crates/amlich-core/src/almanac/tang_can.rs` | `cargo test --package amlich-core tang_can` | complete | TangCan module exists and callable |
| TC-05 | `crates/amlich-core/data/almanac/baseline.json` (`tang_can_meta`) | `cargo test --package amlich-core tang_can` and full snapshot | complete | Baseline schema extension present and loadable |
| TC-06 | `crates/amlich-core/data/almanac/baseline.json` (`tang_can_by_chi`) | `cargo test --package amlich-core tang_can` | complete | All 12 branches populated |
| TC-07 | `crates/amlich-core/src/almanac/types.rs` (`DayFortune.tang_can`) | `cargo test --package amlich-core tang_can` | complete | Field integrated into API surface |
| TC-08 | `crates/amlich-core/src/almanac/calc.rs` (`get_tang_can` call in `calculate_day_fortune`) | `cargo test --package amlich-core tang_can` | complete | Runtime population path verified |
| TC-09 | `crates/amlich-core/src/almanac/tang_can.rs` tests | `cargo test --package amlich-core tang_can` | complete | TangCan tests green with fresh run |
| TK-01 | `crates/amlich-core/src/tietkhi.rs` (`get_days_to_nearest_tiet_khi`) | `cargo test --package amlich-core` | blocked | Helper exists; acceptance gate red |
| TK-02 | `crates/amlich-core/src/tietkhi.rs` (nearest-before search behavior) | `cargo test --package amlich-core` | blocked | `test_before_returns_negative` failing |
| TK-03 | `crates/amlich-core/src/tietkhi.rs` (nearest-after search behavior) | `cargo test --package amlich-core` | blocked | `test_after_returns_positive` failing |
| TK-04 | `crates/amlich-core/src/tietkhi.rs` (signed difference behavior) | `cargo test --package amlich-core` | blocked | `test_equidistant_prefers_after` failing |
| TK-05 | `crates/amlich-core/src/tietkhi.rs` (edge-case handling) | `cargo test --package amlich-core` | blocked | `test_exact_match_returns_zero` and `test_term_positions_in_year` failing |
| TK-06 | `crates/amlich-core/src/tietkhi.rs` tests | `cargo test --package amlich-core` | blocked | Unit tests exist but not green; handoff to v1.1.1-02 |

## Required Artifact Links

| From | To | Via |
|------|----|-----|
| `.planning/phases/v1.1-foundation-extensions/v1.1-01-SUMMARY.md` | `.planning/phases/v1.1-foundation-extensions/v1.1-VERIFICATION.md` | `requirements-completed`, `verification-artifact` frontmatter |
| `.planning/phases/v1.1-foundation-extensions/v1.1-02-SUMMARY.md` | `.planning/phases/v1.1-foundation-extensions/v1.1-VERIFICATION.md` | `requirements-completed`, `verification-artifact` frontmatter |
| `.planning/REQUIREMENTS-v1.1.md` | `.planning/phases/v1.1-foundation-extensions/v1.1-VERIFICATION.md` | Traceability status references canonical evidence ledger |

## Gaps Summary

- **Open gaps:** TK-01..TK-06 (blocked by tietkhi test failures)
- **Handoff:** v1.1.1-02 focuses on fixing TK gate and reconciling milestone status once full acceptance is green
- **No over-claims:** This artifact marks only XH/TC complete; TK remains blocked until full gate passes

---

_Verified: 2026-03-02T07:45:19Z_
_Verifier: Claude (gsd-executor)_
