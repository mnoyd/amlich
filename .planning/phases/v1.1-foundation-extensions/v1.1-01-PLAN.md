---
phase: v1.1-foundation-extensions
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/amlich-core/src/almanac/xung_hop.rs, crates/amlich-core/src/almanac/types.rs]
autonomous: true
requirements: [XH-01, XH-02, XH-03, XH-04, XH-05, XH-06, XH-07, XH-08]
must_haves:
  truths:
    - "User can see all 6 Xung Hợp relationship types (Lục xung, Tam hợp, Tứ hành xung, Lục hợp, Tương hại, Tương hình)"
    - "All relationship types are symmetric (A's partner is B, B's partner is A)"
    - "Tương hình self-punishment (Ngọ Ngọ) is handled correctly"
  artifacts:
    - path: "crates/amlich-core/src/almanac/xung_hop.rs"
      provides: "Lục hợp, Tương hại, Tương hình constant tables and functions"
      exports: ["LIUHE", "XIANGHAI", "XIANGXING", "get_xung_hop()"]
      min_lines: 250
    - path: "crates/amlich-core/src/almanac/types.rs"
      provides: "Extended XungHopResult struct with new relationship fields"
      contains: "liu_he, xiang_hai, xiang_xing fields"
  key_links:
    - from: "crates/amlich-core/src/almanac/xung_hop.rs"
      to: "crates/amlich-core/src/almanac/types.rs"
      via: "XungHopResult struct definition"
      pattern: "pub struct XungHopResult.*pub liu_he.*pub xiang_hai.*pub xiang_xing"
    - from: "crates/amlich-core/src/almanac/calc.rs"
      to: "crates/amlich-core/src/almanac/xung_hop.rs"
      via: "get_xung_hop() function call"
      pattern: "use super::xung_hop::get_xung_hop"
---

<objective>
Implement 3 missing Xung Hợp relationship types (Lục hợp, Tương hại, Tương hình) to provide complete branch relationship analysis, extending the existing system that already has Lục xung, Tam hợp, and Tứ hành xung.

Purpose: Complete the Xung Hợp relationship system so users can see all 6 classical relationship types for any given Địa Chi, enabling more comprehensive almanac analysis and preparation for advanced features.
Output: Enhanced `xung_hop.rs` module with 3 new constant tables, updated `XungHopResult` struct, and comprehensive test coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS-v1.1.md
@crates/amlich-core/src/almanac/xung_hop.rs
@crates/amlich-core/src/almanac/types.rs
@crates/amlich-core/src/canchi.rs

# Only reference prior plan SUMMARYs if genuinely needed
@.planning/MILESTONES.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add Lục hợp constant table and tests</name>
  <files>crates/amlich-core/src/almanac/xung_hop.rs</files>
  <behavior>
    - Test 1: LIUHE constant has 6 pairs covering all 12 branches
    - Test 2: LIUHE is symmetric (if (0,1) exists, (1,0) exists)
    - Test 3: get_liu_he() function returns correct partner for all branches
  </behavior>
  <action>
    Add LIUHE constant table to xung_hop.rs following existing pattern:
    ```rust
    pub const LIUHE: [(usize, usize); 6] = [
        (0, 1),   // 子丑
        (2, 11),  // 寅亥
        (3, 10),  // 卯戌
        (4, 9),   // 辰酉
        (5, 8),   // 巳申
        (6, 7),   // 午未
    ];
    ```
    Add get_liu_he() function that returns harmony partner:
    ```rust
    pub fn get_liu_he(chi_index: usize) -> &'static str {
        for (a, b) in LIUHE.iter() {
            if *a == chi_index {
                return CHI[*b];
            }
            if *b == chi_index {
                return CHI[*a];
            }
        }
        CHI[chi_index] // No partner (should not happen with full coverage)
    }
    ```
    Add unit tests in #[cfg(test)] mod:
    - test_liu_he_complete_coverage: all 12 branches find a partner
    - test_liu_he_symmetric: every pair is bidirectional
    - test_liu_he_returns_correct_partner: specific branches return expected results (子→丑, etc.)
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib xung_hop</automated>
  </verify>
  <done>
    LIUHE constant table defined with 6 pairs
    get_liu_he() function returns correct partner for all 12 branches
    All unit tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add Tương hại constant table and tests</name>
  <files>crates/amlich-core/src/almanac/xung_hop.rs</files>
  <behavior>
    - Test 1: XIANGHAI constant has 6 pairs covering all 12 branches
    - Test 2: XIANGHAI is symmetric (if (0,7) exists, (7,0) exists)
    - Test 3: get_xiang_hai() function returns correct harm partner for all branches
  </behavior>
  <action>
    Add XIANGHAI constant table to xung_hop.rs:
    ```rust
    pub const XIANGHAI: [(usize, usize); 6] = [
        (0, 7),   // 子未
        (1, 6),   // 丑午
        (2, 9),   // 寅酉
        (3, 8),   // 卯申
        (4, 11),  // 辰亥
        (5, 10),  // 巳戌
    ];
    ```
    Add get_xiang_hai() function that returns harm partner:
    ```rust
    pub fn get_xiang_hai(chi_index: usize) -> &'static str {
        for (a, b) in XIANGHAI.iter() {
            if *a == chi_index {
                return CHI[*b];
            }
            if *b == chi_index {
                return CHI[*a];
            }
        }
        CHI[chi_index]
    }
    ```
    Add unit tests:
    - test_xiang_hai_complete_coverage: all 12 branches find a partner
    - test_xiang_hai_symmetric: every pair is bidirectional
    - test_xiang_hai_returns_correct_partner: specific branches return expected results
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib xung_hop</automated>
  </verify>
  <done>
    XIANGHAI constant table defined with 6 pairs
    get_xiang_hai() function returns correct partner for all 12 branches
    All unit tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add Tương hình constant table and tests</name>
  <files>crates/amlich-core/src/almanac/xung_hop.rs</files>
  <behavior>
    - Test 1: XIANGXING constant has 4 groups with 3-4 members each
    - Test 2: Self-punishment group 午午 has one member
    - Test 3: get_xiang_xing() function returns correct group for all branches
  </behavior>
  <action>
    Add XIANGXING constant table to xung_hop.rs:
    ```rust
    pub const XIANGXING: [[usize; 3]; 4] = [
        [2, 3, 5],   // 寅卯巳 (Vô恩之刑)
        [0, 1, 4],   // 子辰丑 (恃势之刑)
        [8, 9, 11],  // 申酉亥 (无礼之刑)
        [6, 6, 6],   // 午午 (自刑 - self-punishment)
    ];
    ```
    Add get_xiang_xing() function that returns punishment group members as Vec<String>:
    ```rust
    pub fn get_xiang_xing(chi_index: usize) -> Vec<String> {
        for group in XIANGXING.iter() {
            if group.contains(&chi_index) {
                return group.iter().map(|&idx| CHI[idx].to_string()).collect();
            }
        }
        vec![]
    }
    ```
    Add unit tests:
    - test_xiang_xing_groups_complete: all 12 branches appear in punishment groups
    - test_xiang_xing_self_punishment: Ngọ returns [Ngọ, Ngọ, Ngọ] not just [Ngọ]
    - test_xiang_xing_returns_correct_groups: 寅卯巳 returns [Dần, Mão, Tỵ]
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib xung_hop</automated>
  </verify>
  <done>
    XIANGXING constant table defined with 4 groups
    get_xiang_xing() function returns correct group for all 12 branches
    Self-punishment (Ngọ Ngọ) handled correctly
    All unit tests pass
  </done>
</task>

<task type="auto">
  <name>Task 4: Extend XungHopResult struct and update get_xung_hop()</name>
  <files>crates/amlich-core/src/almanac/types.rs, crates/amlich-core/src/almanac/xung_hop.rs</files>
  <action>
    In almanac/types.rs, extend XungHopResult struct:
    ```rust
    pub struct XungHopResult {
        pub luc_xung: String,
        pub tam_hop: Vec<String>,
        pub tu_hanh_xung: Vec<String>,
        // NEW FIELDS:
        pub liu_he: Option<String>,
        pub xiang_hai: Option<String>,
        pub xiang_xing: Option<Vec<String>>,
    }
    ```
    In xung_hop.rs, update get_xung_hop() function to populate new fields:
    ```rust
    pub fn get_xung_hop(chi_index: usize) -> XungHopResult {
        XungHopResult {
            luc_xung: luc_xung(chi_index).to_string(),
            tam_hop: tam_hop(chi_index).iter().map(|s| s.to_string()).collect(),
            tu_hanh_xung: tu_hanh_xung(chi_index).iter().map(|s| s.to_string()).collect(),
            // NEW:
            liu_he: Some(get_liu_he(chi_index).to_string()),
            xiang_hai: Some(get_xiang_hai(chi_index).to_string()),
            xiang_xing: Some(get_xiang_xing(chi_index)),
        }
    }
    ```
    Note: Use Some() for liu_he and xiang_hai (always returns a branch), use Option<Vec<String>> for xiang_xing (may return empty for branches not in punishment groups).
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib xung_hop</automated>
  </verify>
  <done>
    XungHopResult struct extended with 3 new fields
    get_xung_hop() function populates all 3 new relationship types
    All existing tests still pass (no breaking changes)
    Compilation succeeds with no warnings
  </done>
</task>

</tasks>

<verification>
Run full test suite to ensure all existing functionality still works and new tests pass:
```bash
cargo test --package amlich-core
```

Expected result: All tests pass including new Xung Hợp tests, no breaking changes to existing luc_xung, tam_hop, tu_hanh_xung functionality.
</verification>

<success_criteria>
1. ✅ All 6 Xung Hợp relationship types are implemented (Lục xung, Tam hợp, Tứ hành xung, Lục hợp, Tương hại, Tương hình)
2. ✅ Constant tables LIUHE, XIANGHAI, XIANGXING defined with correct values
3. ✅ Functions get_liu_he(), get_xiang_hai(), get_xiang_xing() exist and work correctly
4. ✅ XungHopResult struct extended with liu_he, xiang_hai, xiang_xing fields
5. ✅ get_xung_hop() function updated to populate all 3 new fields
6. ✅ All unit tests pass (new + existing)
7. ✅ No breaking changes to existing relationships (backward compatible)
8. ✅ cargo test passes with 0 failures
</success_criteria>

<output>
After completion, create `.planning/phases/v1.1-foundation-extensions/v1.1-01-SUMMARY.md`
</output>
