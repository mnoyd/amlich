---
phase: 04-core-dai-van-module
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/almanac/dai_van.rs
  - crates/amlich-core/src/canchi.rs
  - crates/amlich-core/src/tietkhi.rs
autonomous: true
requirements: [DV-CALC-01, DV-CALC-04, DV-CALC-05, DV-CALC-06]
must_haves:
  truths:
    - "System generates exactly 8 Dai Van pillars with contiguous 10-year ranges from computed start age"
    - "Month Can Chi is the base pillar and progression follows computed direction without overlap"
    - "Edge-case paths (Tiet Khi boundaries, leap-month month-pillar source, polarity transitions) are test-covered"
  artifacts:
    - path: "crates/amlich-core/src/almanac/dai_van.rs"
      provides: "End-to-end pillar generation and age-range lookup helpers"
      exports: ["calculate_dai_van", "generate_pillars", "get_current_pillar", "get_pillar_at_age", "years_to_next_transition"]
      min_lines: 260
    - path: "crates/amlich-core/src/tietkhi.rs"
      provides: "Signed nearest-term distance primitive consumed by Dai Van start-age conversion"
      contains: "get_days_to_nearest_tiet_khi"
  key_links:
    - from: "crates/amlich-core/src/almanac/dai_van.rs"
      to: "crates/amlich-core/src/canchi.rs"
      via: "month Can Chi base pillar acquisition"
      pattern: "get_month_canchi"
    - from: "crates/amlich-core/src/almanac/dai_van.rs"
      to: "crates/amlich-core/src/tietkhi.rs"
      via: "signed nearest Tiet Khi distance for start_age"
      pattern: "get_days_to_nearest_tiet_khi"
---

<objective>
Implement deterministic Dai Van calculation flow that transforms birth inputs into 8 contiguous pillars and helper lookups, with explicit edge-case handling.

Purpose: Deliver the functional core promised by Phase 4 so downstream phases (Ten Gods and Kua integrations) can rely on stable pillar lifecycle semantics.
Output: Complete `calculate_dai_van` flow, helper functions, and robust edge-case tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-core-dai-van-module/04-CONTEXT.md
@.planning/phases/04-core-dai-van-module/04-RESEARCH.md
@crates/amlich-core/src/canchi.rs
@crates/amlich-core/src/tietkhi.rs
@crates/amlich-core/src/almanac/types.rs

# Implementation constraints:
# - Pillar age ranges must be `[start_age, end_age)` with incoming pillar at exact boundary
# - Helper lookups return None outside covered ranges (no clamping)
# - Keep progression deterministic and table/formula explicit (no hidden magic values)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement calculate_dai_van flow and 8-pillar generation</name>
  <files>crates/amlich-core/src/almanac/dai_van.rs</files>
  <behavior>
    - Test 1: Given valid birth date and gender, result contains exactly 8 pillars
    - Test 2: Pillars have contiguous 10-year ranges with `[start_age, end_age)` semantics
    - Test 3: Month Can Chi is used as the base for directional progression
  </behavior>
  <action>
    Build `calculate_dai_van(...)` orchestration that:
    1. resolves lunar date and month/year Can Chi inputs
    2. determines Chieuthu from year stem polarity x gender
    3. computes start age from nearest Tiet Khi distance
    4. generates 8 pillars by directional stem/branch progression

    Keep the generation algorithm explicit and auditable:
    - directional step of +/-1 for stem (mod 10) and branch (mod 12)
    - age window calculation tied to computed start_age
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib dai_van::tests::pillar_generation</automated>
  </verify>
  <done>
    End-to-end Dai Van calculation returns deterministic 8-pillar output
    Contiguous range invariant holds across all generated pillars
    Base-pillar and progression rules are test-backed
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add helper lookups and edge-case coverage</name>
  <files>crates/amlich-core/src/almanac/dai_van.rs</files>
  <behavior>
    - Test 1: `get_current_pillar` and `get_pillar_at_age` return None before first start_age and after final end_age
    - Test 2: `years_to_next_transition` returns exact remaining years at in-range ages
    - Test 3: Edge fixtures cover Tiet Khi boundary (0 distance), leap-month pathway, and polarity transition cases
  </behavior>
  <action>
    Implement helpers against canonical range semantics:
    - `get_current_pillar`
    - `get_pillar_at_age`
    - `years_to_next_transition`

    Add explicit edge-case tests for:
    - exact boundary behavior (`age == start_age`, `age == end_age`)
    - zero-distance start age
    - leap-month birth flow using existing month Can Chi integration path
    - year polarity transition examples to prevent matrix drift
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib dai_van::tests::helpers_and_edge_cases</automated>
  </verify>
  <done>
    Helper APIs produce explicit Option behavior for out-of-range ages
    Boundary semantics are locked by tests
    Required edge conditions are covered and stable
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --package amlich-core --lib dai_van
```
</verification>

<success_criteria>
1. System generates 8 contiguous Dai Van pillars from birth date and gender (DV-CALC-01, DV-CALC-04)
2. Month Can Chi is used as base pillar for progression (DV-CALC-05)
3. Edge conditions are covered and behavior remains deterministic (DV-CALC-06)
4. Helper lookups reflect explicit range semantics and do not clamp silently
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-dai-van-module/04-02-SUMMARY.md`
</output>
