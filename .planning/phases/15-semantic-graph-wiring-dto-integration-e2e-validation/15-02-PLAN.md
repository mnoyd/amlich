---
phase: 15-semantic-graph-wiring-dto-integration-e2e-validation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/semantic_graph/ontology.rs
autonomous: true
requirements: [INT-03]
must_haves:
  truths:
    - "A semantic-graph reader can find NodeConcept::Ritual and NodeConcept::FlyingStar variants."
    - "A reader can find EdgeConcept::PrescribedFor, EdgeConcept::OccupiesPalace, EdgeConcept::CarriesElement variants."
    - "All match arms (label / as_str) are exhaustive — the compiler enforces NodeConcept/EdgeConcept coverage."
    - "GraphOntology::node_concepts() and edge_concepts() include the new variants (not compiler-checked — test-verified)."
  artifacts:
    - path: "crates/amlich-core/src/semantic_graph/ontology.rs"
      provides: "Two new NodeConcept variants, three new EdgeConcept variants, ConceptLabel mirrors, as_str strings, GraphOntology static slice entries, completeness test"
      contains: "FlyingStar"
  key_links:
    - from: "NodeConcept::Ritual"
      to: "GraphOntology::node_concepts()"
      via: "static slice entry"
      pattern: "NodeConcept::Ritual"
    - from: "EdgeConcept::OccupiesPalace"
      to: "GraphOntology::edge_concepts()"
      via: "static slice entry"
      pattern: "EdgeConcept::OccupiesPalace"
---

<objective>
Extend the semantic-graph ontology with two new node concepts (`Ritual`, `FlyingStar`) and three new edge concepts (`PrescribedFor`, `OccupiesPalace`, `CarriesElement`), updating all six locations in `ontology.rs` atomically so Rust's exhaustive-match checking stays satisfied, and add a completeness test for the non-compiler-checked static slices.

Purpose: INT-03 requires the ontology to surface the two v1.5 pillars as first-class semantic concepts. The enums are closed with parallel `label()`/`as_str()` match arms plus hand-maintained `GraphOntology` static slices. Missing a match arm is a compile error; missing a static-slice entry is a SILENT bug — hence the explicit completeness test.

Output: Extended ontology enums + a test asserting the new variants are enumerable.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/15-semantic-graph-wiring-dto-integration-e2e-validation/15-RESEARCH.md

<interfaces>
<!-- ontology.rs structure (src/semantic_graph/ontology.rs, 346 lines). SIX locations to update. -->

1. `pub enum NodeConcept` (line 5) — 33 variants ending with `RecommendationSummary` (line ~39). Add `Ritual`, `FlyingStar`.
2. `impl NodeConcept { pub fn label(&self) -> ConceptLabel }` (line 43) — parallel match arms `Self::X => ConceptLabel::X`. Add arms for Ritual, FlyingStar.
3. `pub enum EdgeConcept` (line 85) — 25 variants ending with `Aggregates`. Add `PrescribedFor`, `OccupiesPalace`, `CarriesElement`.
4. `impl EdgeConcept { pub fn label(&self) -> ConceptLabel }` (line 114) — add arms for the three edge variants.
5. `pub enum ConceptLabel` (line 147) + `impl ConceptLabel { pub fn as_str(&self) -> &'static str }` (line 210) — mirrors EVERY node + edge variant. Add `Ritual`, `FlyingStar`, `PrescribedFor`, `OccupiesPalace`, `CarriesElement` to the enum AND their `as_str()` string arms.
6. `impl GraphOntology` (line 277): `pub fn node_concepts() -> &'static [NodeConcept]` (line 278) and `pub fn edge_concepts() -> &'static [EdgeConcept]` (line 317) — hand-maintained static slices, NOT compiler-checked. Add the two node + three edge variants to the respective slices.

ConceptLabel::as_str() strings to use (snake_case per existing convention):
- Ritual => "ritual"
- FlyingStar => "flying_star"
- PrescribedFor => "prescribed_for"
- OccupiesPalace => "occupies_palace"
- CarriesElement => "carries_element"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend NodeConcept + EdgeConcept + ConceptLabel across all six ontology locations</name>
  <files>crates/amlich-core/src/semantic_graph/ontology.rs</files>
  <behavior>
    - `NodeConcept::Ritual.label().as_str() == "ritual"` and `NodeConcept::FlyingStar.label().as_str() == "flying_star"`.
    - `EdgeConcept::PrescribedFor.label().as_str() == "prescribed_for"`, `OccupiesPalace => "occupies_palace"`, `CarriesElement => "carries_element"`.
    - Code compiles — exhaustive matches in `label()` and `as_str()` are satisfied.
  </behavior>
  <action>
    Edit `crates/amlich-core/src/semantic_graph/ontology.rs`, touching all six locations atomically:

    1. `NodeConcept` enum: add `Ritual,` and `FlyingStar,` (after `RecommendationSummary`).
    2. `NodeConcept::label()`: add `Self::Ritual => ConceptLabel::Ritual,` and `Self::FlyingStar => ConceptLabel::FlyingStar,`.
    3. `EdgeConcept` enum: add `PrescribedFor,`, `OccupiesPalace,`, `CarriesElement,` (after `Aggregates`).
    4. `EdgeConcept::label()`: add `Self::PrescribedFor => ConceptLabel::PrescribedFor,`, `Self::OccupiesPalace => ConceptLabel::OccupiesPalace,`, `Self::CarriesElement => ConceptLabel::CarriesElement,`.
    5. `ConceptLabel` enum: add `Ritual,`, `FlyingStar,`, `PrescribedFor,`, `OccupiesPalace,`, `CarriesElement,`.
    6. `ConceptLabel::as_str()`: add `Self::Ritual => "ritual",`, `Self::FlyingStar => "flying_star",`, `Self::PrescribedFor => "prescribed_for",`, `Self::OccupiesPalace => "occupies_palace",`, `Self::CarriesElement => "carries_element",`.

    Follow the EXACT formatting/ordering convention already present (place new node concepts after the last node concept, new edge concepts after the last edge concept, and group ConceptLabel additions consistently with existing layout). Do not reorder existing variants.
  </action>
  <verify>
    <automated>cargo build -p amlich-core 2>&1 | tail -5</automated>
  </verify>
  <done>Compiles with no missing-match-arm errors; the five new variants exist with correct `as_str()` strings.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add GraphOntology static-slice entries + completeness test</name>
  <files>crates/amlich-core/src/semantic_graph/ontology.rs</files>
  <behavior>
    - `GraphOntology::node_concepts()` contains `NodeConcept::Ritual` and `NodeConcept::FlyingStar`.
    - `GraphOntology::edge_concepts()` contains `EdgeConcept::PrescribedFor`, `OccupiesPalace`, `CarriesElement`.
    - A test fails RED if any of these five entries is missing from the static slices (the silent-bug guard).
  </behavior>
  <action>
    1. In `GraphOntology::node_concepts()` static slice, add `NodeConcept::Ritual` and `NodeConcept::FlyingStar`.
    2. In `GraphOntology::edge_concepts()` static slice, add `EdgeConcept::PrescribedFor`, `EdgeConcept::OccupiesPalace`, `EdgeConcept::CarriesElement`.
    3. Add a test to the existing `#[cfg(test)] mod tests` block in ontology.rs (or create one if absent):
    ```rust
    #[test]
    fn v15_concepts_present_in_ontology_slices() {
        let nodes = GraphOntology::node_concepts();
        assert!(nodes.contains(&NodeConcept::Ritual), "Ritual missing from node_concepts()");
        assert!(nodes.contains(&NodeConcept::FlyingStar), "FlyingStar missing from node_concepts()");
        let edges = GraphOntology::edge_concepts();
        assert!(edges.contains(&EdgeConcept::PrescribedFor), "PrescribedFor missing from edge_concepts()");
        assert!(edges.contains(&EdgeConcept::OccupiesPalace), "OccupiesPalace missing from edge_concepts()");
        assert!(edges.contains(&EdgeConcept::CarriesElement), "CarriesElement missing from edge_concepts()");
        // Label round-trip sanity:
        assert_eq!(NodeConcept::FlyingStar.label().as_str(), "flying_star");
        assert_eq!(EdgeConcept::OccupiesPalace.label().as_str(), "occupies_palace");
    }
    ```
    Ensure `NodeConcept`/`EdgeConcept` derive `PartialEq` so `.contains` compiles — they almost certainly already do (used in matches/slices); if not, add `PartialEq` to their derive lists.
  </action>
  <verify>
    <automated>cargo test -p amlich-core v15_concepts_present_in_ontology_slices 2>&1 | tail -8</automated>
  </verify>
  <done>The completeness test passes; all five new concepts are enumerable via GraphOntology and label round-trips are correct.</done>
</task>

</tasks>

<verification>
- `cargo build -p amlich-core` compiles (exhaustive matches satisfied).
- `cargo test -p amlich-core v15_concepts_present_in_ontology_slices` passes.
- `cargo test -p amlich-core 2>&1 | tail -5` — no regression in the semantic_graph test suite.
- `grep -c "FlyingStar\|Ritual\|PrescribedFor\|OccupiesPalace\|CarriesElement" crates/amlich-core/src/semantic_graph/ontology.rs` shows entries in all six logical locations.
</verification>

<success_criteria>
- Two NodeConcept + three EdgeConcept variants added across all six ontology locations.
- ConceptLabel::as_str returns the documented snake_case strings.
- GraphOntology static slices include all five new concepts, verified by an automated completeness test.
</success_criteria>

<output>
After completion, create `.planning/phases/15-semantic-graph-wiring-dto-integration-e2e-validation/15-02-SUMMARY.md`
</output>
