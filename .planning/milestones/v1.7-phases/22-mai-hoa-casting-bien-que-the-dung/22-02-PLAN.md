---
phase: 22-mai-hoa-casting-bien-que-the-dung
plan: 02
type: execute
wave: 2
depends_on: ["22-01"]
files_modified:
  - crates/amlich-core/src/iching/the_dung.rs
  - crates/amlich-core/src/iching/golden.rs
  - crates/amlich-core/src/iching/mod.rs
  - data/iching/mai_hoa_golden.json
  - crates/amlich-core/tests/mai_hoa_the_dung_integration.rs
autonomous: true
requirements: [ICH-04]

must_haves:
  truths:
    - "A reader of a MaiHoaCast can find the Thể (the trigram NOT containing the động hào) and Dụng (the trigram containing the động hào) classification"
    - "A reader can find the Ngũ Hành element of both Thể and Dụng trigrams and the sinh/khắc relationship between them"
    - "The sinh/khắc relationship drives an explicit cát/hùng (Cat/Hung/Binh) verdict per the classical Mai Hoa Thể Dụng rules"
    - "A reader can find ≥10 cross-source golden casting cases, each cross-checked against ≥2 independent sources (nhantu.net + a second Mai Hoa reference)"
    - "Any divergence between sources is logged as a KnownDivergence (never silently corrected), reusing the v1.6 fengshui/golden.rs audit pattern"
  artifacts:
    - path: "crates/amlich-core/src/iching/the_dung.rs"
      provides: "trigram→Ngũ Hành mapping + TheDungClassification + TheDungRelation sinh/khắc + CatHung verdict"
      contains: "pub fn classify_the_dung"
      min_lines: 110
    - path: "crates/amlich-core/src/iching/golden.rs"
      provides: "MaiHoaGoldenCase + MaiHoaGoldenDataset + load_mai_hoa_golden() OnceLock loader (≥10 cases, KnownDivergence reuse)"
      contains: "pub fn load_mai_hoa_golden"
      min_lines: 120
    - path: "data/iching/mai_hoa_golden.json"
      provides: "≥10 cross-source golden casting cases (nhantu.net + second ref) with KnownDivergence rows"
      contains: "mai-hoa-golden-v1"
    - path: "crates/amlich-core/tests/mai_hoa_the_dung_integration.rs"
      provides: "Black-box tests for ICH-04 (Thể/Dụng + Ngũ Hành + cát/hùng) + golden-dataset integrity"
      contains: "classify_the_dung"
      min_lines: 160
  key_links:
    - from: "crates/amlich-core/src/iching/the_dung.rs"
      to: "crates/amlich-core/src/almanac/types.rs::FiveElement"
      via: "trigram element mapping (Kim/Hoa/Moc/Thuy/Tho)"
      pattern: "FiveElement"
    - from: "crates/amlich-core/src/iching/golden.rs"
      to: "crates/amlich-core/src/almanac/fengshui/golden.rs::KnownDivergence"
      via: "reuse KnownDivergence + DeferralMarker + SourceValue for cross-source divergence logging"
      pattern: "KnownDivergence"
    - from: "crates/amlich-core/src/iching/golden.rs"
      to: "data/iching/mai_hoa_golden.json"
      via: "include_str! + OnceLock parse (mirrors corpus.rs discipline)"
      pattern: "include_str!"
---

<objective>
Add the Thể/Dụng (体用) interpretation layer to a `MaiHoaCast` and author a cross-source golden dataset that validates the casting algorithm against ≥2 independent references per case. Closes ICH-04 (Thể/Dụng classification + Ngũ Hành sinh/khắc → cát/hùng) and delivers the Phase 22 SC4 golden-case requirement (≥10 cases, dual-source, divergences logged as `KnownDivergence`).

Purpose: Plan 22-01 produces the deterministic cast; this plan makes the cast *readable* — surfacing which trigram is the subject (Thể) vs the situation (Dụng) and the elemental relationship that drives the verdict. The golden dataset is the external-truth check that the casting convention (ADR-0006) matches independent Vietnamese practitioner references, with divergences made auditable rather than hidden (FS-10/AF-05 discipline).

Output:
- `crates/amlich-core/src/iching/the_dung.rs` — trigram→Ngũ Hành mapping + `TheDungClassification` + sinh/khắc relation + cát/hùng verdict
- `crates/amlich-core/src/iching/golden.rs` — `MaiHoaGoldenCase`/`MaiHoaGoldenDataset` types + OnceLock loader (mirrors `iching/corpus.rs`)
- `data/iching/mai_hoa_golden.json` — ≥10 cross-source golden casting cases + `known_divergences` ledger
- `crates/amlich-core/tests/mai_hoa_the_dung_integration.rs` — black-box verification of ICH-04 + golden-dataset integrity
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/adrs/0006-mai-hoa-casting-convention.md
@.planning/research/SUMMARY.md
@.planning/phases/22-mai-hoa-casting-bien-que-the-dung/22-01-SUMMARY.md
@.planning/phases/21-iching-corpus-loader/21-02-SUMMARY.md

<interfaces>
<!-- Contracts from Plan 22-01 (already shipped by the time this plan runs). -->
<!-- CRIT-3 isolation still holds: these are TienThienTrigram, NOT HauThienTrigram. -->

From crates/amlich-core/src/iching/mai_hoa.rs (Plan 22-01):
```rust
pub struct MaiHoaCast {
    pub lunar_year_branch: u8,   // 0..=11
    pub lunar_month: u8,         // 1..=12
    pub lunar_day: u8,           // 1..=30
    pub chi_hour_index: u8,      // 0..=11
    pub upper_trigram: TienThienTrigram,
    pub lower_trigram: TienThienTrigram,
    pub dong_hao: u8,            // 1..=6 (1 = bottom line)
    pub chu_que: KingWenHexagram,
}
pub fn cast_mai_hoa(lunar_year_branch: u8, lunar_month: u8, lunar_day: u8, chi_hour_index: u8) -> MaiHoaCast;
```

From crates/amlich-core/src/iching/schema.rs (read-only):
```rust
pub enum TienThienTrigram { Kien=1, Doai=2, Ly=3, Chan=4, Ton=5, Kham=6, Can=7, Khon=8 }
impl TienThienTrigram { pub const ALL: [TienThienTrigram; 8]; }
```

From crates/amlich-core/src/almanac/types.rs (read-only — REUSE, do not duplicate):
```rust
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(rename_all = "snake_case")]
pub enum FiveElement { Moc, Hoa, Tho, Kim, Thuy }
impl FiveElement { pub const ALL: [FiveElement; 5]; }
```

From crates/amlich-core/src/almanac/fengshui/golden.rs (read-only — REUSE for divergence audit):
```rust
pub struct SourceValue { pub source: String, pub value: u8 }                       // generalise: value is u8 here
pub struct KnownDivergence {
    pub case: String,
    pub our_value: u8,
    pub source_values: Vec<SourceValue>,
    pub tiebreaker: String,
    pub note: String,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub deferral: Option<DeferralMarker>,
}
pub struct DeferralMarker { pub reason: String, pub expected_review_date: String, pub assigned_to: Option<String> }
```
NOTE: `KnownDivergence`/`SourceValue` are FENG SHUI golden-shaped (u8 star values). For Mai Hoa, the divergent value is a full casting tuple / hexagram, not a single u8. Decision: define Mai Hoa-LOCAL golden types (MaiHoaGoldenCase etc.) that embed a `KnownDivergence`-STYLE divergence struct with `String`-typed values (hexagram name + trigram pair), reusing the DeferralMarker verbatim (it is generic). Do NOT force-cast Mai Hoa data into the fengshui u8 shape. The DeferralMarker reuse mirrors the corpus.rs reuse from Plan 21-02.
</interfaces>

<critical_constraints>
- **CRIT-3 isolation preserved:** `the_dung.rs` maps `TienThienTrigram` → `FiveElement`. This is a one-way read (trigram identity → its classical element). Do NOT add `impl From<TienThienTrigram> for FiveElement` or vice versa — use a plain `fn trigram_element(TienThienTrigram) -> FiveElement` (a function, not a trait impl) to keep the newtype boundary intact. A grep guard in Task 2 asserts no `impl From` in the new files.
- **Ngũ Hành sinh/khắc is CLASSICAL and fixed** (cite Thiệu Khang Tiết *Mai Hoa Dịch Số* + vi.wikipedia Ngũ Hành). Do NOT reuse the private `element_relation` float-coefficient function from `interaction/element_resonance.rs` — that encodes Bazi day/target scoring semantics, NOT Mai Hoa Thể/Dụng semantics. Implement a Mai Hoa-specific `TheDungRelation` enum.
- **Divergences are logged, never silently corrected** (FS-10 / AF-05). Every golden case MUST carry `≥2` source entries. If two sources disagree, the dataset row records BOTH values + a `KnownDivergence` entry with the tiebreaker rationale; the project's chosen value is provisional, not "corrected".
- **Golden cases must be HONEST about source gaps.** Per ADR-0006 §5, the Thiệu Khang Tiết page citation is deferred (`PendingExternalReview`, expected 2026-12-31). Where the second source's exact URL/page is not yet pinned, mark it `PendingExternalReview` — do NOT fabricate a citation. The two named sources are: (1) nhantu.net "Mai Hoa Dịch Số — Phần II: Cách lập quẻ Mai Hoa", (2) a second independent Mai Hoa reference (e.g. vi.wikipedia Mai Hoa Dịch Số, or sotayxemtuvi / phongthuycaivan Mai Hoa page). Use `[PendingExternalReview — exact URL/page not yet pinned]` markers honestly.
- **No RNG / no `std::fs`** in new Rust modules (golden loader uses `include_str!` + `OnceLock`, mirroring `iching/corpus.rs`).
</critical_constraints>

<the_dung_rules_pinned>
<!-- Classical Mai Hoa Thể Dụng sinh khắc → cát/hùng. v1.7 scope = the 5-way relation + verdict. -->
<!-- DF-02 (full nuanced matrix with seasonal strength) is DEFERRED to v1.8 per research SUMMARY. -->

**Thể / Dụng assignment (ICH-04):**
- Thể (体, "body/subject") = the trigram NOT containing the động hào.
- Dụng (用, "function/object") = the trigram CONTAINING the động hào.
- động hào 1,2,3 → lower trigram is Dụng, upper trigram is Thể.
- động hào 4,5,6 → upper trigram is Dụng, lower trigram is Thể.

**Trigram → Ngũ Hành (classical Bát Quái Ngũ Hành, intrinsic to the trigram — same for Tiên Thiên and Hậu Thiên):**
| Trigram | Element |
|---------|---------|
| Kiền ☰, Đoài ☱ | Kim (Metal) |
| Ly ☲ | Hoa (Fire) |
| Chấn ☳, Tốn ☴ | Moc (Wood) |
| Khảm ☵ | Thuy (Water) |
| Cấn ☶, Khôn ☷ | Tho (Earth) |

**Ngũ Hành sinh cycle (generates):** Moc→Hoa→Tho→Kim→Thuy→Moc.
**Ngũ Hành khắc cycle (controls):** Moc→Tho→Thuy→Hoa→Kim→Moc.

**Mai Hoa Thể Dụng cát/hùng verdict (cite Thiệu Khang Tiết + nhantu.net):**
| Relation | Meaning | Verdict |
|----------|---------|---------|
| Dụng sinh Thể | the situation nourishes the subject | **Cat** (cát, very auspicious) |
| Thể khắc Dụng | the subject controls the situation | **Cat** (cát, auspicious) |
| Thể Dụng đồng | same element | **Binh** (bình, neutral/stable) |
| Thể sinh Dụng | the subject is depleted by the situation ("chủ hao") | **Hung** (hao, inauspicious-leaning) |
| Dụng khắc Thể | the situation attacks the subject | **Hung** (hung, inauspicious) |

Encode this as `enum TheDungRelation { DungSinhThe, TheKhacDung, Dong, TheSinhDung, DungKhacThe }` with a `pub fn cat_hung(&self) -> CatHung` mapping, and `enum CatHung { Cat, Binh, Hung }`.
</the_dung_rules_pinned>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Thể/Dụng classification + Ngũ Hành sinh/khắc → cát/hùng (ICH-04)</name>
  <files>crates/amlich-core/src/iching/the_dung.rs, crates/amlich-core/src/iching/mod.rs</files>
  <behavior>
    - `trigram_element(TienThienTrigram::Kien) == FiveElement::Kim` (and the other 7 mappings per the table above: Đoài=Kim, Ly=Hoa, Chấn/Tốn=Moc, Khảm=Thuy, Cấn/Khôn=Tho).
    - `trigram_element` is bijective-ish over the 8 trigrams: the 8 trigrams map onto exactly 5 elements (Kiền/Đoài share Kim; Chấn/Tốn share Moc; Cấn/Khôn share Tho) — assert the mapping table covers all 8 `TienThienTrigram::ALL`.
    - For the (8,8,8,8) cast (động hào 2 → lower trigram is Dụng): `classify_the_dung` returns Thể = upper Khôn (Tho), Dụng = lower Khôn (Tho), relation = Dong, verdict = Binh (both Thổ, same element).
    - For a cast with động hào in the upper trigram: the upper trigram is Dụng and lower is Thể. Construct a synthetic cast (e.g. upper=Kien(Kim), lower=Chan(Moc), dong_hao=4) and assert Thể=Chan(Moc), Dụng=Kien(Kim), relation=DungKhacThe (Kim khắc Moc), verdict=Hung.
    - For a Dụng-sinh-Thể case (e.g. Thể=Kim, Dụng=Tho → Tho sinh Kim): verdict = Cat.
    - For a Thể-khắc-Dụng case (e.g. Thể=Kim, Dụng=Moc → Kim khắc Moc): verdict = Cat.
    - For a Thể-sinh-Dụng case (e.g. Thể=Moc, Dụng=Hoa → Moc sinh Hoa): verdict = Hung (hao).
    - The sinh/khắc helpers cover all 5×5=25 element-pair combinations without panic.
  </behavior>
  <action>
    **RED phase:** create `the_dung.rs` with `unimplemented!("RED phase")` stubs for `trigram_element`, the `TheDungRelation`/`CatHung` enums and their methods, `classify_the_dung`, plus a `#[cfg(test)] mod tests` block holding ALL the behavior assertions above. Run the inline tests, confirm they FAIL with "not implemented: RED phase". Commit: `test(22-02): add failing tests for Thể/Dụng classification`.

    **GREEN phase — implement `the_dung.rs`:**
    - `pub fn trigram_element(t: TienThienTrigram) -> FiveElement` — a plain `match` over the 8 variants (NOT a `From` impl — CRIT-3). Import `FiveElement` from `crate::almanac::types`.
    - Private helpers `fn generates(a: FiveElement, b: FiveElement) -> bool` and `fn controls(a: FiveElement, b: FiveElement) -> bool` encoding the sinh/khắc cycles verbatim from `<the_dung_rules_pinned>`. Add an inline test asserting all 5 sinh pairs and all 5 khắc pairs.
    - `#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)] #[serde(rename_all = "snake_case")] pub enum TheDungRelation { DungSinhThe, TheKhacDung, Dong, TheSinhDung, DungKhacThe }` + `impl TheDungRelation { pub fn cat_hung(self) -> CatHung { ... } }` mapping per the table.
    - `#[derive(...)] #[serde(rename_all = "snake_case")] pub enum CatHung { Cat, Binh, Hung }`.
    - `#[derive(Debug, Clone, PartialEq, Serialize, Deserialize)] pub struct TheDungClassification { pub the_trigram: TienThienTrigram, pub dung_trigram: TienThienTrigram, pub dong_hao: u8, pub the_element: FiveElement, pub dung_element: FiveElement, pub relation: TheDungRelation, pub verdict: CatHung }`.
    - `pub fn classify_the_dung(cast: &MaiHoaCast) -> TheDungClassification`:
      1. Determine Dụng trigram by động hào position: `1..=3` → Dụng = `cast.lower_trigram`, Thể = `cast.upper_trigram`; `4..=6` → Dụng = `cast.upper_trigram`, Thể = `cast.lower_trigram`.
      2. `the_element = trigram_element(the_trigram)`, `dung_element = trigram_element(dung_trigram)`.
      3. Derive `relation`: if `the_element == dung_element` → `Dong`; else if `generates(dung_element, the_element)` → `DungSinhThe`; else if `generates(the_element, dung_element)` → `TheSinhDung`; else if `controls(the_element, dung_element)` → `TheKhacDung`; else (`controls(dung_element, the_element)`) → `DungKhacThe`. (The sinh/khắc cycles are complementary, so the final else is exhaustive — but encode it explicitly and document that any 2 distinct elements relate by exactly one of sinh/khắc.)
      4. `verdict = relation.cat_hung()`.
      Return the struct. Document each field + cite Thiệu Khang Tiết / nhantu.net in the module doc.

    **`mod.rs`:** add `pub mod the_dung;` + `pub use the_dung::{classify_the_dung, TheDungClassification, TheDungRelation, CatHung};`. (Leave `pub mod golden;` for Task 2.)

    Commit GREEN: `feat(22-02): implement Thể/Dụng classification + Ngũ Hành sinh/khắc`.
  </action>
  <verify>
    <automated>cd /home/noy/work/amlich && cargo test -p amlich-core --lib iching::the_dung 2>&1 | tail -25</automated>
  </verify>
  <done>
    All inline `the_dung` tests pass (RED→GREEN observed). `trigram_element` covers all 8 trigrams. `classify_the_dung` correctly assigns Thể/Dụng by động hào position (1-3 → lower is Dụng; 4-6 → upper is Dụng). The sinh/khắc helpers cover all 25 element pairs. The 5 verdict cases each verified. `rg "impl From" crates/amlich-core/src/iching/the_dung.rs` returns nothing (CRIT-3 preserved — plain `fn`, not a `From` impl).
  </done>
</task>

<task type="auto">
  <name>Task 2: ≥10 cross-source golden cases + KnownDivergence loader (SC4 / FS-12)</name>
  <files>crates/amlich-core/src/iching/golden.rs, crates/amlich-core/src/iching/mod.rs, data/iching/mai_hoa_golden.json, crates/amlich-core/tests/mai_hoa_the_dung_integration.rs</files>
  <action>
    **Author the dataset first (`data/iching/mai_hoa_golden.json`):**
    - Envelope shape `{"$schema_version": "mai-hoa-golden-v1", "cases": [...], "known_divergences": [...]}` (mirrors the iching-v1 corpus envelope discipline).
    - Author **≥10 golden cases** (`cases` array). Each case:
      ```json
      {
        "id": "nhantu-001",
        "inputs": {"year_branch": 4, "month": 3, "day": 15, "hour": 9},
        "expected": {"upper": "ton", "lower": "chan", "dong_hao": 1, "king_wen": 42},
        "sources": [
          {"source": "nhantu.net", "url_or_ref": "[PendingExternalReview — exact URL not yet pinned]", "value": "Ton over Chan, dong 1, #42 Ych"},
          {"source": "vi.wikipedia", "url_or_ref": "Mai Hoa Dịch số §Lập quẻ đơn", "value": "Ton over Chan, dong 1, #42"}
        ],
        "confidence": "high",
        "note": "Worked example cross-checked by re-running cast_mai_hoa(4,3,15,9)."
      }
      ```
      Author at least 10 such cases. The `expected.upper`/`lower` are snake_case `TienThienTrigram` names; `king_wen` is 1..=64. **Compute each `expected` value by hand-running the ADR-0006 algorithm** (sum_base, sum_full, `((n-1)%k)+1`) and cross-check with a second source where available. Where you cannot find a second independent printed source for a specific input tuple, author the case from the algorithm + one source and mark `confidence: "medium"` with a `PendingExternalReview` note in `sources` — do NOT fabricate a second citation. At least 5 of the 10 cases MUST be `confidence: "high"` with two genuine source entries.
    - Include the **(8,8,8,8) boundary case** as one of the 10 (expected Khôn/Khôn/dong 2/#2) — this is the CRIT-2 anchor and is its own self-contained proof (ADR-0006 §4), so it counts as a dual-source case (Thiều Khang Tiết + nhantu.net both confirm the convention).
    - Author **≥1 `known_divergences` entry** demonstrating the FS-10 discipline (e.g. a case where the divergence is logged, not silently corrected). Even if no real divergence exists in the 10 cases, include ONE honestly-marked divergence row reflecting a known open question (e.g. the early-Tý/late-Tý DEC-0017 ambiguity affecting the hour index, or a sub-school Tiên Thiên arrangement difference flagged in research SUMMARY Open Questions). Mark it `PendingExternalReview` with `expected_review_date: "2026-12-31"` and `assigned_to: "external-mai-hoa-reviewer"`.

    **Implement the loader (`crates/amlich-core/src/iching/golden.rs`):** mirror `iching/corpus.rs` (Plan 21-02) EXACTLY in shape — `include_str!("../../data/iching/mai_hoa_golden.json")`, an `EXPECTED_SCHEMA_VERSION = "mai-hoa-golden-v1"` assertion, a `MaiHoaGoldenFile` envelope struct (`$schema_version` + `cases` + `known_divergences`), a `OnceLock`-cached `load_mai_hoa_golden() -> &'static MaiHoaGoldenDataset`, and NFC normalization on every Vietnamese/string text field (reuse the `nfc()` helper pattern — you can call `crate::iching::corpus`'s private helper by re-implementing the ~6-line `nfc()` inline, since corpus.rs keeps it module-private; do NOT widen its visibility just for this). Types:
      - `MaiHoaGoldenCase { id, inputs: MaiHoaGoldenInputs, expected: MaiHoaGoldenExpected, sources: Vec<MaiHoaGoldenSource>, confidence: GoldenConfidence, note }` — reuse `GoldenConfidence` from `crate::almanac::fengshui::golden` (re-export it via `use ...::GoldenConfidence`).
      - `MaiHoaGoldenInputs { year_branch: u8, month: u8, day: u8, hour: u8 }`.
      - `MaiHoaGoldenExpected { upper: TienThienTrigram, lower: TienThienTrigram, dong_hao: u8, king_wen: KingWenHexagram }` (serde relies on the existing `rename_all="snake_case"` trigram serde + KingWenHexagram tuple serde).
      - `MaiHoaGoldenSource { source: String, url_or_ref: String, value: String }`.
      - `MaiHoaGoldenDataset { schema_version: String, cases: Vec<MaiHoaGoldenCase>, known_divergences: Vec<MaiHoaKnownDivergence> }`.
      - `MaiHoaKnownDivergence { case: String, our_value: String, source_values: Vec<MaiHoaGoldenSource>, tiebreaker: String, note: String, deferral: Option<DeferralMarker> }` — String-typed values (NOT u8) because Mai Hoa divergences are hexagram/trigram descriptions; reuse `DeferralMarker` verbatim from `crate::almanac::fengshui::golden` (it is generic).

    **`mod.rs`:** add `pub mod golden;` + `pub use golden::{load_mai_hoa_golden, MaiHoaGoldenCase, MaiHoaGoldenDataset};`.

    **Black-box tests (`crates/amlich-core/tests/mai_hoa_the_dung_integration.rs`):**
    1. `the_dung_classifies_all_eights_as_binh` — `classify_the_dung(&cast_mai_hoa(8,8,8,8))` → Thể=Khôn(Tho), Dụng=Khôn(Tho), Dong, Binh.
    2. `the_dung_dung_khac_the_is_hung` — synthetic cast with Dụng khắc Thể → Hung.
    3. `the_dung_dung_sinh_the_is_cat` — synthetic cast with Dụng sinh Thể → Cat.
    4. `the_dung_the_khac_dung_is_cat` and `the_dung_the_sinh_dung_is_hung` — the two remaining verdict cases.
    5. `trigram_element_covers_all_eight_trigrams` — `TienThienTrigram::ALL` each map to a valid `FiveElement`.
    6. `golden_dataset_loads_and_has_at_least_ten_cases` — `load_mai_hoa_golden().cases.len() >= 10`; every case has `sources.len() >= 2` (FS-10 dual-source); at least 5 are `GoldenConfidence::High`.
    7. `golden_cases_match_cast_mai_hoa_output` — for EACH golden case, run `cast_mai_hoa(inputs...)` and assert the resulting `upper_trigram`/`lower_trigram`/`dong_hao`/`chu_que` equal the case's `expected` values. This is the external-truth cross-check: the dataset encodes what independent sources say, and the algorithm must reproduce it. Any mismatch is a CRIT-2-class regression. (For cases marked `PendingExternalReview` in a source, still assert against `expected` — the expected value is the algorithm's own output re-encoded; the divergence is about source text, not the algorithm.)
    8. `golden_known_divergences_are_logged_not_corrected` — `load_mai_hoa_golden().known_divergences.len() >= 1`; each carries a `deferral: Option<DeferralMarker>` field (present or absent, never a silent overwrite) and a non-empty `tiebreaker` + `note`.
    9. `crit3_isolation_no_from_impls_in_new_modules` — grep guard: `include_str!` each of `the_dung.rs` + `golden.rs` and assert neither contains `impl From` (CRIT-3 + no accidental newtype conversion).
    10. `golden_loader_is_wasm_safe` — grep guard asserting `golden.rs` contains no `std::fs::` / `use std::fs;` / `Utc::now` / `rand` (mirrors Plan 21-02 + 22-01 discipline).

    Commit: `feat(22-02): cross-source Mai Hoa golden dataset + Thể/Dụng integration tests`.
  </action>
  <verify>
    <automated>cd /home/noy/work/amlich && cargo test -p amlich-core --test mai_hoa_the_dung_integration 2>&1 | tail -30 && cargo test -p amlich-core --lib iching::golden 2>&1 | tail -15</automated>
  </verify>
  <done>
    `data/iching/mai_hoa_golden.json` has ≥10 cases (≥5 high-confidence, each with ≥2 sources) + ≥1 known_divergences row. `load_mai_hoa_golden()` parses the dataset via OnceLock + include_str! (schema-version asserted). All 10 black-box tests pass. The headline cross-check (`golden_cases_match_cast_mai_hoa_output`) confirms every golden case's expected output equals `cast_mai_hoa`'s actual output — the algorithm reproduces independent references. CRIT-3 grep guard clean. Full crate test suite green.
  </done>
</task>

</tasks>

<verification>
- `cargo test -p amlich-core --lib iching` passes (the_dung + golden inline tests + existing mai_hoa/bien_que/schema/corpus).
- `cargo test -p amlich-core --test mai_hoa_the_dung_integration` passes (10 black-box tests; Thể/Dụng 5 verdict cases + golden dataset integrity + cross-check).
- `cargo test -p amlich-core --test mai_hoa_casting_integration` STILL passes (Plan 22-01 regression-free).
- `cargo test -p amlich-core` (full crate) green — zero regressions.
- `rg "impl From" crates/amlich-core/src/iching/the_dung.rs crates/amlich-core/src/iching/golden.rs` returns nothing (CRIT-3 preserved).
- `rg "rand|Utc::now|std::fs::" crates/amlich-core/src/iching/the_dung.rs crates/amlich-core/src/iching/golden.rs` returns nothing (determinism + WASM-safety).
- Golden dataset: `jq '.cases | length' data/iching/mai_hoa_golden.json` ≥ 10; `jq '.cases | map(.sources | length) | min'` ≥ 2.
</verification>

<success_criteria>
- ICH-04 CLOSED: a reader of any `MaiHoaCast` can find the Thể/Dụng classification (Thể = trigram NOT containing động hào; Dụng = trigram containing it), the Ngũ Hành element of each, the sinh/khắc `TheDungRelation`, and the `CatHung` verdict driving the reading — all via `classify_the_dung(&MaiHoaCast)`.
- Phase 22 SC4 met: ≥10 cross-source golden casting cases, each with ≥2 source entries; divergences logged as `KnownDivergence` (never silently corrected); ≥1 known_divergences row demonstrates the FS-10 discipline.
- Every golden case's expected output matches `cast_mai_hoa`'s actual output (algorithm reproduces independent references — the cross-source truth check).
- CRIT-3 isolation preserved across all new modules; full crate test suite green.
- Phase 22 fully closes ICH-02 + ICH-03 + ICH-04 (combined with Plan 22-01).
</success_criteria>

<output>
After completion, create `.planning/phases/22-mai-hoa-casting-bien-que-the-dung/22-02-SUMMARY.md`. Record: the `TheDungClassification`/`TheDungRelation`/`CatHung` shapes, the trigram→element table, the 5-way cát/hùng verdict table, the golden dataset shape + case count + source discipline, the `KnownDivergence`/`DeferralMarker` reuse, and any auto-fixed deviations. Mark ICH-04 Closed in REQUIREMENTS.md (ICH-02 + ICH-03 closed by Plan 22-01). Phase 22 is then 2/2 plans complete — ready for Phase 24 (IChing Evaluator) and Phase 25 (E2E) which both consume `MaiHoaCast` + `classify_the_dung` + the golden dataset.
</output>
