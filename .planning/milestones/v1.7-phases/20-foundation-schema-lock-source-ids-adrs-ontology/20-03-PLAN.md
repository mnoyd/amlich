---
phase: 20-foundation-schema-lock-source-ids-adrs-ontology
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/semantic_graph/ontology.rs
  - crates/amlich-core/src/reasoning/types.rs
  - crates/amlich-core/src/semantic_graph/views/helpers.rs
  - crates/amlich-core/src/semantic_graph/views/visualization.rs
autonomous: true
requirements: [FND-12]

must_haves:
  truths:
    - "A reader can find NodeConcept::Hexagram added across all 6 ontology slice locations (enum, label() match, ConceptLabel enum, as_str() match, node_concepts() slice) with compiler-enforced exhaustiveness and NO #[non_exhaustive] escape"
    - "A reader can find EdgeConcept::LocatedAt + EdgeConcept::Transforms added across their 6 edge-slice locations"
    - "A reader can find ReasoningEvidenceSourceFamily::IChing + ActionId::IChing variants in reasoning/types.rs"
    - "A reader can find a v1.7 ontology test asserting Hexagram/LocatedAt/Transforms presence + label round-trips"
    - "The whole crate compiles — all exhaustive match arms in semantic_graph/views/ updated for the new NodeConcept variant"
  artifacts:
    - path: "crates/amlich-core/src/semantic_graph/ontology.rs"
      provides: "6-slice extension for Hexagram node + LocatedAt/Transforms edges + v1.7 test"
      contains: "Hexagram"
    - path: "crates/amlich-core/src/reasoning/types.rs"
      provides: "IChing variants on ActionId + ReasoningEvidenceSourceFamily"
      contains: "IChing"
    - path: "crates/amlich-core/src/semantic_graph/views/helpers.rs"
      provides: "cluster_for_node_id arm for Hexagram"
      contains: "Hexagram"
    - path: "crates/amlich-core/src/semantic_graph/views/visualization.rs"
      provides: "shape_hint_for_node arm for Hexagram"
      contains: "Hexagram"
  key_links:
    - from: "crates/amlich-core/src/semantic_graph/ontology.rs"
      to: "crates/amlich-core/src/semantic_graph/views/helpers.rs"
      via: "cluster_for_node_id exhaustive match must gain a Hexagram arm"
      pattern: "NodeConcept::Hexagram"
    - from: "crates/amlich-core/src/semantic_graph/ontology.rs"
      to: "crates/amlich-core/src/semantic_graph/views/visualization.rs"
      via: "shape_hint_for_node exhaustive match must gain a Hexagram arm"
      pattern: "NodeConcept::Hexagram"
---

<objective>
Extend the 6-slice semantic-graph ontology with `NodeConcept::Hexagram` (node) + `EdgeConcept::LocatedAt` + `EdgeConcept::Transforms` (edges), plus `ReasoningEvidenceSourceFamily::IChing` + `ActionId::IChing` enum variants — all compiler-enforced exhaustive with no `#[non_exhaustive]` escape.

Purpose: The ontology extension reserves the semantic-graph vocabulary that Phase 24's `add_iching_facts()` builder will populate (Hexagram nodes wired via LocatedAt/Transforms edges). The `IChing` enum variants mark IChing as a distinct Tier-0 evidence family + action id. This is the last piece of the v1.7 type-level scaffolding that Phases 21-25 build upon.

Output: An extended ontology.rs (6 slices + test), two new reasoning enum variants, and updated exhaustive-match arms in the views layer.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-foundation-schema-lock-source-ids-adrs-ontology/20-CONTEXT.md
@.planning/phases/20-foundation-schema-lock-source-ids-adrs-ontology/20-RESEARCH.md

<interfaces>
<!-- The 6-slice ontology structure (verified line numbers). Each concept MUST appear in ALL slices. -->

From crates/amlich-core/src/semantic_graph/ontology.rs:
```
Slice 1: enum NodeConcept (lines 5-43)      — append Hexagram after Offering (line 42)
Slice 2: NodeConcept::label() match (47-85) — arm Self::Hexagram => ConceptLabel::Hexagram
Slice 3: enum ConceptLabel (161-228)        — append Hexagram + LocatedAt + Transforms
Slice 4: ConceptLabel::as_str() match (232-299) — 3 arms => "hexagram"/"located_at"/"transforms"
Slice 5: GraphOntology::node_concepts() (338-376) — NodeConcept::Hexagram entry
Slice 6 (edges): GraphOntology::edge_concepts() (380-409) — LocatedAt + Transforms entries

PLUS symmetric EdgeConcept slices (LocatedAt + Transforms are EDGES):
  - enum EdgeConcept (91-121): append LocatedAt + Transforms after RecommendsOffering (line 120)
  - EdgeConcept::label() match (124-156): 2 arms
```

Exhaustive match arms OUTSIDE ontology.rs (Phase 19 precedent — Offering forced updates here):
- crates/amlich-core/src/semantic_graph/views/helpers.rs:6 — `cluster_for_node_id(concept)` match ends with `NodeConcept::Ritual | NodeConcept::FlyingStar | NodeConcept::Offering => {...}` (line 45). Adding Hexagram forces a new arm.
- crates/amlich-core/src/semantic_graph/views/visualization.rs:77 — `shape_hint_for_node(concept)` match ends with `NodeConcept::Ritual | NodeConcept::FlyingStar | NodeConcept::Offering => {...}` (line 117). Adding Hexagram forces a new arm.

From crates/amlich-core/src/reasoning/types.rs:
```rust
// ActionId (lines 5-7) — currently 1 variant (InitiationOpening). Additive-safe: no exhaustive match exists (rg-confirmed).
pub enum ActionId { InitiationOpening, /* + IChing */ }

// ReasoningEvidenceSourceFamily (lines 134-142) — 7 variants. Additive-safe: no exhaustive match exists.
pub enum ReasoningEvidenceSourceFamily {
    Snapshot, Interaction, Bazi, Axis, AlmanacRule, Insight, Derived, /* + IChing */
}
```

Label convention (snake_case English, matching existing): Hexagram => "hexagram", LocatedAt => "located_at", Transforms => "transforms".
v1.7 test template: the v1.5 test at ontology.rs:309-321 + v1.6 test at ontology.rs:324-333 (inside `#[cfg(test)] mod tests`).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend 6 ontology slices for Hexagram node + LocatedAt/Transforms edges + v1.7 test + fix views exhaustive-match arms (FND-12)</name>
  <files>crates/amlich-core/src/semantic_graph/ontology.rs, crates/amlich-core/src/semantic_graph/views/helpers.rs, crates/amlich-core/src/semantic_graph/views/visualization.rs</files>
  <behavior>
    - Test (v1.7 ontology): `v17_concepts_present_in_ontology_slices` asserts node_concepts() contains Hexagram, edge_concepts() contains LocatedAt + Transforms, and label round-trips: NodeConcept::Hexagram.label().as_str() == "hexagram", EdgeConcept::LocatedAt.label().as_str() == "located_at", EdgeConcept::Transforms.label().as_str() == "transforms".
    - Compile guarantee: the crate compiles with NO #[non_exhaustive] escape — every exhaustive match arm (ontology.rs internal + views/helpers.rs + views/visualization.rs) gained the new variants.
  </behavior>
  <action>
In `crates/amlich-core/src/semantic_graph/ontology.rs`, extend ALL 6 slices + the symmetric EdgeConcept slices. Add in order:

1. `enum NodeConcept` (after `Offering,` line 42): append `Hexagram,`.
2. `NodeConcept::label()` match (after the `Self::Offering => ConceptLabel::Offering,` arm ~line 84): add `Self::Hexagram => ConceptLabel::Hexagram,`.
3. `enum EdgeConcept` (after `RecommendsOffering,` line 120): append `LocatedAt,` then `Transforms,`.
4. `EdgeConcept::label()` match (after `Self::RecommendsOffering => ...` ~line 154): add `Self::LocatedAt => ConceptLabel::LocatedAt,` + `Self::Transforms => ConceptLabel::Transforms,`.
5. `enum ConceptLabel` (after `RecommendsOffering,` ~line 227): append `Hexagram,`, `LocatedAt,`, `Transforms,`.
6. `ConceptLabel::as_str()` match: add `Self::Hexagram => "hexagram",` + `Self::LocatedAt => "located_at",` + `Self::Transforms => "transforms",`.
7. `GraphOntology::node_concepts()` slice (after `NodeConcept::Offering,` ~line 375): add `NodeConcept::Hexagram,`.
8. `GraphOntology::edge_concepts()` slice (after `EdgeConcept::RecommendsOffering,` ~line 409): add `EdgeConcept::LocatedAt,` + `EdgeConcept::Transforms,`.

Add a `v17_concepts_present_in_ontology_slices()` test inside the existing `#[cfg(test)] mod tests` (after the v1.6 test at ~line 333), mirroring the v1.5/v1.6 test templates — assert Hexagram/LocatedAt/Transforms presence in the slices + label round-trips.

Then FIX the two exhaustive-match sites the compiler will flag (Phase 19 precedent):
- `crates/amlich-core/src/semantic_graph/views/helpers.rs:6` — `cluster_for_node_id`'s match. Add `NodeConcept::Hexagram` to the existing `NodeConcept::Ritual | NodeConcept::FlyingStar | NodeConcept::Offering` arm (line ~45) — Hexagram is a corpus node in the same cluster family. Resulting arm: `NodeConcept::Ritual | NodeConcept::FlyingStar | NodeConcept::Offering | NodeConcept::Hexagram => {...}`.
- `crates/amlich-core/src/semantic_graph/views/visualization.rs:77` — `shape_hint_for_node`'s match. Add `NodeConcept::Hexagram` to the existing `NodeConcept::Ritual | NodeConcept::FlyingStar | NodeConcept::Offering` arm (line ~117) — same shape family.

Do NOT add `#[non_exhaustive]` anywhere (FND-12 locks compiler-enforced exhaustiveness). After editing ontology.rs, run `cargo build -p amlich-core` immediately — the compiler lists every remaining non-exhaustive match site; fix each arm.
  </action>
  <verify>
    <automated>cargo test -p amlich-core ontology --quiet && cargo build -p amlich-core --quiet</automated>
  </verify>
  <done>All 6 ontology slices + symmetric edge slices extended; v1.7 test passes; views/helpers.rs + views/visualization.rs arms updated; crate compiles with no #[non_exhaustive].</done>
</task>

<task type="auto">
  <name>Task 2: Add ReasoningEvidenceSourceFamily::IChing + ActionId::IChing variants (FND-12)</name>
  <files>crates/amlich-core/src/reasoning/types.rs</files>
  <action>
In `crates/amlich-core/src/reasoning/types.rs`:

1. `enum ActionId` (lines 5-7): append `IChing,` after `InitiationOpening,`. Additive-safe — research confirmed `rg` shows NO exhaustive match blocks on ActionId anywhere in src/ (it is only constructed, never matched). Zero call-site churn.

2. `enum ReasoningEvidenceSourceFamily` (lines 134-142): append `IChing,` after `Derived,`. This is a sibling variant (NOT a reuse of `AlmanacRule`) — IChing is a distinct Tier-0 evidence family per the roadmap. Additive-safe — research confirmed no exhaustive match blocks exist on this enum either.

Both enums already derive `Debug + Clone + Copy + PartialEq + Eq + Serialize + Deserialize` with `#[serde(rename_all = "snake_case")]`, so `IChing` serializes to `"i_ching"` automatically. No other changes needed.
  </action>
  <verify>
    <automated>cargo build -p amlich-core --quiet && rg -q "IChing" crates/amlich-core/src/reasoning/types.rs</automated>
  </verify>
  <done>ReasoningEvidenceSourceFamily::IChing + ActionId::IChing variants present; crate compiles (no exhaustive-match churn); serializes to "i_ching".</done>
</task>

</tasks>

<verification>
- `cargo test -p amlich-core ontology --quiet` — v1.7 ontology test passes (Hexagram/LocatedAt/Transforms present + labels round-trip).
- `cargo build -p amlich-core --quiet` — whole crate compiles with the new variants (views arms updated).
- `rg "#\[non_exhaustive\]" crates/amlich-core/src/semantic_graph/ontology.rs` returns NO matches (no escape used).
- `rg "IChing" crates/amlich-core/src/reasoning/types.rs` shows 2 variant additions.
- `cargo test -p amlich-core --quiet` — full suite green (no regressions from the enum extensions).
</verification>

<success_criteria>
- The 6-slice ontology extended with Hexagram (node) + LocatedAt/Transforms (edges) — compiler-enforced exhaustive, no escape.
- The v1.7 ontology test asserts presence + label round-trips.
- IChing variants added to both reasoning enums as distinct Tier-0 family members.
- The views layer compiles with the new NodeConcept variant.
</success_criteria>

<output>
After completion, create `.planning/phases/20-foundation-schema-lock-source-ids-adrs-ontology/20-03-SUMMARY.md`
</output>
