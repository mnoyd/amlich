---
phase: 23-th-i-tu-tam-s-t-phi-tinh-cross-link
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/reasoning/direction_composite.rs
  - crates/amlich-core/src/reasoning/mod.rs
  - crates/amlich-core/src/lib.rs
autonomous: true
requirements:
  - XLK-03
must_haves:
  truths:
    - "The reasoning layer has an explicit, serializable contract for rich eight-direction cross-link cells, agreement semantics, both entry-point variants, and the immutable summary projection."
    - "The cross-link DTO preserves the required `birth_chi_index: usize` and documents `usize::MAX` as the date-only sentinel without creating a new directional type."
    - "`DaySnapshot.direction_cross_link` exists as an additive optional field, defaults to None, and is absent from ordinary snapshot JSON; no calculation path auto-populates it."
    - "The reasoning layer can obtain annual palace safety-hint text from snapshot DTO data without importing the lower-level palace-layout module at the future cross-link implementation site."
  artifacts:
    - path: "crates/amlich-core/src/reasoning/direction_composite.rs"
      provides: "DirectionCrossLink, DirectionCrossLinkSummary, DirectionCell, DirectionalTaboo, HuyenKhongCell, Agreement, and locked direction-order contracts"
      contains: "pub struct DirectionCrossLinkSummary"
    - path: "crates/amlich-core/src/lib.rs"
      provides: "Additive FlyingStarsSummary safety-hint transport and DaySnapshot.direction_cross_link initialized to None"
      contains: "direction_cross_link"
    - path: "crates/amlich-core/src/reasoning/mod.rs"
      provides: "Selective public re-exports for Phase 23 cross-link contracts"
      contains: "direction_composite"
  key_links:
    - from: "crates/amlich-core/src/reasoning/direction_composite.rs::DIRECTION_ORDER"
      to: "crates/amlich-core/src/interaction/direction_merge.rs::ALL_DIRECTIONS"
      via: "same North, Northeast, East, Southeast, South, Southwest, West, Northwest order"
      pattern: "DIRECTION_ORDER"
    - from: "crates/amlich-core/src/lib.rs::FlyingStarsSummary::palace_safety_hints"
      to: "crates/amlich-core/src/lib.rs::calculate_day_snapshot_internal"
      via: "annual palace safety text is precomputed into the DTO; the cross-link later consumes only snapshot.flying_stars"
      pattern: "palace_safety_hints"
    - from: "crates/amlich-core/src/lib.rs::DaySnapshot::direction_cross_link"
      to: "crates/amlich-core/src/reasoning/direction_composite.rs::DirectionCrossLinkSummary"
      via: "additive Option<T> field with serde default/skip attributes"
      pattern: "DirectionCrossLinkSummary"
---

<objective>
Lay down the public cross-link data contracts and the additive snapshot transport that the implementation plan consumes.

Purpose: Keep interfaces explicit before the multi-tradition assembly is implemented, preserve Phase 24's downstream type identity, and solve the CRIT-3-safe safety-hint transport at the DTO boundary. This plan deliberately does not build the cross-link or auto-populate the snapshot field.
Output: Serializable reasoning contracts, selective re-exports, annual palace safety-hint DTO enrichment, and a default-None `DaySnapshot.direction_cross_link` field.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/23-th-i-tu-tam-s-t-phi-tinh-cross-link/23-CONTEXT.md
@.planning/phases/23-th-i-tu-tam-s-t-phi-tinh-cross-link/23-RESEARCH.md
@.planning/adrs/0007-cross-link-crit3-carve-out.md
@crates/amlich-core/src/lib.rs
@crates/amlich-core/src/reasoning/mod.rs
@crates/amlich-core/src/reasoning/personal.rs
@crates/amlich-core/src/reasoning/types.rs
@crates/amlich-core/src/almanac/tu_menh.rs
@crates/amlich-core/src/almanac/fengshui/types.rs

<interfaces>
From `crates/amlich-core/src/reasoning/personal.rs`:
```rust
pub struct PersonalFactNode {
    pub id: String,
    pub summary_vi: String,
    pub severity: Option<String>,
    pub evidence: Vec<ReasoningEvidenceEnvelope>,
}
```

From `crates/amlich-core/src/reasoning/types.rs`:
```rust
pub enum ReasoningEvidenceSourceFamily { Snapshot, Interaction, Bazi, Axis, AlmanacRule, Insight, Derived, IChing }
pub struct ReasoningEvidenceEnvelope {
    pub source_family: ReasoningEvidenceSourceFamily,
    pub source_id: String,
    pub method: String,
    pub note: Option<String>,
}
pub enum ReasoningNodeSeverity { Auspicious, Inauspicious, HardTaboo, SoftTaboo, HoangDao, HacDao }
```

From `crates/amlich-core/src/lib.rs`:
```rust
pub struct FlyingStarsSummary {
    pub van: u8,
    pub year: i32,
    pub month: u8,
    pub center_star: crate::almanac::fengshui::types::FlyingStar,
    pub palace_overlays: [(crate::almanac::fengshui::types::FlyingStar,
                           crate::almanac::fengshui::types::FlyingStar); 9],
}

pub struct DaySnapshot { /* existing fields, including flying_stars: Option<FlyingStarsSummary> */ }
```

The new contract must use the existing `Direction` enum, not a new `Direction8`, `Direction4`, or 24-sơn representation. The eight direction order is exactly North, Northeast, East, Southeast, South, Southwest, West, Northwest.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define the rich direction-cross-link and summary contracts</name>
  <files>crates/amlich-core/src/reasoning/direction_composite.rs, crates/amlich-core/src/reasoning/mod.rs</files>
  <behavior>
    - The contract test observes the locked eight-element `DIRECTION_ORDER` and the documented `DATE_ONLY_BIRTH_CHI_INDEX == usize::MAX` sentinel.
    - The public cell, agreement, detailed-result, and summary types compile with owned serde-compatible fields, including `birth_chi_index: usize` and exactly eight `DirectionCell` slots.
    - The selective reasoning re-export exposes the contract types/constants without exposing semantic-graph edge APIs or a new directional enum.
  </behavior>
  <action>
    Create `reasoning/direction_composite.rs` as the Phase 23 reasoning-layer contract module. Keep all types serializable and owned (`Debug, Clone, PartialEq, Eq, Serialize, Deserialize`) so a summary can be stored on `DaySnapshot` and consumed by Phase 24 without lifetimes.

    Define exactly these public contracts:
    - `pub const COMPOSITE_DIRECTION_CROSS_LINK: &str = "rule.composite.direction_cross_link"`.
    - `pub const DATE_ONLY_BIRTH_CHI_INDEX: usize = usize::MAX`, with a doc comment stating that every real branch is 0..=11 and this sentinel means the date-only variant has no birth context.
    - `pub const DIRECTION_ORDER: [Direction; 8]` in the existing `direction_merge.rs` order.
    - `Agreement` enum with `Agreement`, `BothSilent`, `KhcbpptOnly`, `HuyenKhongOnly`, and `Conflict` variants, snake_case serde names.
    - `DirectionalThaiTue { direction: Direction, conflict_kinds: Vec<ThaiTueConflictKind> }`.
    - `DirectionalTaboo { thai_tue: Option<DirectionalThaiTue>, tam_sat_branches: Vec<String>, sat_phuong_direction: Option<String>, severity: ReasoningNodeSeverity, summary_vi: String }`.
    - `HuyenKhongCell { direction: Direction, palace_number: u8, annual_star: u8, monthly_star: u8, safety_hint_vi: Option<String>, summary_vi: String }`. Store star numbers as DTO projections (`u8`), not lower-level palace-layout types.
    - `DirectionCell { direction: Direction, khcbppt: Option<DirectionalTaboo>, huyen_khong: Option<HuyenKhongCell>, agreement: Option<Agreement>, severity: ReasoningNodeSeverity }`.
    - `DirectionCrossLink { cross_link_kind: String, date: String, day_chi_index: u8, birth_chi_index: usize, cells: [DirectionCell; 8], summary_vi: String, composite_severity: ReasoningNodeSeverity, evidence: Vec<ReasoningEvidenceEnvelope> }`.
    - `DirectionCrossLinkSummary` with the same rich fields plus `cross_link_source: String`. Keep `birth_chi_index: usize` (not u8/Option) and document the sentinel on both detailed and summary structs. `cross_link_source` exists for Phase 24's graph consumer and is populated from the named composite constant by the implementation plan.

    Declare the eventual public function signatures in the module's API comments, but leave their implementations to Plan 23-03: `build_direction_cross_link`, `build_direction_cross_link_personal`, `build_direction_cross_link_date`, and `project_to_summary`. Do not create temporary semantic-graph edges or modify `interaction/direction_merge.rs`.

    Add a small inline contract test proving `DIRECTION_ORDER.len() == 8` and `DATE_ONLY_BIRTH_CHI_INDEX == usize::MAX`. Update `reasoning/mod.rs` with a selective `pub use direction_composite::{...}` re-export for the types/constants (the function re-exports are added when Plan 23-03 implements them). Avoid comments/docstrings containing the exact lower-level module/type names that the later sibling CRIT-3 scan forbids in the implementation target.
  </action>
  <verify>
    <automated>cargo test -p amlich-core --lib reasoning::direction_composite && cargo build -p amlich-core</automated>
  </verify>
  <done>The contract module compiles with the existing `Direction`, evidence, and severity types; all rich cells and summary fields are owned/serde-compatible; the eight-direction order and `usize::MAX` sentinel are locked; no graph-edge or interaction-layer code was added.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add CRIT-3-safe palace safety-hint transport and default-None DaySnapshot field</name>
  <files>crates/amlich-core/src/lib.rs</files>
  <behavior>
    - `FlyingStarsSummary` has an additive optional `[Option<String>; 9]` safety-hint projection with serde defaults, so a serialized DTO that predates the field still deserializes with `None`.
    - Ordinary `calculate_day_snapshot(...)` calls populate the annual safety-hint projection but leave `direction_cross_link == None`; the new cross-link field is never inferred from the available calendar or annual layout.
    - Serializing an ordinary snapshot omits `direction_cross_link` rather than emitting a null key.
    - The new field type is `crate::reasoning::DirectionCrossLinkSummary`, preserving one public type identity for the Phase 24 consumer.
  </behavior>
  <action>
    Extend `FlyingStarsSummary` additively with:
    ```rust
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub palace_safety_hints: Option<[Option<String>; 9]>,
    ```
    and document that the entries follow the existing palace-overlay array order and are annual-layer safety text only. In the existing `calculate_day_snapshot_internal` block that constructs `FlyingStarsSummary`, compute this owned projection with `std::array::from_fn`: for each annual star in `overlay.palace_overlays[i].0`, call the already-public safety-hint lookup at the lower almanac boundary and copy only its Vietnamese hint text into `Option<String>`. This is DTO preparation; do not import that lower-level module from `reasoning/direction_composite.rs`, and do not consume `daily_flying_stars` here.

    Extend `DaySnapshot` after the existing v1.6 additive fields with:
    ```rust
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub direction_cross_link: Option<crate::reasoning::DirectionCrossLinkSummary>,
    ```
    Initialize it to `None` in the `DaySnapshot` constructor. Do not add an enrichment call or any conditional population to `calculate_day_snapshot_internal`; Plan 23-03 owns the explicit immutable enrichment helper. Preserve all existing field order and behavior. No new dependencies, no semantic-graph field, no change to `direction_merge.rs`.
  </action>
  <verify>
    <automated>cargo test -p amlich-core --lib day_snapshot_serde_round_trip && cargo test -p amlich-core --test day_snapshot_v14_compat && cargo build -p amlich-core</automated>
  </verify>
  <done>`FlyingStarsSummary` carries optional annual safety-hint text with backward-compatible serde behavior; `DaySnapshot.direction_cross_link` exists, is initialized to None, is omitted from ordinary JSON, and is not auto-populated; existing snapshot tests remain green.</done>
</task>

</tasks>

<verification>
- `cargo test -p amlich-core --lib reasoning::direction_composite` passes the contract test.
- `cargo test -p amlich-core --test day_snapshot_v14_compat` passes all existing v1.4/v1.5/v1.6 compatibility tests.
- `cargo test -p amlich-core --test source_id_guard` remains green (no production source-id literals were introduced).
- `cargo test -p amlich-core` is green.
- A source diff confirms `direction_cross_link` is initialized only to `None` in this plan; no implicit cross-link builder call exists.
</verification>

<success_criteria>
- The Phase 23 reasoning contract is explicit and usable by a later implementation without inventing types or field semantics.
- Rich eight-direction cells, five agreement states, the Vietnamese summary slot, majority-severity slot, evidence slot, and date sentinel are represented in owned serializable types.
- The annual safety-hint projection keeps the later reasoning module DTO-only and CRIT-3-safe.
- The additive snapshot field is absent by default and remains backward-compatible.
</success_criteria>

<output>
After completion, create `.planning/phases/23-th-i-tu-tam-s-t-phi-tinh-cross-link/23-02-SUMMARY.md` documenting the exact public contracts, field order, sentinel, DTO safety-hint transport, and the no-auto-population invariant for Plan 23-03.
</output>
