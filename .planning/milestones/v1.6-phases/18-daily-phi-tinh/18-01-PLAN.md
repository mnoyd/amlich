---
phase: 18-daily-phi-tinh
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/adrs/0004-daily-phi-tinh-starting-star-convention.md
  - crates/amlich-core/src/almanac/fengshui/types.rs
  - crates/amlich-core/src/almanac/fengshui/mod.rs
autonomous: true
requirements: [FS-17]
gap_closure: false

must_haves:
  truths:
    - "A reader of `.planning/adrs/0004-daily-phi-tinh-starting-star-convention.md` can find: which year's annual chart seeds the daily count, how 冬至/夏至 (Đông Chí / Hạ Chí) pivot reverses the forward sequence, a chapter + verse citation in *Thẩm Thị Huyền Không Học* (三元日白訣 / Tam Nguyên Nhật Bạch Quyết), and a list of at least 3 alternative conventions considered with reasons for the chosen one"
    - "The frozen v1 Phi Tinh `FlyingStarLayout` field set is unchanged: only `FlyingStarPeriod` gains an additive `Daily { date: (i32, u32, u32) }` variant, and a sibling `DailyFlyingStarLayout` struct is added alongside the locked `FlyingStarLayout`"
    - "The `FlyingStarPeriod::Daily { ... }` variant round-trips byte-equal through serde_json when serialized/deserialized"
    - "`almanac::fengshui` re-exports `DailyFlyingStarLayout` for external-crate test consumers"
  artifacts:
    - path: ".planning/adrs/0004-daily-phi-tinh-starting-star-convention.md"
      provides: "ADR-0004 daily starting-star convention + 6 Trung Khí pivot table + Dương-thuận / Âm-nghịch direction rule + chapter+verse citation + 3 alternative conventions considered"
      contains: "Tam Nguyên Nhật Bạch Quyết"
    - path: "crates/amlich-core/src/almanac/fengshui/types.rs"
      provides: "Additive `Daily { date: (i32, u32, u32) }` variant on `FlyingStarPeriod` + sibling `DailyFlyingStarLayout` struct"
      contains: "DailyFlyingStarLayout"
    - path: "crates/amlich-core/src/almanac/fengshui/mod.rs"
      provides: "Re-exports `DailyFlyingStarLayout` for external-crate test consumers"
      contains: "DailyFlyingStarLayout"
  key_links:
    - from: ".planning/adrs/0004-daily-phi-tinh-starting-star-convention.md"
      to: "crates/amlich-core/src/almanac/fengshui/types.rs"
      via: "ADR-0004 names the `Daily { date: (i32, u32, u32) }` variant shape + the sibling struct name + the additive-only field-set discipline"
      pattern: "DailyFlyingStarLayout"
    - from: "crates/amlich-core/src/almanac/fengshui/types.rs"
      to: "crates/amlich-core/src/almanac/fengshui/mod.rs"
      via: "Re-export line in mod.rs mirrors the additive-only DTO contract for the new variant"
      pattern: "pub use types::.*DailyFlyingStarLayout"
---

<objective>
Close FS-17 by authoring ADR-0004 (the daily starting-star convention ADR) and extending `types.rs` with the additive `FlyingStarPeriod::Daily { date: (i32, u32, u32) }` variant plus a sibling `DailyFlyingStarLayout` struct. This is the schema-lock step that gates Phase 18-02 (the algorithm) and Phase 18-03 (the golden dataset) — v1.5 retrospective pattern: schema-lock-before-algorithm before builder emission (PITFALLS CRIT-1/5).

Purpose: v1.5 deferred daily Phi Tinh because boundary semantics were ambiguous. v1.6 Phase 18 closes this by ADR-0004 capturing (a) the 6 Trung Khí pivot table, (b) the Dương-thuận / Âm-nghịch direction rule, (c) the Giáp Tý-as-seed-day mechanic (Pitfall P-7), (d) a chapter + verse citation in *Thẩm Thị Huyền Không Học*, and (e) 3 alternative conventions explicitly considered with rejection reasons. The Type stub locks the public API surface so 18-02 can build against a stable contract.

Output: ADR-0004 (~120 lines Markdown); one additive enum variant + one sibling struct in `types.rs`; one re-export line in `mod.rs`; one extended unit test confirming serde round-trip for the new variant.

Locked CONTEXT.md / 18-RESEARCH.md decisions honored (NON-NEGOTIABLE):
- The frozen v1 `FlyingStarLayout` field set is NOT mutated — sibling `DailyFlyingStarLayout` struct, not a variant mutation (types.rs:107 lock).
- `FlyingStarPeriod::Daily { date: (i32, u32, u32) }` matches the existing SolarDate day/month/year i32-tuple style (lib.rs:110-115) — does NOT introduce chrono::NaiveDate (zero new deps).
- ADR-0004 cites chapter + verse ("三元日白訣 / Tam Nguyên Nhật Bạch Quyết"); exact page citation deferred per Phase 16 deferral discipline (Q4 Option A) with explicit note that page-level citation awaits a numbered-edition lookup.
- 3 alternative conventions must be listed with rejection reasons (FS-17 success criterion).
- No `daily.rs` file in this plan — the algorithm is Plan 18-02 work; this plan only locks the schema + ADR.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/18-daily-phi-tinh/18-RESEARCH.md
@.planning/phases/17-van-khan-reviewer-closure/17-01-PLAN.md
@.planning/phases/16-foundation-adr-0003-confidence-closure/16-01-SUMMARY.md

# Source artifacts (read-only references for this plan)
@.planning/adrs/0002-phi-tinh-monthly-anchor.md
@.planning/adrs/0003-nien-tu-bach-polarity.md
@.planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md
@.planning/research/SUMMARY.md
@.planning/RETROSPECTIVE.md
@crates/amlich-core/src/almanac/fengshui/types.rs
@crates/amlich-core/src/almanac/fengshui/mod.rs
@crates/amlich-core/src/lib.rs
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. No exploration needed. -->

From `crates/amlich-core/src/almanac/fengshui/types.rs` (LOCKED FIELD SET — DO NOT MUTATE FlyingStarLayout):
```rust
// Lines 98-104: locked FlyingStarPeriod enum (Phase 13; only additive variant extension permitted)
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(tag = "kind", rename_all = "snake_case")]
pub enum FlyingStarPeriod {
    Van { van: u8 },
    Yearly { year: i32 },
    Monthly { year: i32, month: u8 },
}

// Lines 106-118: LOCKED FlyingStarLayout field set — Phase 18-01 MUST NOT mutate this struct
/// FIELD SET LOCKED — any field-set change requires a superseding ADR.
pub struct FlyingStarLayout {
    pub period: FlyingStarPeriod,
    pub palaces: [FlyingStar; 9],
    pub center_star: FlyingStar,
    pub evidence: ReasoningEvidenceEnvelope,
}

// Lines 201-214: existing serde round-trip test pattern (Test 5) — extend with Daily variant case
#[test]
fn test_flying_star_period_serde_round_trip() {
    let cases = [
        FlyingStarPeriod::Van { van: 9 },
        FlyingStarPeriod::Yearly { year: 2025 },
        FlyingStarPeriod::Monthly { year: 2025, month: 3 },
        // Phase 18-01 adds: FlyingStarPeriod::Daily { date: (2024, 12, 25) },
    ];
    ...
}
```

From `crates/amlich-core/src/lib.rs` (lines 109-115) — SolarDate i32-tuple style that the new `Daily` variant mirrors:
```rust
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct SolarDate {
    pub day: i32,
    pub month: i32,
    pub year: i32,
    pub day_of_week: usize,
}
```

From `crates/amlich-core/src/almanac/fengshui/mod.rs` (lines 13-37) — re-export surface to extend:
```rust
pub mod annual;
pub mod aspects;
pub mod combined;
pub mod golden;
pub mod monthly;
pub mod period;
pub mod safety;
pub mod scanner;
pub mod stars;
pub mod types;

// Re-exports — public API surface for fengshui module consumers.
pub use annual::{compute_yearly_flying_stars, YearPolarity};
pub use aspects::{compute_palace_aspects, lookup_star_pair_aspect, FsCitation, FsConfidenceTier, StarPairAspect};
pub use combined::{compute_combined_overlay, CombinedFlyingStarLayout};
pub use golden::{
    load_flying_stars_golden, DeferralMarker, GoldenConfidence, KnownDivergence,
    PhiTinhGoldenCase, PhiTinhGoldenDataset,
};
pub use monthly::compute_monthly_flying_stars;
pub use period::{
    base_palaces_for_van, compute_period, compute_period_for_year, load_flying_stars_base, Period,
};
pub use safety::{element_hint_for_palace, is_danger_palace, RemedyHint};
pub use scanner::TietKhiScanner;
pub use stars::{flying_star_from_u8, star_metadata};
```

From `.planning/adrs/0003a-nien-tu-bach-polarity-confidence-closure.md` (lines 1-9) — ADR template / narrative tone to mirror:
```
Status: Accepted
Date: 2026-07-15
Deciders: Phase 16 Foundation
Supersedes: ADR-0003 §6 (Confidence Acknowledgment)
---
## Context
## Decision
## Consequences
```

From `.planning/phases/18-daily-phi-tinh/18-RESEARCH.md` — algorithm decisions locked by research:
- 6 Trung Khí pivots partition the year (Đông Chí / Vũ Thuỷ / Cốc Vũ / Hạ Chí / Xử Thử / Sương Giáng)
- Đông Chí / Vũ Thuỷ / Cốc Vũ are Dương → thuận (forward, +1 per Giáp Tý cycle, mod 9)
- Hạ Chí / Xử Thử / Sương Giáng are Âm → nghịch (descending, -1 per Giáp Tý cycle, mod 9)
- Seeds: Đông Chí=1 (Nhất Bạch), Vũ Thuỷ=7 (Thất Xích), Cốc Vũ=4 (Tứ Lục), Hạ Chí=9 (Cửu Tử), Xử Thử=3 (Tam Bích), Sương Giáng=6 (Lục Bạch)
- Each pivot "kicks in" at the FIRST Giáp Tý with JD >= pivot_jd (Pitfall P-7)
- The classical verse is "TAM NGUYÊN NHHẬT BẠCH QUYẾT" / 三元日白訣
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Author ADR-0004 daily starting-star convention with 6-pivot table, Dương-thuận/Âm-nghịch direction rule, chapter+verse citation, and 3 alternative conventions</name>
  <files>.planning/adrs/0004-daily-phi-tinh-starting-star-convention.md</files>
  <action>
Author `.planning/adrs/0004-daily-phi-tinh-starting-star-convention.md` (~120 lines) following the locked ADR template from ADR-0003a:

1. **Frontmatter** (exact format from ADR-0003a lines 1-9):
   - `Status: Accepted`
   - `Date: 2026-07-15`
   - `Deciders: Phase 18 Daily Phi Tinh`
   - No `Supersedes:` line (this is a new ADR; ADR-0002 boundary discipline and ADR-0003 polarity matrix remain authoritative).

2. **`## Context`** (~25 lines): Explain why daily Phi Tinh (Lưu Nhật / 日紫白) was deferred from v1.5 (boundary semantics ambiguous — daily boundary could be naïve calendar, lunar, or solar-term; v1.5 PITFALLS MOD-2 flagged this). v1.6 Phase 18 closes the deferral by adopting the *Trung Khí pivot / Giáp Tý seed* convention per the classical "TAM NGUYÊN NHHẬT BẠCH QUYẾT" (三元日白訣) verse from *Thẩm Thị Huyền Không Học*. Reference Phase 13 / 14 / 16 patterns (annual + monthly + golden) so the reader sees daily as a sibling time-layer, not a replacement.

3. **`## Decision`** (~60 lines, structured):
   - **§1 — Boundary semantics**: Daily Phi Tinh pivots on the **6 Trung Khí** (Đông Chí, Vũ Thuỷ, Cốc Vũ, Hạ Chí, Xử Thử, Sương Giáng) — NOT on calendar Dec 21 / Jun 21. Boundary resolution is always via the v1.1.2 `TietKhiScanner::terms_for_year(year)` (filters the 24-term list by the 6 pivot names). This inherits the CRIT-2 / ADR-0002 boundary discipline — NO naïve calendar arithmetic.
   - **§2 — Pivot table** (the table from 18-RESEARCH.md lines 30-31 + 116):
     | Pivot | Starting Star | Polarity | Direction | Classical Name |
     |-------|---------------|----------|-----------|----------------|
     | Đông Chí | 1 (Nhất Bạch) | Dương | thuận (forward) | White Water |
     | Vũ Thuỷ | 7 (Thất Xích) | Dương | thuận (forward) | Red Fire |
     | Cốc Vũ | 4 (Tứ Lục) | Dương | thuận (forward) | Green Wood |
     | Hạ Chí | 9 (Cửu Tử) | Âm | nghịch (descending) | Purple Fire |
     | Xử Thử | 3 (Tam Bích) | Âm | nghịch (descending) | Jade/Green Wood |
     | Sương Giáng | 6 (Lục Bạch) | Âm | nghịch (descending) | White Metal |
   - **§3 — Direction rule**: Dương pivot → thuận hành (forward, +1 per Giáp Tý cycle, mod 9 wrapping 1↔9); Âm pivot → nghịch hành (descending, -1 per Giáp Tý cycle, mod 9 wrapping 1↔9). This is the OPPOSITE direction rule from the annual layer (ADR-0003 §4: dương year = nghịch, âm year = thuận); the daily rule uses Tiết-Khí pivot polarity, not year-stem polarity. Acknowledge this explicitly so a reader does not confuse the two rules.
   - **§4 — Giáp Tý-as-seed-day mechanic (Pitfall P-7)**: Each Tiết Khí spans ~15 days. The pivot "kicks in" at the FIRST Giáp Tý (Can=0, Chi=0) day with JD >= pivot_jd — NOT at the pivot instant itself. Days in the new Tiết Khí but BEFORE the first Giáp Tý STILL use the PREVIOUS pivot's seed and direction. Cite Phongthuycaivan.org's 2021/2022 Đông Chí example ("từ Quý Mão đến Giáp Tý là 22 ngày… ngày 11/1/2022 mới đúng ngày Giáp Tý, an từ Nhất bạch… trước ngày 10/1/2022 dùng tiết khí Sương giáng suy nghịch của năm trước").
   - **§5 — Classical citation**: Cite *Thẩm Thị Huyền Không Học* (沈氏玄空學) chapter "三元日白訣 / Tam Nguyên Nhật Bạch Quyết". EXPLICITLY NOTE that exact page-level citation is deferred per Phase 16 deferral discipline (mirrors the 1960 Trung Nguyên `PendingExternalReview` marker in ADR-0003a §4) — a numbered edition of the classical text is not located in the open Vietnamese-language references reviewed for Phase 18 research. The chapter + verse citation is achievable; the exact page awaits a digital or physical copy of the numbered edition.
   - **§6 — Alternative conventions considered (NON-NEGOTIABLE for FS-17)**:
     1. **Naïve calendar-bounded pivots** (Dec 21 / Jun 21 ± 1 day) — REJECTED. Inherits CRIT-2 / ADR-0002 boundary discipline failure mode: synthetic approximation regresses when real Tiết Khí instants shift year-to-year (e.g., Đông Chí 2024 = 2024-12-21 22:21 ICT, Đông Chí 2025 = 2025-12-21 04:02 ICT — the JD offset is non-trivial). The v1.1.2 scanner gives the EXACT JD per year.
     2. **Annual-seed descent** (start from `nien_center(year)` and add a daily offset) — REJECTED. Conflates year-polarity direction (dương year = nghịch, ADR-0003) with daily-pivot polarity direction (Dương pivot = thuận, this ADR); the two directions are OPPOSITE for the same year. Daily Phi Tinh does NOT descend from the annual center star.
     3. **Lunar-month bounded pivots** (each lunar-month 1 = Đông Chí-like reset) — REJECTED. Lunar-month boundaries shift Gregorian dates year-to-year and produce school-dependent results (PITFALLS MOD-2). Inconsistent with the ADR-0002 / ADR-0003 boundary discipline that already standardizes on solar-term anchors.
   - **§7 — Adopted**: Trung-Khí-pivot / Giáp-Tý-seed per *Tam Nguyên Nhật Bạch Quyết* chapter of *Thẩm Thị Huyền Không Học*.

4. **`## Consequences`** (~25 lines, structured per ADR-0003a Consequences pattern):
   - **FS-17 (closed in Plan 18-01)** — narrative disposition of this ADR's requirements (cite chapter + verse; list 3 alternatives; lock direction rule).
   - **FS-16 / FS-18 / FS-19** — explicit forward pointers to which plan satisfies each (18-02 algorithm + tests, 18-03 golden dataset, 18-04 DaySnapshot additive field + round-trip test + CRIT-3 grep guard).
   - **Backward Compatibility** — note that ADR-0002 (monthly anchor) and ADR-0003 (annual polarity matrix) remain authoritative; this ADR is a SIBLING decision for the daily time-layer, not a supersession.

5. **`## References`** (~10 lines): cite the three Vietnamese-language secondary modern sources (phongthuycaivan.org, phongthuyhocvungtau.com, phongthuyphamsuu.com) and the *Thẩm Thị* classical text. Cross-reference ADR-0002 / 0003 / 0003a.

This task is documentation-only — no Rust code, no JSON data, no schema changes. Commit with message `docs(phase-18): author ADR-0004 daily Phi Tinh starting-star convention with 6-pivot table and Tam Nguyen Nhat Bach Quyet citation (FS-17)`.
  </action>
  <verify>
    <automated>f=.planning/adrs/0004-daily-phi-tinh-starting-star-convention.md; test -f "$f" || { echo MISSING_FILE; exit 1; }; grep -q '^Status: Accepted$' "$f" || { echo MISSING_STATUS; exit 1; }; grep -q '^Date: 2026-07-15$' "$f" || { echo MISSING_DATE; exit 1; }; grep -q '^Deciders: Phase 18 Daily Phi Tinh$' "$f" || { echo MISSING_DECIDERS; exit 1; }; for pivot in 'Đông Chí' 'Vũ Thuỷ' 'Cốc Vũ' 'Hạ Chí' 'Xử Thử' 'Sương Giáng'; do grep -q "$pivot" "$f" || { echo "MISSING_PIVOT_$pivot"; exit 1; }; done; for star in 'Nhất Bạch' 'Thất Xích' 'Tứ Lục' 'Cửu Tử' 'Tam Bích' 'Lục Bạch'; do grep -q "$star" "$f" || { echo "MISSING_STAR_$star"; exit 1; }; done; grep -q 'Tam Nguyên Nhật Bạch Quyết' "$f" || { echo MISSING_VERSE; exit 1; }; grep -q 'Thẩm Thị Huyền Không Học' "$f" || { echo MISSING_CLASSICAL; exit 1; }; rejected=$(grep -c 'REJECTED' "$f"); [ "$rejected" -ge 3 ] || { echo "REJECTED_COUNT_$rejected"; exit 1; }; grep -q 'exact page-level citation is deferred' "$f" || { echo MISSING_DEFERRAL_NOTE; exit 1; }; echo ADR_0004_OK</automated>
  </verify>
  <done>
- `.planning/adrs/0004-daily-phi-tinh-starting-star-convention.md` exists at ~120 lines.
- Contains all 6 pivot names, all 6 classical star names, `Tam Nguyên Nhật Bạch Quyết`, `Thẩm Thị Huyền Không Học`, ≥ 3 literal `REJECTED` tokens, and the explicit page-deferral phrase.
- File ends with a `## References` section citing the 3 Vietnamese-language sources and cross-referencing ADR-0002 / 0003 / 0003a.
- Single commit `docs(phase-18): author ADR-0004 daily Phi Tinh starting-star convention with 6-pivot table and Tam Nguyen Nhat Bach Quyet citation (FS-17)` is on the branch.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add Daily { date: (i32, u32, u32) } variant + sibling DailyFlyingStarLayout struct in types.rs + re-export in mod.rs</name>
  <files>crates/amlich-core/src/almanac/fengshui/types.rs crates/amlich-core/src/almanac/fengshui/mod.rs</files>
  <action>
Edit `crates/amlich-core/src/almanac/fengshui/types.rs` to extend the locked `FlyingStarPeriod` enum with an additive 4th variant and add a sibling `DailyFlyingStarLayout` struct. DO NOT mutate the locked `FlyingStarLayout` struct (types.rs:106-118 lock).

**Step 1 — Extend `FlyingStarPeriod` enum** (insert at line 103, after `Monthly { year: i32, month: u8 },`):
```rust
    Daily { date: (i32, u32, u32) },
```

**Step 2 — Add `DailyFlyingStarLayout` sibling struct** (insert immediately after the locked `FlyingStarLayout` struct at line 125, BEFORE the `minimal_evidence` helper at line 128):
```rust
/// Sibling layout struct for the daily Phi Tinh (Lưu Nhật / 日紫白) layer.
///
/// The locked v1 `FlyingStarLayout` field set (above) is NOT mutated — this sibling
/// carries the daily-layer-specific period variant while sharing the `palaces` /
/// `center_star` / `evidence` shape. Phase 18-01 (FS-17) schema-lock step; populated
/// by `compute_daily_flying_stars` (Plan 18-02) and surfaced via
/// `DaySnapshot.daily_flying_stars` (Plan 18-04).
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct DailyFlyingStarLayout {
    pub period: FlyingStarPeriod,
    /// Flying star assigned to each palace, indexed per `Palace::ALL` order.
    pub palaces: [FlyingStar; 9],
    pub center_star: FlyingStar,
    pub evidence: ReasoningEvidenceEnvelope,
}
```

**Step 3 — Extend the existing `test_flying_star_period_serde_round_trip` test** (lines 201-214) by appending the `Daily` case to the cases array:
```rust
    let cases = [
        FlyingStarPeriod::Van { van: 9 },
        FlyingStarPeriod::Yearly { year: 2025 },
        FlyingStarPeriod::Monthly { year: 2025, month: 3 },
        FlyingStarPeriod::Daily { date: (2024, 12, 25) },
    ];
```

**Step 4 — Add a new test** that exercises the sibling struct + serde round-trip:
```rust
    /// Phase 18-01 (FS-17): DailyFlyingStarLayout serde round-trip.
    #[test]
    fn test_daily_flying_star_layout_period_serde() {
        let layout = DailyFlyingStarLayout {
            period: FlyingStarPeriod::Daily { date: (2024, 12, 25) },
            palaces: [FlyingStar::NhatBach; 9],
            center_star: FlyingStar::NhatBach,
            evidence: minimal_evidence(),
        };
        let json = serde_json::to_string(&layout).expect("serialize");
        let roundtripped: DailyFlyingStarLayout = serde_json::from_str(&json).expect("deserialize");
        assert_eq!(layout.center_star as u8, roundtripped.center_star as u8);
        if let FlyingStarPeriod::Daily { date: (y, m, d) } = roundtripped.period {
            assert_eq!((y, m, d), (2024, 12, 25));
        } else {
            panic!("Expected Daily period variant");
        }
    }
```

**Step 5 — Modify `crates/amlich-core/src/almanac/fengshui/mod.rs`** to re-export the new struct for external-crate test consumers. Add a line to the re-exports block (after line 31, after the existing `pub use golden::{ ... };` block):
```rust
pub use types::{DailyFlyingStarLayout, FlyingStar, FlyingStarLayout, FlyingStarPeriod, Palace, minimal_evidence};
```
(Replace any existing `pub use types::...` if present — the consolidated re-export covers both the locked types AND the new daily sibling.)

**Step 6 — Verify** by running the types tests + full build:
- `cargo build -p amlich-core` succeeds (no new warnings).
- `cargo test -p amlich-core --lib almanac::fengshui::types::tests` reports 7/7 (5 pre-existing + 2 from the Daily variant extension + the new DailyFlyingStarLayout test).
- `git diff crates/amlich-core/src/almanac/fengshui/types.rs` shows ONLY: (a) `Daily { date: ... }` variant added to the enum, (b) `DailyFlyingStarLayout` struct added, (c) the `Daily { date: (2024, 12, 25) }` case appended to the existing serde test, (d) the new `test_daily_flying_star_layout_period_serde` test added. NO removal of any existing struct field, variant, or test.

**Commit** with message `feat(phase-18): add DailyFlyingStarLayout sibling struct + FlyingStarPeriod::Daily variant (FS-17 schema lock)`.
  </action>
  <verify>
    <automated># New variant added
grep -q 'Daily { date: (i32, u32, u32) }' crates/amlich-core/src/almanac/fengshui/types.rs || { echo MISSING_DAILY_VARIANT; exit 1; }
# Sibling struct added
grep -q 'pub struct DailyFlyingStarLayout' crates/amlich-core/src/almanac/fengshui/types.rs || { echo MISSING_SIBLING_STRUCT; exit 1; }
# Locked FlyingStarLayout field set UNCHANGED (4 fields verified by grep)
locked_fields=$(grep -A 6 'pub struct FlyingStarLayout {' crates/amlich-core/src/almanac/fengshui/types.rs | grep -c 'pub ')
[ "$locked_fields" -eq 4 ] || { echo "FLYING_STAR_LAYOUT_FIELD_COUNT_$locked_fields"; exit 1; }
# mod.rs re-exports DailyFlyingStarLayout
grep -q 'DailyFlyingStarLayout' crates/amlich-core/src/almanac/fengshui/mod.rs || { echo MISSING_REEXPORT; exit 1; }
# Build is clean
cargo build -p amlich-core --quiet && echo BUILD_OK
# Types tests pass (5 pre-existing + 2 new = 7)
cargo test -p amlich-core --lib almanac::fengshui::types::tests 2>&1 | tail -3
  </verify>
  <done>
- `FlyingStarPeriod` gains the additive `Daily { date: (i32, u32, u32) }` variant after `Monthly`.
- Sibling `DailyFlyingStarLayout` struct is added with the 4 fields mirroring `FlyingStarLayout`.
- The locked `FlyingStarLayout` field set is UNCHANGED (4 fields verified by grep).
- `almanac::fengshui::mod.rs` re-exports `DailyFlyingStarLayout` for external-crate test consumers.
- `test_flying_star_period_serde_round_trip` extended with the `Daily` case (no regression — existing 3 cases still pass).
- New `test_daily_flying_star_layout_period_serde` test passes.
- `cargo test -p amlich-core --lib almanac::fengshui::types::tests` reports 7/7 (5 pre-existing + 2 new).
- `cargo build -p amlich-core` succeeds with no new warnings.
- Single commit `feat(phase-18): add DailyFlyingStarLayout sibling struct + FlyingStarPeriod::Daily variant (FS-17 schema lock)` is on the branch.
  </done>
</task>

</tasks>

<verification>
- Phase-level: `cargo build -p amlich-core` succeeds; `cargo test -p amlich-core --lib almanac::fengshui::types::tests` reports 7/7 (5 pre-existing + 2 new).
- Schema-level: locked `FlyingStarLayout` field set is unchanged (verified by grep — exactly 4 `pub ` fields); sibling `DailyFlyingStarLayout` is added with the 4 fields matching the locked shape; `FlyingStarPeriod::Daily { date: (i32, u32, u32) }` variant round-trips serde byte-equal.
- ADR-level: ADR-0004 exists with all 6 pivot names, all 6 classical star names, the chapter+verse citation `Tam Nguyên Nhật Bạch Quyết`, the classical text `Thẩm Thị Huyền Không Học`, ≥ 3 literal `REJECTED` tokens, and the explicit page-deferral phrase.
- Re-export: `mod.rs` surfaces `DailyFlyingStarLayout` to the public API surface so external-crate tests (18-03 / 18-04) can consume it.
- No production algorithm (daily.rs) is added in this plan — that is Plan 18-02 work. No golden dataset is added in this plan — that is Plan 18-03 work. No DaySnapshot field is added in this plan — that is Plan 18-04 work.
</verification>

<success_criteria>
1. ADR-0004 exists at `.planning/adrs/0004-daily-phi-tinh-starting-star-convention.md` with the locked 6-pivot table, the chapter+verse citation in *Thẩm Thị Huyền Không Học*, ≥ 3 alternative conventions each carrying the literal `REJECTED` token, and an explicit page-deferral phrase per Phase 16 deferral discipline.
2. `FlyingStarPeriod::Daily { date: (i32, u32, u32) }` is the additive 4th variant; the first 3 variants are unchanged.
3. `DailyFlyingStarLayout` is a sibling struct (NOT a mutation of `FlyingStarLayout`) with the 4 fields `period, palaces, center_star, evidence`.
4. `cargo test -p amlich-core --lib almanac::fengshui::types::tests` reports 7/7 (5 pre-existing + 2 new) with no regressions.
5. `almanac::fengshui::mod.rs` re-exports `DailyFlyingStarLayout` so external-crate test consumers can import it.
6. The two git commits contain only the targeted files (no accidental edits to locked types or unrelated modules).
</success_criteria>

<output>
After completion, create `.planning/phases/18-daily-phi-tinh/18-01-SUMMARY.md` using the SUMMARY.md template.
</output>