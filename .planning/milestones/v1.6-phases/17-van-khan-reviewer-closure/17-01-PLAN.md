---
phase: 17-van-khan-reviewer-closure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/data/rituals/provenance_audit.md
autonomous: true
requirements: [RIT-14, RIT-15]
gap_closure: false

must_haves:
  truths:
    - "Every one of the 60 ritual entries in `provenance_audit.md` carries a reviewer cell that is either an actual-name record or the literal `ExternalReviewPending(...)` marker; no bare `pending` cell remains anywhere in the file"
    - "Every one of the 60 ledger rows carries method_of_review, date_reviewed, and outcome columns populated with controlled tokens"
    - "Every `ExternalReviewPending(...)` marker has a non-empty reason and expected_review_date; outcome column matches the reviewer-cell disposition"
    - "All 13 category sub-tables use the same eight-column header in the same order, so a downstream parser reads identical columns across tables"
  artifacts:
    - path: "crates/amlich-core/data/rituals/provenance_audit.md"
      provides: "60-row reviewer-audit ledger (RIT-14 + RIT-15 closure)"
      contains: "ExternalReviewPending(reason="
  key_links:
    - from: "crates/amlich-core/data/rituals/provenance_audit.md"
      to: "crates/amlich-core/data/rituals/*.json (13 corpus files)"
      via: "60 ritual_id cells = 60 unique corpus IDs (1:1 verified post-edit by Plan 17-02 test)"
      pattern: "60 ledger rows covering all 60 all_rituals() IDs"
    - from: "table header row"
      to: "every category sub-table"
      via: "exact identical 8-column header `| ritual_id | classical_reference | page | confidence | reviewer | method_of_review | date_reviewed | outcome |`"
      pattern: "\\| ritual_id \\| classical_reference \\| page \\| confidence \\| reviewer \\| method_of_review \\| date_reviewed \\| outcome \\|"
---

<objective>
Close the v1.5 deferred RIT-11 reviewer-field tech debt by replacing every `pending` reviewer cell in `provenance_audit.md` with an explicit `ExternalReviewPending(...)` deferral marker (per Phase 16 `DeferralMarker` shape adapted for a Markdown ledger) and expanding every per-category table to carry method_of_review, date_reviewed, and outcome columns. This is the editorial ledger pass that satisfies RIT-14 and RIT-15; Plan 17-02 follows with the corrected-entry re-verification test (RIT-16).

Purpose: v1.5 shipped the ledger with `reviewer: pending` for all 60 entries as a documented deferred-work placeholder. Phase 16 established the typed `PendingExternalReview` deferral pattern for the 1960 Phi Tinh divergence; Phase 17 applies the same deferral discipline to the 60-row ritual ledger without modifying the locked `RitualEntry` JSON schema (ADR-0001).

Output: A single updated Markdown file — `crates/amlich-core/data/rituals/provenance_audit.md` — with 60 rows, 13 category sub-tables, an 8-column header, the `ExternalReviewPending(...)` marker on every deferred row, and updated top-of-file prose describing the closure policy and outcome breakdown.

Locked CONTEXT.md decisions honored (NON-NEGOTIABLE):
- All 60 entries get `ExternalReviewPending` markers — no fabricated reviewer identities (source-provenance discipline per DEC-0015/0016 + ADR-0001).
- Marker syntax: `ExternalReviewPending(reason="..."; expected_review_date="..."; assigned_to="...")`.
- `method_of_review` for deferred rows = `desk-check`.
- `date_reviewed` for deferred rows = `2026-07-15`.
- `expected_review_date` for all 60 rows = `2026-12-31`.
- `assigned_to` = `external-vn-folk-ritual-reviewer` for all 60 rows.
- Outcome column for all 60 rows = `ExternalReviewPending`.
- Ledger is the canonical record; no JSON schema changes; no edits to any corpus JSON entry.
- Use `invocation_text_vi` in code (NEVER `body_vi`).
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/17-van-khan-reviewer-closure/17-CONTEXT.md
@.planning/phases/17-van-khan-reviewer-closure/17-RESEARCH.md
@.planning/phases/12-van-khan-corpus-authoring/12-VERIFICATION.md
@.planning/phases/16-foundation-adr-0003-confidence-closure/16-02-SUMMARY.md

# Source artifacts (read-only references for this plan)
@.planning/adrs/0001-ritual-schema-v1.md
@crates/amlich-core/data/rituals/provenance_audit.md
@crates/amlich-core/data/rituals/manifest.json
@crates/amlich-core/src/rituals/schema.rs
@crates/amlich-core/src/rituals/corpus.rs
</context>

<interfaces>
<!-- Contracts the executor needs. Extracted from codebase. No exploration needed. -->

From `crates/amlich-core/src/rituals/schema.rs` (LOCKED — ADR-0001):
```rust
// Line 139: locked field for the ritual body (NEVER `body_vi`)
pub invocation_text_vi: String,
// Lines 127-150: RitualEntry with #[serde(deny_unknown_fields)] —
// adding any JSON field would be a schema change and load-time failure.
```

From `crates/amlich-core/src/rituals/corpus.rs`:
```rust
// Line 94: public loader — returns the full corpus
pub fn all_rituals() -> &'static [RitualEntry]
// Line 60-74: ALL_CORPUS_JSONS — 13 include_str! constants in merge order
// Line 85: static RITUALS: OnceLock<Vec<RitualEntry>> — single shared cache
// Line 76: EXPECTED_SCHEMA_VERSION = "rituals-v1"
```

From `crates/amlich-core/src/rituals/mod.rs`:
```rust
// Lines 26-27: public re-exports
pub use schema::*;
pub use corpus::all_rituals;
```

From `crates/amlich-core/data/rituals/manifest.json` (13 corpus files in order):
```
tet-nguyen-dan.json, nguyen-tieu.json, han-thuc.json, thanh-minh.json,
doan-ngo.json, phat-dan.json, vu-lan.json, trung-thu.json,
trung-cuu-ha-nguyen.json, ong-tao.json, life-events.json, soc-vong.json,
gia-tien-thuong-nhat.json
```

From `crates/amlich-core/data/rituals/provenance_audit.md`:
- 13 category sub-tables; existing 5-column header `| ritual_id | classical_reference | page | confidence | reviewer |`
- 60 rows total (4+5+5+5+4+3+3+4+5+4+3+4+11 = 60)
- Every reviewer cell currently equals `pending`
- Top-of-file prose names the closure policy and references RIT-11

From `crates/amlich-core/src/almanac/fengshui/golden.rs` (Phase 16 reference pattern):
```rust
// Lines 79-89: DeferralMarker shape — { reason, expected_review_date, assigned_to }
// Used as Markdown marker analog: ExternalReviewPending(reason="..."; expected_review_date="..."; assigned_to="...")
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite every category sub-table to the 8-column header with ExternalReviewPending markers</name>
  <files>crates/amlich-core/data/rituals/provenance_audit.md</files>
  <action>
Open `crates/amlich-core/data/rituals/provenance_audit.md` (currently 209 lines). For each of the 13 category sub-tables (the headings `### Tet Nguyen Dan (...)` through `### Su Kien Vong Gia (Life Events)`), do the following IN PLACE — preserving all existing `ritual_id`, `classical_reference`, `page`, and `confidence` cells verbatim:

1. Replace the table header row from
   `| ritual_id | classical_reference | page | confidence | reviewer |`
   to
   `| ritual_id | classical_reference | page | confidence | reviewer | method_of_review | date_reviewed | outcome |`
2. Replace the separator row from
   `|---|---|---|---|---|`
   to
   `|---|---|---|---|---|---|---|---|`
3. For each of the 60 data rows, replace the trailing `| pending ` cell with three new columns AND an updated `reviewer` cell containing the marker:
   - `reviewer` cell becomes exactly: `ExternalReviewPending(reason="Independent classical-Vietnamese reviewer for vn-folk-ritual not available in this Claude execution; deferral preserves source-provenance discipline (DEC-0015/0016)"; expected_review_date="2026-12-31"; assigned_to="external-vn-folk-ritual-reviewer")`
   - `method_of_review` cell becomes exactly: `desk-check`
   - `date_reviewed` cell becomes exactly: `2026-07-15`
   - `outcome` cell becomes exactly: `ExternalReviewPending`

DO NOT modify any `ritual_id`, `classical_reference`, `page`, or `confidence` cell. DO NOT modify the JSON corpus files. DO NOT add new rows. DO NOT remove any row. DO NOT add `body_vi` or any other field — use `invocation_text_vi` if you reference the body field anywhere in the prose.

After the table edits, update the top-of-file prose (lines 1-9 + 207-209 currently):

- Line 3 `**Last updated:**` → `**Last updated:** 2026-07-15`
- Line 4 `**Entries:**` → `**Entries:** 60 unique ritual IDs across 13 event categories`
- Replace the paragraph at lines 6-9 with: "This ledger satisfies RIT-11, RIT-14, and RIT-15: one row per corpus entry recording the classical reference, citation page, confidence tier, reviewer disposition, method_of_review, date_reviewed, and outcome. Every `ritual_id` present in the corpus JSON files appears exactly once. Phase 17 closure policy (2026-07-15): no independent classical-Vietnamese reviewer is available in this Claude execution; per source-provenance discipline (DEC-0015/0016, ADR-0001), the project does NOT fabricate reviewer identities. All 60 entries are dispositioned as `ExternalReviewPending` with truthful reason, expected review date `2026-12-31`, and assignee `external-vn-folk-ritual-reviewer`. The disposition is recorded in both the `reviewer` cell (via the `ExternalReviewPending(...)` marker) and the `outcome` column. Method_of_review is `desk-check` (audit-of-record against the existing cited reference); date_reviewed `2026-07-15` is the date the deferral assessment was recorded. Outcome counts: 0 confirmed, 0 corrected, 0 disputed, 60 ExternalReviewPending."
- Replace the final `*Note:` paragraph at the bottom of the file with: "*Note (2026-07-15): Phase 17 closes RIT-11 by replacing all 60 `pending` reviewer cells with explicit `ExternalReviewPending(...)` deferral markers. No reviewer identities are fabricated. No JSON schema changes are made. The `RitualEntry` JSON schema remains locked per ADR-0001; the canonical reviewer record is this Markdown ledger.*"

Verify by grep that exactly 60 occurrences of `ExternalReviewPending(` exist in the file (one per data row, zero elsewhere) and zero occurrences of the bare token `pending` (case-insensitive, whole-word) remain in any reviewer column. The literal phrase "pending" may still appear in the prose (e.g. "deferred", "PendingExternalReview" is NOT used here — that token belongs to Phase 16 Phi Tinh, not Phase 17 rituals) — only the bare `| pending ` cell token must be gone.

Write the file in place. Do NOT touch any corpus JSON, `schema.rs`, `corpus.rs`, or any other Rust file in this plan. This plan is purely editorial work on the ledger Markdown file.
  </action>
  <verify>
    <automated>grep -c 'ExternalReviewPending(' crates/amlich-core/data/rituals/provenance_audit.md | awk '{ exit ($1 == 60 ? 0 : 1) }' && echo OK_60_MARKERS; grep -cE '\|\s*pending\s*\|' crates/amlich-core/data/rituals/provenance_audit.md | awk '{ exit ($1 == 0 ? 0 : 1) }' && echo OK_NO_BARE_PENDING; grep -c '| desk-check | 2026-07-15 | ExternalReviewPending |' crates/amlich-core/data/rituals/provenance_audit.md | awk '{ exit ($1 == 60 ? 0 : 1) }' && echo OK_60_METHOD_DATE_OUTCOME</automated>
  </verify>
  <done>
- All 13 category sub-tables now have the exact 8-column header `| ritual_id | classical_reference | page | confidence | reviewer | method_of_review | date_reviewed | outcome |` (and the matching 8-cell separator row).
- Exactly 60 `ExternalReviewPending(` markers exist, one per data row.
- Exactly 60 trailing cells equal `desk-check | 2026-07-15 | ExternalReviewPending`.
- Zero occurrences of bare `| pending |` reviewer cells.
- All `ritual_id`, `classical_reference`, `page`, `confidence` values unchanged from the v1.5 baseline (the 60 ritual IDs and their citations are preserved).
- Top-of-file prose documents the closure policy and the 0/0/0/60 outcome breakdown.
- File ends with the updated 2026-07-15 note.
  </done>
</task>

<task type="auto">
  <name>Task 2: Deterministic 60-row coverage audit + pre-handoff invariant sweep</name>
  <files>crates/amlich-core/data/rituals/provenance_audit.md</files>
  <action>
Run a deterministic audit of the updated ledger before handing off to Plan 17-02. This is a Markdown-only sweep (no Rust changes); use shell + python3 (already in PATH per Phase 12 verification precedent) to confirm:

1. The file contains exactly 60 data rows (i.e. 60 lines matching the data-row pattern after the 13 sub-table headers). The Python check must split the file by the `### ` category headings and count non-empty pipe-delimited rows inside each section.

2. Every data row has exactly 8 pipe-delimited cells. (Header row and separator row are exempt.)

3. The set of 60 `ritual_id` cells (column 1 of every data row) is non-empty and has 60 unique values.

4. Every `method_of_review` cell equals exactly `desk-check`. No other value.

5. Every `date_reviewed` cell equals exactly `2026-07-15`. No other value.

6. Every `outcome` cell equals exactly `ExternalReviewPending`. No other value.

7. Every `reviewer` cell starts with `ExternalReviewPending(` and contains non-empty `reason="..."` and `expected_review_date="2026-12-31"` substrings (the `assigned_to` substring must contain `external-vn-folk-ritual-reviewer`).

8. The file contains the literal substring `RIT-14` and `RIT-15` in the top-of-file prose (proving the prose documents which requirements are closed).

If any of the above checks fail, FIX the ledger in place (do NOT alter corpus JSON or Rust code). Do not silently skip a failed check; do not introduce workarounds in the test code in Plan 17-02 to compensate for ledger issues.

After the audit passes, confirm the ledger still passes `cargo build -p amlich-core` (the corpus is unchanged, so the build must remain green; this is a sanity check, not a gate) and run `cargo test -p amlich-core --test rituals_integration` to confirm the existing 6 tests still pass (the ledger is not yet read by any test — Plan 17-02 will add that — but the build path is the same).

Commit the ledger change with a single commit using the v1.5 single-commit RED→GREEN-style discipline (no separate "wip" commit allowed): message `docs(phase-17): close RIT-14 + RIT-15 by expanding provenance_audit.md to 8-column review record with ExternalReviewPending markers on all 60 entries`.

If the existing repo has a git workflow hook that requires different commit formatting, match that style exactly. Do not amend prior commits.
  </action>
  <verify>
    <automated>python3 -c "
import re
text = open('crates/amlich-core/data/rituals/provenance_audit.md', encoding='utf-8').read()
rows = [ln for ln in text.splitlines() if re.match(r'^\|\s*van-khan-', ln)]
assert len(rows) == 60, f'expected 60 data rows, got {len(rows)}'
for r in rows:
    cells = [c.strip() for c in r.strip('|').split('|')]
    assert len(cells) == 8, f'row has {len(cells)} cells, expected 8: {r}'
    assert cells[5] == 'desk-check', f'method != desk-check: {r}'
    assert cells[6] == '2026-07-15', f'date_reviewed != 2026-07-15: {r}'
    assert cells[7] == 'ExternalReviewPending', f'outcome != ExternalReviewPending: {r}'
    assert cells[4].startswith('ExternalReviewPending('), f'reviewer not marker: {r}'
    assert 'expected_review_date=\"2026-12-31\"' in cells[4], f'missing expected_review_date: {r}'
    assert 'external-vn-folk-ritual-reviewer' in cells[4], f'missing assigned_to: {r}'
ids = [r.split('|')[1].strip() for r in rows]
assert len(set(ids)) == 60, f'expected 60 unique IDs, got {len(set(ids))}'
assert 'RIT-14' in text and 'RIT-15' in text, 'top-of-file prose must name RIT-14 and RIT-15'
print('LEDGER_AUDIT_OK', len(rows), 'rows,', len(set(ids)), 'unique IDs')
"
# Build sanity check (corpus unchanged; must stay green)
cargo build -p amlich-core --quiet
# Existing integration tests must still pass (no test changes in this plan)
cargo test -p amlich-core --test rituals_integration --quiet
</automated>
  </verify>
  <done>
- Python audit script reports `LEDGER_AUDIT_OK 60 rows, 60 unique IDs`.
- All 8-cell counts match; all method/date/outcome cells match the controlled tokens; every reviewer cell is the marker with all required substrings.
- Top-of-file prose names both RIT-14 and RIT-15.
- `cargo build -p amlich-core` succeeds with no new warnings.
- `cargo test -p amlich-core --test rituals_integration` still reports 6/6 passing (Plan 17-02 will add 2 more tests).
- Single commit `docs(phase-17): close RIT-14 + RIT-15 by expanding provenance_audit.md to 8-column review record with ExternalReviewPending markers on all 60 entries` is on the branch.
- The ledger is in its final shape ready for Plan 17-02's ledger-driven test parser.
  </done>
</task>

</tasks>

<verification>
- Phase-level: `cargo build -p amlich-core` succeeds; `cargo test -p amlich-core --test rituals_integration` reports 6/6 (no regressions).
- Ledger-level: 60 rows, 8 columns each, 60 unique IDs, every marker non-empty, every method/date/outcome controlled token.
- No JSON corpus file changed; no Rust file in `src/rituals/` changed; no schema or ADR created.
- Outcome counts: 0 confirmed / 0 corrected / 0 disputed / 60 ExternalReviewPending — deterministic and stable for the Plan 17-02 test parser.
</verification>

<success_criteria>
1. `provenance_audit.md` contains exactly 60 data rows, each with the 8-column header.
2. Every reviewer cell is `ExternalReviewPending(reason="..."; expected_review_date="2026-12-31"; assigned_to="external-vn-folk-ritual-reviewer")`.
3. Every method_of_review cell is `desk-check`; every date_reviewed is `2026-07-15`; every outcome is `ExternalReviewPending`.
4. The set of 60 ledger IDs equals the set of `all_rituals()` IDs (1:1 coverage, validated structurally — Plan 17-02 adds the runtime parity test).
5. Zero occurrences of the bare `| pending |` reviewer cell anywhere in the file.
6. The top-of-file prose names both RIT-14 and RIT-15 and documents the outcome-count breakdown.
7. The single git commit contains only `crates/amlich-core/data/rituals/provenance_audit.md` (no accidental edits).
</success_criteria>

<output>
After completion, create `.planning/phases/17-van-khan-reviewer-closure/17-01-SUMMARY.md` using the SUMMARY.md template.
</output>