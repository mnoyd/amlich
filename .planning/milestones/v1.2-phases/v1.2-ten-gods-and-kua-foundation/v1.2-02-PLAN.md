---
phase: v1.2-ten-gods-and-kua-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified: [crates/amlich-core/src/almanac/tu_menh.rs, crates/amlich-core/src/almanac/types.rs, crates/amlich-core/src/almanac/mod.rs, crates/amlich-core/src/lib.rs]
autonomous: true
requirements: [TM-01, TM-02, TM-03, TM-04, TM-05]
must_haves:
  truths:
    - "Kua number computes deterministically from birth year and gender"
    - "East/West group derives from computed Kua number"
    - "Favorable and unfavorable direction sets depend on group"
    - "Year boundary and edge cases are documented and fixture-backed"
  artifacts:
    - path: "crates/amlich-core/src/almanac/tu_menh.rs"
      provides: "Kua calculator, direction sets, and fixture coverage"
      exports: ["KuaGroup", "KuaResult", "compute_kua()"]
      min_lines: 150
    - path: "crates/amlich-core/src/almanac/types.rs"
      provides: "Kua types and metadata structure"
      contains: "KuaGroup and/or KuaResult type definitions"
    - path: "crates/amlich-core/src/almanac/mod.rs"
      provides: "Module export wiring"
      contains: "pub mod tu_menh"
    - path: "crates/amlich-core/src/lib.rs"
      provides: "Public API re-export"
      contains: "compute_kua"
  key_links:
    - from: "crates/amlich-core/src/almanac/tu_menh.rs"
      to: "crates/amlich-core/src/almanac/types.rs"
      via: "Shared Kua types"
      pattern: "Kua(Result|Group)"
    - from: "crates/amlich-core/src/lib.rs"
      to: "crates/amlich-core/src/almanac/tu_menh.rs"
      via: "Public re-export"
      pattern: "pub use .*compute_kua"
---

<objective>
Implement the Tu Menh/Kua calculator so users can compute Kua from birth year and gender with one documented project convention, including East/West grouping and favorable/unfavorable direction sets.

Purpose: Provide typed, deterministic Kua computation required by v1.2 integration with explicit convention documentation and fixture-backed edge-case handling.
Output: New `tu_menh.rs` module, typed Kua API, direction set calculator, documented convention policy, and representative 1900-2099 fixture coverage.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v1.2-ten-gods-and-kua-foundation/v1.2-RESEARCH.md
@.planning/REQUIREMENTS-v1.2.md
@crates/amlich-core/src/almanac/types.rs
@crates/amlich-core/src/almanac/mod.rs
@crates/amlich-core/src/lib.rs

# Implementation constraints:
# - Freeze one explicit Kua convention (year_basis, kua5_resolution, gender_encoding) in docs/tests
# - Kua 5 special case must be resolved (typical convention: males->8, females->2)
# - Document year-boundary handling (lunar vs solar year basis, if applicable)
# - Direction sets must derive from group (not hardcoded per-Kua)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Kua domain types and document project convention</name>
  <files>crates/amlich-core/src/almanac/tu_menh.rs, crates/amlich-core/src/almanac/types.rs</files>
  <behavior>
    - Test 1: KuaGroup enum has East and West variants
    - Test 2: KuaResult struct contains kua number, group, direction sets, and convention metadata
    - Test 3: Convention metadata includes year_basis, kua5_resolution, and gender_encoding fields
  </behavior>
  <action>
    Create domain types:
    - `KuaGroup` enum (`East`, `West`)
    - `KuaResult` struct with fields: `kua: u8`, `group: KuaGroup`, `favorable_directions: [Direction; 4]`, `unfavorable_directions: [Direction; 4]`, `convention: ConventionMetadata`
    - `ConventionMetadata` struct documenting frozen project decisions
    - `Direction` enum for 8 cardinal directions
    - `compute_kua(birth_year: i32, gender: Gender)` API

    Add explicit doc comments documenting:
    - Year basis (solar vs lunar)
    - Kua 5 resolution (male->8, female->2 or other)
    - Gender encoding (how to represent male/female)
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib tu_menh</automated>
  </verify>
  <done>
    Typed Kua types exist with documented convention
    Convention metadata structure is serializable
    API signature is defined and type-safe
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic Kua calculator with edge-case handling</name>
  <files>crates/amlich-core/src/almanac/tu_menh.rs</files>
  <behavior>
    - Test 1: Standard years compute to correct Kua numbers per documented formula
    - Test 2: Kua 5 resolves to correct replacement based on gender
    - Test 3: East/West group derives correctly from Kua number (odd=East, even=West or project convention)
  </behavior>
  <action>
    Implement Kua calculation using:
    - Documented project convention formula (sum digits, reduce by gender parity)
    - Explicit Kua 5 resolution per frozen policy
    - Group derivation (odd/even mapping to East/West)
    - Year boundary handling (if applicable to chosen convention)

    Keep formula explicit and readable with inline comments mapping steps to convention rationale.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib tu_menh::tests::compute</automated>
  </verify>
  <done>
    Kua calculator is deterministic and convention-compliant
    Kua 5 special case handled explicitly
    Group assignment logic tested
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement direction set derivation and representative fixtures</name>
  <files>crates/amlich-core/src/almanac/tu_menh.rs</files>
  <behavior>
    - Test 1: East group returns correct favorable/unfavorable direction sets
    - Test 2: West group returns correct favorable/unfavorable direction sets
    - Test 3: Representative years from 1900-2099 produce expected Kua and direction outputs
  </behavior>
  <action>
    Implement direction set calculator:
    - Define East group favorable directions: North, South, East, Southeast
    - Define East group unfavorable directions: Northwest, Southwest, West, Northeast
    - Define West group favorable directions: Northwest, Southwest, West, Northeast
    - Define West group unfavorable directions: North, South, East, Southeast

    Add representative fixtures for 1900-2099:
    - Include year-boundary cases (e.g., 1899/1900, 2099/2100)
    - Include male/female pairs for edge years
    - Include Kua 5 years (e.g., 2002 male/female)
    - At least 20 representative cases covering full Kua range and groups
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib tu_menh::tests::directions</automated>
    <automated>cargo test --package amlich-core --lib tu_menh::tests::fixtures</automated>
  </verify>
  <done>
    Direction sets are correct for both groups
    Representative fixtures pass for 1900-2099
    Edge cases are covered and documented
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire module exports for downstream integration</name>
  <files>crates/amlich-core/src/almanac/mod.rs, crates/amlich-core/src/lib.rs</files>
  <action>
    Export new module and API so v1.2-03 integration can consume it:
    - Add `pub mod tu_menh;` in almanac mod
    - Re-export `compute_kua` and key types from crate root if consistent with existing public API patterns
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib</automated>
  </verify>
  <done>
    Module compiles and is discoverable
    API is available for integration plan
    No regressions introduced
  </done>
</task>

</tasks>

<verification>
Run full package tests to ensure Kua additions do not regress validated behavior:

```bash
cargo test --package amlich-core
```

Expected result: all existing tests remain green and Kua fixture coverage passes.
</verification>

<success_criteria>
1. Kua calculator computes deterministically from year and gender (TM-01)
2. Convention policy (year basis, Kua 5, gender encoding) is documented and tested (TM-02)
3. Typed Kua output includes number, East/West group, and direction sets (TM-03, TM-04)
4. Representative 1900-2099 fixtures pass including edge cases (TM-05)
5. Module/public exports are wired for downstream v1.2 integration
6. `cargo test --package amlich-core` passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-ten-gods-and-kua-foundation/v1.2-02-SUMMARY.md`
</output>
