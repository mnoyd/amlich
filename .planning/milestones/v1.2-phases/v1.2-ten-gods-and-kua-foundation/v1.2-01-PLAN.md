---
phase: v1.2-ten-gods-and-kua-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [crates/amlich-core/src/almanac/thap_than.rs, crates/amlich-core/src/almanac/types.rs, crates/amlich-core/src/almanac/mod.rs, crates/amlich-core/src/lib.rs]
autonomous: true
requirements: [TT-01, TT-02, TT-03, TT-04, TT-05]
must_haves:
  truths:
    - "Ten Gods relationship is deterministic for every day stem to target stem pair"
    - "All 100 combinations (10 day stems x 10 target stems) are covered by tests"
    - "Public API exposes typed Ten Gods result via get_thap_than(day_can, target_can)"
    - "Ten Gods result includes rule/source evidence metadata"
  artifacts:
    - path: "crates/amlich-core/src/almanac/thap_than.rs"
      provides: "Ten Gods mapping engine, typed API, and matrix tests"
      exports: ["ThapThanLabel", "ThapThanResult", "get_thap_than()"]
      min_lines: 180
    - path: "crates/amlich-core/src/almanac/types.rs"
      provides: "Shared types and metadata shape for Ten Gods evidence"
      contains: "ThapThanLabel and/or ThapThanResult type definitions"
    - path: "crates/amlich-core/src/almanac/mod.rs"
      provides: "Almanac module export wiring"
      contains: "pub mod thap_than"
    - path: "crates/amlich-core/src/lib.rs"
      provides: "Public API re-export"
      contains: "get_thap_than"
  key_links:
    - from: "crates/amlich-core/src/almanac/thap_than.rs"
      to: "crates/amlich-core/src/almanac/types.rs"
      via: "Shared typed result and metadata"
      pattern: "ThapThan(Result|Label)"
    - from: "crates/amlich-core/src/lib.rs"
      to: "crates/amlich-core/src/almanac/thap_than.rs"
      via: "Public re-export"
      pattern: "pub use .*get_thap_than"
---

<objective>
Implement the Thap Than (Ten Gods) foundation engine so any day stem can map to the correct relationship label for any target stem using deterministic polarity and five-element rules.

Purpose: Provide a typed and test-verified relationship core required by v1.2 integration and future advanced calculations.
Output: New `thap_than.rs` module, typed API, 100-case matrix tests, and evidence metadata support.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v1.2-ten-gods-and-kua-foundation/v1.2-RESEARCH.md
@.planning/REQUIREMENTS-v1.2.md
@crates/amlich-core/src/almanac/types.rs
@crates/amlich-core/src/almanac/mod.rs
@crates/amlich-core/src/lib.rs
@crates/amlich-core/src/canchi.rs

# Implementation constraints:
# - Keep API typed (avoid stringly-typed public contracts)
# - Deterministic lookup only (no runtime randomness or locale branching)
# - Align metadata style with existing evidence/ruleset patterns in almanac types
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define Ten Gods domain types and public API contract</name>
  <files>crates/amlich-core/src/almanac/thap_than.rs, crates/amlich-core/src/almanac/types.rs</files>
  <behavior>
    - Test 1: Ten Gods labels are represented as a typed enum with 10 variants
    - Test 2: API returns a typed result object suitable for serde serialization
    - Test 3: Invalid stem inputs are handled explicitly (Result/error or panic policy consistent with existing code)
  </behavior>
  <action>
    Create domain types:
    - `ThapThanLabel` enum with 10 variants matching TT-01 names
    - `ThapThanResult` struct with `label` and evidence/source metadata fields
    - `get_thap_than(day_can, target_can)` API (typed inputs preferred)

    If existing can-typing already exists, reuse it rather than duplicating stem enums.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib thap_than</automated>
  </verify>
  <done>
    Typed Ten Gods labels and result struct exist
    get_thap_than API is callable and serializable
    Input validation behavior is documented and tested
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement deterministic mapping logic (polarity + five elements)</name>
  <files>crates/amlich-core/src/almanac/thap_than.rs</files>
  <behavior>
    - Test 1: Same-element + same-polarity maps to Ty Kien
    - Test 2: Same-element + opposite-polarity maps to Kiep Tai
    - Test 3: Generate/control/controlled relationships map to correct labels by polarity split
  </behavior>
  <action>
    Implement deterministic mapping using:
    - Day stem element + yin/yang polarity
    - Target stem element + yin/yang polarity
    - Five-element relation class (same, generate, generated-by, control, controlled-by)

    Keep mapping table explicit in code for auditability.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib thap_than::tests::mapping</automated>
  </verify>
  <done>
    Core mapping function is deterministic and table-driven
    Relation-to-label policy is explicit and readable
    Core mapping tests pass
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add full 100-case matrix tests and evidence metadata assertions</name>
  <files>crates/amlich-core/src/almanac/thap_than.rs</files>
  <behavior>
    - Test 1: All 10x10 combinations are asserted with expected labels
    - Test 2: Matrix is stable/deterministic across repeated calls
    - Test 3: Evidence metadata is present and consistent for all outputs
  </behavior>
  <action>
    Add table-driven matrix fixture that asserts:
    - each (day_can, target_can) pair returns expected `ThapThanLabel`
    - output includes evidence/source attribution fields required by TT-05

    Include at least one serialization snapshot/assertion to lock JSON label form.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib thap_than</automated>
  </verify>
  <done>
    100-case matrix test exists and passes
    Metadata assertions pass for representative and matrix cases
    JSON representation is stable
  </done>
</task>

<task type="auto">
  <name>Task 4: Wire module exports for downstream integration</name>
  <files>crates/amlich-core/src/almanac/mod.rs, crates/amlich-core/src/lib.rs</files>
  <action>
    Export new module and API so v1.2-03 integration can consume it without path churn:
    - Add `pub mod thap_than;` in almanac mod
    - Re-export `get_thap_than` and key types from crate root if consistent with existing public API patterns
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib</automated>
  </verify>
  <done>
    Module compiles and is discoverable
    API is available for integration plan
    No regressions introduced
  </done>
</task>

</tasks>

<verification>
Run full package tests to ensure Ten Gods additions do not regress validated behavior:

```bash
cargo test --package amlich-core
```

Expected result: all existing tests remain green and Ten Gods matrix coverage passes.
</verification>

<success_criteria>
1. Ten Gods model and deterministic mapping are implemented (TT-01, TT-02)
2. `get_thap_than(day_can, target_can)` typed API exists and is serializable (TT-03)
3. Complete 10x10 matrix tests pass with explicit expected labels (TT-04)
4. Evidence/source metadata is present and tested (TT-05)
5. Module/public exports are wired for downstream v1.2 integration
6. `cargo test --package amlich-core` passes with no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-ten-gods-and-kua-foundation/v1.2-01-SUMMARY.md`
</output>
