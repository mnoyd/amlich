---
phase: v1.2-ten-gods-and-kua-foundation
plan: 03
type: execute
wave: 2
depends_on: [v1.2-01, v1.2-02]
files_modified: [crates/amlich-core/src/almanac/types.rs, crates/amlich-core/src/almanac/calc.rs, crates/amlich-core/src/lib.rs, crates/amlich-api/src/dto.rs, crates/amlich-api/src/convert.rs]
autonomous: true
requirements: [INT-01, INT-02, INT-03, INT-04, INT-05, INT-06]
must_haves:
  truths:
    - "DayFortune includes Ten Gods and Kua fields as optional, non-breaking additions"
    - "API/DTO surfaces expose new fields through stable serialization"
    - "New fields populate deterministically only when required inputs are present"
    - "Existing consumers remain unaffected (backward compatibility)"
  artifacts:
    - path: "crates/amlich-core/src/almanac/types.rs"
      provides: "Extended DayFortune with optional Ten Gods and Kua fields"
      contains: "ten_gods: Option<DayTenGods> and/or tu_menh: Option<KuaResult>"
    - path: "crates/amlich-core/src/almanac/calc.rs"
      provides: "Conditional computation wiring for new fields"
      contains: "DayFortune population logic"
    - path: "crates/amlich-api/src/dto.rs"
      provides: "API DTO with mirrored optional fields"
      contains: "Ten Gods and Kua fields"
    - path: "crates/amlich-api/src/convert.rs"
      provides: "Conversion layer preserving optional fields"
      contains: "DTO mapping logic"
  key_links:
    - from: "crates/amlich-core/src/almanac/calc.rs"
      to: "crates/amlich-core/src/almanac/thap_than.rs"
      via: "Ten Gods computation on demand"
      pattern: "get_thap_than"
    - from: "crates/amlich-core/src/almanac/calc.rs"
      to: "crates/amlich-core/src/almanac/tu_menh.rs"
      via: "Kua computation on demand"
      pattern: "compute_kua"
    - from: "crates/amlich-api/src/convert.rs"
      to: "crates/amlich-core/src/almanac/types.rs"
      via: "Optional field propagation"
      pattern: "Option<"
    - from: "crates/amlich-api/src/dto.rs"
      to: "crates/amlich-core/src/almanac/types.rs"
      via: "Type mirroring for API surface"
      pattern: "ten_gods|tu_menh"
---

<objective>
Integrate Ten Gods and Kua outputs into DayFortune and API/serialization contracts with additive-only changes, ensuring deterministic population and backward compatibility for existing consumers.

Purpose: Expose new Ten Gods and Kua fields through established surfaces without breaking existing integrations, maintaining KHCBPPT correctness guarantees while extending output capabilities.
Output: Extended DayFortune type, conditional computation wiring, API DTO/convert propagation, and full regression gate passing.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/v1.2-ten-gods-and-kua-foundation/v1.2-01-SUMMARY.md
@.planning/phases/v1.2-ten-gods-and-kua-foundation/v1.2-02-SUMMARY.md
@.planning/REQUIREMENTS-v1.2.md
@crates/amlich-core/src/almanac/types.rs
@crates/amlich-core/src/almanac/calc.rs
@crates/amlich-core/src/lib.rs
@crates/amlich-api/src/dto.rs
@crates/amlich-api/src/convert.rs

# Implementation constraints:
# - All new fields must be Option<T> to preserve backward compatibility
# - Ten Gods: compute from day stem to predefined targets (e.g., year stem, self)
# - Kua: compute ONLY when birth year and gender are provided in input context
# - Do NOT populate Kua for date-only requests (keep field absent with clear reasoning)
# - Update types.rs, dto.rs, and convert.rs in atomic changes to avoid schema drift
# - Ensure JSON field names are stable and backward-compatible
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend DayFortune type with optional Ten Gods and Kua fields</name>
  <files>crates/amlich-core/src/almanac/types.rs</files>
  <behavior>
    - Test 1: DayFortune includes optional ten_gods field
    - Test 2: DayFortune includes optional tu_menh field
    - Test 3: Serialization produces stable JSON field names (snake_case)
  </behavior>
  <action>
    Extend DayFortune struct with additive fields:
    - `ten_gods: Option<DayTenGods>` where DayTenGods contains target-specific Ten Gods results
    - `tu_menh: Option<KuaResult>` reusing type from tu_menh module

    Define DayTenGods structure:
    - Include Ten Gods relations to predefined targets (e.g., `to_year_stem: ThapThanResult`, `to_self: ThapThanResult`)
    - Use stable JSON field names (snake_case)

    Ensure all new fields are Option<T> to preserve backward compatibility.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib types</automated>
  </verify>
  <done>
    DayFortune has optional ten_gods and tu_menh fields
    Field names are stable for JSON serialization
    Type compiles without breaking existing DayFortune consumers
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Wire conditional computation in calc.rs</name>
  <files>crates/amlich-core/src/almanac/calc.rs</files>
  <behavior>
    - Test 1: Ten Gods populates deterministically when day stem available
    - Test 2: Kua populates only when birth year and gender provided in context
    - Test 3: Date-only requests leave Kua field as None
  </behavior>
  <action>
    Wire conditional computation in existing day fortune calculation:
    - For Ten Gods: call `get_thap_than(day_stem, target_stem)` for predefined targets
    - For Kua: check if birth context (year, gender) exists; if yes, call `compute_kua()`, else leave None
    - Populate DayFortune fields with computed results

    Add clear inline comments documenting:
    - When each field populates (preconditions)
    - Why Kua is None for date-only requests (birth context requirement)
    - Deterministic guarantees (same inputs = same outputs)
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib calc</automated>
  </verify>
  <done>
    Ten Gods computes deterministically from day stem
    Kua computes only with birth context, otherwise None
    Conditional logic is explicit and well-documented
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Mirror fields in API DTO and implement conversion</name>
  <files>crates/amlich-api/src/dto.rs, crates/amlich-api/src/convert.rs</files>
  <behavior>
    - Test 1: DTO has matching optional ten_gods and tu_menh fields
    - Test 2: Conversion preserves optional fields (Some->Some, None->None)
    - Test 3: JSON serialization matches expected stable field names
  </behavior>
  <action>
    Extend API DTO in dto.rs:
    - Add optional `ten_gods` and `tu_menh` fields matching core types
    - Use stable field names (snake_case for JSON)

    Implement conversion in convert.rs:
    - Map DayFortune.ten_gods to DTO.ten_gods preserving Option
    - Map DayFortune.tu_menh to DTO.tu_menh preserving Option
    - Ensure conversion does not drop or alter metadata

    Update conversion tests to assert:
    - Optional fields propagate correctly
    - JSON snapshots remain stable
    - Existing DTO fields unchanged (backward compatibility)
  </action>
  <verify>
    <automated>cargo test --package amlich-api</automated>
  </verify>
  <done>
    DTO mirrors core types with optional fields
    Conversion preserves Option fields correctly
    JSON serialization is stable and backward-compatible
  </done>
</task>

</tasks>

<verification>
Run full regression gate to ensure backward compatibility and correctness:

```bash
cargo test --package amlich-core --package amlich-api
```

Expected result: all tests pass, including:
- Existing KHCBPPT validation tests (no regressions)
- New Ten Gods integration tests
- New Kua integration tests
- DTO conversion and serialization tests
- Backward compatibility contract tests
</verification>

<success_criteria>
1. DayFortune includes Ten Gods and Kua as optional, additive fields (INT-01)
2. API/DTO surfaces expose new fields through stable serialization (INT-02)
3. Fields populate deterministically only when required inputs present (INT-03)
4. JSON serialization is stable and backward-compatible (INT-04)
5. Contract and integration tests verify compatibility (INT-05)
6. Full regression gate passes: `cargo test --package amlich-core` green (INT-06)
</success_criteria>

<output>
After completion, create `.planning/phases/v1.2-ten-gods-and-kua-foundation/v1.2-03-SUMMARY.md`
</output>
