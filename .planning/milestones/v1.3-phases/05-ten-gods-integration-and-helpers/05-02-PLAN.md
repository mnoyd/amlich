---
phase: 05-ten-gods-integration-and-helpers
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/almanac/dai_van.rs
autonomous: true
requirements: [DV-HELP-01, DV-HELP-02, DV-HELP-03, DV-HELP-04]
must_haves:
  truths:
    - "Users can query which Dai Van pillar applies at any age and receive None for out-of-range ages"
    - "Users can query years until the next pillar transition and receive exact remaining years for in-range ages"
    - "Boundary semantics remain half-open: transition age belongs to incoming pillar"
  artifacts:
    - path: "crates/amlich-core/src/almanac/dai_van.rs"
      provides: "Stable helper contract tests for get_current_pillar/get_pillar_at_age/years_to_next_transition"
      exports: ["get_current_pillar", "get_pillar_at_age", "years_to_next_transition"]
      min_lines: 440
  key_links:
    - from: "crates/amlich-core/src/almanac/dai_van.rs"
      to: "crates/amlich-core/src/almanac/dai_van.rs"
      via: "years_to_next_transition uses get_pillar_at_age for range lookup"
      pattern: "years_to_next_transition.*get_pillar_at_age"
---

<objective>
Lock and harden existing Dai Van age helper contracts so Phase 4 behavior remains stable and explicit for all range and transition queries.

Purpose: Prevent future drift (clamping, boundary regressions, ambiguous returns) by adding focused contract-level tests around helper APIs that Phase 5 and Phase 6 depend on.
Output: Expanded helper contract test coverage for boundary ages, out-of-range handling, and transition math.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-ten-gods-integration-and-helpers/05-RESEARCH.md
@.planning/phases/04-core-dai-van-module/04-02-SUMMARY.md
@crates/amlich-core/src/almanac/dai_van.rs

<interfaces>
From `crates/amlich-core/src/almanac/dai_van.rs`:
```rust
pub fn get_pillar_at_age(result: &DaiVanResult, age: f64) -> Option<&DaiVanPillar>
pub fn get_current_pillar(result: &DaiVanResult, age: f64) -> Option<&DaiVanPillar>
pub fn years_to_next_transition(result: &DaiVanResult, age: f64) -> Option<f64>
```
</interfaces>

# Implementation constraints:
# - Do not change helper return types away from Option
# - Preserve `[start_age, end_age)` semantics for every helper path
# - No clamping, coercion, or implicit nearest-pillar fallback
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add contract tests for age lookup boundaries and Option behavior</name>
  <files>crates/amlich-core/src/almanac/dai_van.rs</files>
  <behavior>
    - Test 1: `age == start_age` resolves to the expected pillar, `age == end_age` resolves to the next pillar
    - Test 2: Ages below first start or at/after last end return `None`
    - Test 3: `get_current_pillar` mirrors `get_pillar_at_age` for same inputs
  </behavior>
  <action>
    Add a dedicated helper-contract test module in `dai_van.rs` (or expand existing helper tests) with explicit boundary fixtures and assertions that document half-open semantics.

    Ensure tests assert Option behavior for out-of-range ages on both ends and verify no accidental clamping behavior is introduced.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib dai_van::tests::helper_contracts</automated>
  </verify>
  <done>
    Range lookup behavior is fully specified and test-enforced
    Boundary semantics are explicit and stable for downstream phases
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add transition-countdown contract tests</name>
  <files>crates/amlich-core/src/almanac/dai_van.rs</files>
  <behavior>
    - Test 1: In-range age returns exact years to pillar end (`end_age - age`)
    - Test 2: At exact transition age, countdown reflects full span for incoming pillar
    - Test 3: Out-of-range ages return `None`
  </behavior>
  <action>
    Add focused tests for `years_to_next_transition` across representative ages:
    - early in first pillar
    - exact transition boundary
    - before first pillar and after final pillar

    Use epsilon-based float assertions where appropriate while preserving exact formula semantics.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib dai_van::tests::helper_contracts</automated>
  </verify>
  <done>
    Transition countdown helper has deterministic and documented behavior
    Out-of-range handling remains Option-based and consistent with age lookup helpers
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --package amlich-core --lib dai_van::tests::helper_contracts
```
</verification>

<success_criteria>
1. Current-pillar and pillar-at-age queries are stable for all boundary conditions (DV-HELP-01, DV-HELP-03)
2. Transition countdown returns deterministic remaining years for in-range ages (DV-HELP-02)
3. All helper APIs preserve explicit `Option` out-of-range behavior with no clamping drift (DV-HELP-04)
</success_criteria>

<output>
After completion, create `.planning/phases/05-ten-gods-integration-and-helpers/05-02-SUMMARY.md`
</output>
