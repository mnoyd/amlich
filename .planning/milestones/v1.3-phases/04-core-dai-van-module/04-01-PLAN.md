---
phase: 04-core-dai-van-module
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/almanac/dai_van.rs
autonomous: true
requirements: [DV-CALC-02, DV-CALC-03, DV-META-01, DV-META-02, DV-META-03, DV-META-04]
must_haves:
  truths:
    - "Chieuthu direction is deterministic from year polarity x gender with full 4-case coverage"
    - "Start age is derived from nearest Tiet Khi distance using 3-days-per-year and preserves decimal precision"
    - "Dai Van result includes convention and evidence metadata for source and method traceability"
  artifacts:
    - path: "crates/amlich-core/src/almanac/dai_van.rs"
      provides: "Core domain types and base calculation primitives"
      exports: ["ChieuThu", "DaiVanResult", "DaiVanPillar", "DaiVanConvention", "DaiVanEvidence", "determine_chieu_thu", "calculate_start_age_years"]
      min_lines: 220
  key_links:
    - from: "crates/amlich-core/src/almanac/dai_van.rs"
      to: "crates/amlich-core/src/almanac/types.rs"
      via: "HeavenlyStem polarity and RuleEvidence-compatible metadata pattern reuse"
      pattern: "HeavenlyStem|source_id|method"
---

<objective>
Create the Phase 4 core Dai Van domain foundation: direction rule, start-age conversion, and metadata model that locks deterministic and auditable behavior before pillar generation is added.

Purpose: Prevent downstream drift by codifying non-negotiable conventions (year polarity x gender direction matrix, start-age conversion semantics, and traceability metadata) at the module boundary.
Output: New `dai_van.rs` with strongly typed core structures, deterministic primitives, and focused tests.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-core-dai-van-module/04-CONTEXT.md
@.planning/phases/04-core-dai-van-module/04-RESEARCH.md
@crates/amlich-core/src/almanac/types.rs
@crates/amlich-core/src/almanac/tu_menh.rs
@crates/amlich-core/src/canchi.rs
@crates/amlich-core/src/tietkhi.rs

# Implementation constraints:
# - Honor locked context decisions exactly: [start_age, end_age), decimal start_age, explicit error/absence for invalid gender
# - Keep source_id as "khcbppt" placeholder with explicit TODO-style provenance note in metadata docs
# - Use deterministic pure functions only (no wall clock, randomness, or hidden defaults)
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create Dai Van core types and metadata contracts</name>
  <files>crates/amlich-core/src/almanac/dai_van.rs</files>
  <behavior>
    - Test 1: Result type carries convention metadata fields (`year_basis`, `start_age_method`, `gender_encoding`) and evidence fields (`source_id`, `method`)
    - Test 2: Direction type supports canonical semantics and Vietnamese display label mapping (`Thuan`/`Nghich`)
    - Test 3: Pillar range structure supports `[start_age, end_age)` with decimal age representation
  </behavior>
  <action>
    Add new domain types in `dai_van.rs`:
    - `ChieuThu` enum (forward/backward semantics + Vietnamese display label helper)
    - `DaiVanPillar` and `DaiVanResult` structs
    - `DaiVanConvention` and `DaiVanEvidence` metadata structs aligned with existing project evidence/convention style

    Include a documented constructor/default metadata profile for Phase 4 with:
    - `source_id = "khcbppt"` placeholder
    - method marker (e.g. `bai-quyet`)
    - explicit convention markers required by DV-META requirements
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib dai_van::tests::types_and_metadata</automated>
  </verify>
  <done>
    Core Dai Van structs/enums exist and serialize cleanly
    Metadata shape is explicit and test-backed
    Display semantics for direction are deterministic and documented
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement direction matrix and start-age conversion primitives</name>
  <files>crates/amlich-core/src/almanac/dai_van.rs</files>
  <behavior>
    - Test 1: All 4 year-polarity x gender combinations map to expected ChieuThu
    - Test 2: `distance == 0` yields start_age `0.0`
    - Test 3: Partial-day/decimal input preserves precision in 3-days-per-year conversion
  </behavior>
  <action>
    Implement pure functions:
    - `determine_chieu_thu(year_stem: HeavenlyStem, gender: Gender) -> ChieuThu`
    - `calculate_start_age_years(days_to_nearest_tiet_khi: f64) -> f64`

    Keep behavior explicit:
    - Use year stem polarity as matrix source
    - No silent fallback for invalid/missing gender paths (model absence/error explicitly at call sites)
    - Preserve decimal precision for canonical storage value
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib dai_van::tests::direction_and_start_age</automated>
  </verify>
  <done>
    Direction matrix is fully covered by tests
    Start-age conversion behavior is deterministic and precise
    Core primitive API is ready for pillar-generation workflow
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --package amlich-core --lib dai_van
```
</verification>

<success_criteria>
1. Core Dai Van types and metadata contracts are implemented (DV-META-01/02/03/04)
2. Chieuthu matrix from year polarity x gender is deterministic and fully tested (DV-CALC-02)
3. Start-age conversion uses 3-days-per-year with decimal precision and boundary handling (DV-CALC-03)
4. Module remains deterministic and side-effect free
</success_criteria>

<output>
After completion, create `.planning/phases/04-core-dai-van-module/04-01-SUMMARY.md`
</output>
