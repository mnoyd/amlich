---
phase: 10-foundation-schema-lock-adrs-source-id-registration
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/adrs/0002-phi-tinh-monthly-anchor.md
  - .planning/adrs/0003-nien-tu-bach-polarity.md
  - crates/amlich-core/src/almanac/fengshui/mod.rs
  - crates/amlich-core/src/almanac/fengshui/types.rs
  - crates/amlich-core/src/almanac/mod.rs
autonomous: true
requirements:
  - FND-02
  - FND-04
  - FND-05

must_haves:
  truths:
    - "A reader can find .planning/adrs/0002-phi-tinh-monthly-anchor.md with Status: Accepted, naming the v1.1.2 Tiết Khí scanner and the 12 solar-month opening terms"
    - "A reader can find .planning/adrs/0003-nien-tu-bach-polarity.md with Status: Accepted, a Tam Nguyên × year-polarity matrix table, and explicit MEDIUM-confidence acknowledgment for Thượng/Trung Nguyên rows"
    - "A caller can construct a FlyingStarLayout { period, palaces: [FlyingStar; 9], center_star, evidence } value — the field set is frozen at FND-02"
    - "Palace enum carries canonical Lo Shu numbering (N=1, NE=8, E=3, SE=4, S=9, SW=2, W=7, NW=6, Center=5) with palace_to_direction() stub"
    - "FlyingStar enum has 9 variants (NhatBach=1 through CuuTu=9) with no metadata logic in Phase 10 — Phase 13 fills associated element/polarity/auspice from JSON"
    - "Phi Tinh types do NOT touch interaction/direction_merge.rs in v1.5 (PITFALLS CRIT-3)"
  artifacts:
    - path: ".planning/adrs/0002-phi-tinh-monthly-anchor.md"
      provides: "Locked monthly Phi Tinh anchor convention — solar-term boundaries via v1.1.2 Tiết Khí scanner"
      contains: "Status: Accepted"
    - path: ".planning/adrs/0003-nien-tu-bach-polarity.md"
      provides: "Locked Niên Tử Bạch direction polarity matrix (Tam Nguyên × year polarity)"
      contains: "Status: Accepted"
    - path: "crates/amlich-core/src/almanac/fengshui/types.rs"
      provides: "Locked FlyingStarLayout, FlyingStar, Palace, FlyingStarPeriod type stubs"
      contains: "pub struct FlyingStarLayout"
    - path: "crates/amlich-core/src/almanac/fengshui/mod.rs"
      provides: "fengshui module entry point declaring types sub-module (Phase 13 fills algorithms)"
      contains: "pub mod types"
  key_links:
    - from: "crates/amlich-core/src/almanac/mod.rs"
      to: "crates/amlich-core/src/almanac/fengshui/mod.rs"
      via: "pub mod fengshui;"
      pattern: "pub mod fengshui"
    - from: ".planning/adrs/0002-phi-tinh-monthly-anchor.md"
      to: "crates/amlich-core/src/tietkhi.rs"
      via: "Names get_all_tiet_khi_for_year as boundary resolver"
      pattern: "get_all_tiet_khi_for_year"
    - from: "crates/amlich-core/src/almanac/fengshui/types.rs"
      to: ".planning/adrs/0002-phi-tinh-monthly-anchor.md"
      via: "FlyingStarPeriod::Monthly anchors per ADR 0002"
      pattern: "FlyingStarPeriod"
---

<objective>
Write the two remaining Phi Tinh ADRs (monthly anchor convention, Niên Tử Bạch polarity matrix) and land the locked `FlyingStarLayout` Rust type stubs that Phase 13 will fill with algorithm implementations.

Purpose:
- FND-02 — `FlyingStarLayout` shape must be frozen before Phase 13 starts Vận-table work; PITFALLS CRIT-4 (palace-table typos) requires the shape locked so Lo Shu invariant checks have a target.
- FND-04 — monthly anchor convention (solar-term per Thẩm Thị Huyền Không Học, reusing v1.1.2 Tiết Khí scanner) must be ADR'd before Phase 13 implements `compute_monthly_flying_stars`.
- FND-05 — Niên Tử Bạch direction rule per Tam Nguyên × year polarity must be ADR'd with the polarity matrix table so Phase 13 has unambiguous input.

PITFALLS CRIT-3 anchor: Phi Tinh types must NOT be wired into `interaction/direction_merge.rs` in v1.5. This plan creates types in `almanac/fengshui/types.rs` deliberately separated from KHCBPPT direction modules.

Output:
- `.planning/adrs/0002-phi-tinh-monthly-anchor.md`
- `.planning/adrs/0003-nien-tu-bach-polarity.md`
- `crates/amlich-core/src/almanac/fengshui/mod.rs` declaring `pub mod types;`
- `crates/amlich-core/src/almanac/fengshui/types.rs` with locked stubs
- `pub mod fengshui;` added to `almanac/mod.rs`

What this plan does NOT do (deferred to Phase 13):
- No `compute_period`, `compute_yearly_flying_stars`, `compute_monthly_flying_stars`, `compute_combined_overlay`
- No Vận-table JSON files under `data/almanac/`
- No Lo Shu invariant runtime checks (only the type slots that will hold the data)
- No `lookup_star_pair_aspect` (Phase 14)
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-foundation-schema-lock-adrs-source-id-registration/10-CONTEXT.md
@.planning/phases/10-foundation-schema-lock-adrs-source-id-registration/10-RESEARCH.md
@.planning/research/PITFALLS.md
@.planning/research/ARCHITECTURE.md

<interfaces>
FlyingStarLayout field set required by FND-02 (locked):
- period: FlyingStarPeriod  (Van | Yearly | Monthly  — discriminator)
- palaces: [FlyingStar; 9]
- center_star: FlyingStar
- evidence: ReasoningEvidenceEnvelope  (existing type — reuse)

CONTEXT.md §Claude's Discretion: planner may use one parameterized struct (recommended) OR three distinct types. FND-02 requires only that the field set is frozen.

Recommendation: ONE parameterized struct with `FlyingStarPeriod` discriminator — simpler API surface, single conversion path through Phase 13, matches Phase 15 DTO surfacing (one `FlyingStarsSummary` aggregate per day rather than three competing nullable summaries).

Palace enum (FS-02, canonical Lo Shu numbering):
- N = 1
- SW = 2
- E = 3
- SE = 4
- Center = 5
- NW = 6
- W = 7
- NE = 8
- S = 9
(Each variant explicitly numbered via `#[repr(u8)]` so palace_to_direction lookups are O(1) and the table is statically verifiable.)

FlyingStar enum (FS-03, 9 variants):
- NhatBach = 1   (1 White)
- NhiHac = 2     (2 Black)
- TamBich = 3    (3 Jade/Green)
- TuLuc = 4      (4 Green)
- NguHoang = 5   (5 Yellow — danger per FS-14)
- LucBach = 6    (6 White)
- ThatXich = 7   (7 Red)
- BatBach = 8    (8 White)
- CuuTu = 9      (9 Purple)

Phase 10 does NOT load metadata (element/polarity/auspice) — Phase 13 builds `data/almanac/flying_stars.json` and the loader. Phase 10 only declares the enum + Lo Shu number.

Existing reusable types:
- `ReasoningEvidenceEnvelope` — find via `grep -rn 'pub struct ReasoningEvidenceEnvelope' crates/amlich-core/src/` (likely under `analysis_envelope.rs` or `reasoning/`). FlyingStarLayout's `evidence` field uses this type.
- v1.1.2 Tiết Khí scanner: `get_all_tiet_khi_for_year(year: i32, time_zone: f64) -> Vec<SolarTermWithDate>` at `crates/amlich-core/src/tietkhi.rs:227`. NAMED ONLY in the ADRs — no code call in Phase 10.

12 solar-month opening terms (from `TIET_KHI` const array, every other term — research-verified):
| Month | Tiết Khí | Longitude |
|-------|----------|-----------|
| Dần (1) | Lập Xuân | 315° |
| Mão (2) | Kinh Trập | 345° |
| Thìn (3) | Thanh Minh | 15° |
| Tỵ (4) | Lập Hạ | 45° |
| Ngọ (5) | Mang Chủng | 75° |
| Mùi (6) | Tiểu Thử | 105° |
| Thân (7) | Lập Thu | 135° |
| Dậu (8) | Bạch Lộ | 165° |
| Tuất (9) | Hàn Lộ | 195° |
| Hợi (10) | Lập Đông | 225° |
| Tý (11) | Đại Tuyết | 255° |
| Sửu (12) | Tiểu Hàn | 285° |

Tam Nguyên (FS-05, from PITFALLS MOD-3 + phongthuycaivan.org):
- Thượng Nguyên: Vận 1 (1864-1883), Vận 2 (1884-1903), Vận 3 (1904-1923)  — starting star 1
- Trung Nguyên: Vận 4 (1924-1943), Vận 5 (1944-1963), Vận 6 (1964-1983)  — starting star 4
- Hạ Nguyên: Vận 7 (1984-2003), Vận 8 (2004-2023), Vận 9 (2024-2043)   — starting star 7
- Boundaries: Lập Xuân (not Jan 1) — anchored by the v1.1.2 Tiết Khí scanner

Year polarity rule (FS-05):
- Year is DƯƠNG (yang) when can-chi stem index is odd in the 10-stem sequence: Giáp(1), Bính(3), Mậu(5), Canh(7), Nhâm(9)
- Year is ÂM (yin) when stem index is even: Ất(2), Đinh(4), Kỷ(6), Tân(8), Quý(10)
- 2024 = Giáp Thìn → DƯƠNG; 2025 = Ất Tỵ → ÂM
- Pattern (per phongthuycaivan.org — MEDIUM confidence): DƯƠNG → nghịch hành (retrograde); ÂM → thuận hành (forward). Pattern holds across all three Yuans per the source.

Module placement (per CONTEXT.md):
- `crates/amlich-core/src/almanac/fengshui/` — folder, not file. (Tier-3 spatial_compose will join it later.)
- Add `pub mod fengshui;` to `crates/amlich-core/src/almanac/mod.rs`.
- Reserve location only — do NOT also add `pub mod rituals` here (that's at the crate root, not under `almanac`, per plan 10-03).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write ADR 0002 (monthly anchor) and ADR 0003 (polarity matrix)</name>
  <files>.planning/adrs/0002-phi-tinh-monthly-anchor.md, .planning/adrs/0003-nien-tu-bach-polarity.md</files>
  <action>
Write both ADRs in Nygard short-form (Title / Status / Context / Decision / Consequences). Each ~1 page.

**ADR 0002 — Phi Tinh Monthly Anchor Convention**

- Title: `# ADR-0002: Phi Tinh Monthly Anchor Convention`
- Status: `Accepted`  / Date: `2026-05-26`
- Context: Monthly Phi Tinh (Nguyệt Tử Bạch) requires a month-boundary definition. Vietnamese practice splits across schools (lunar months vs solar-term months vs civil months). PITFALLS MOD-2: lunar months shift Gregorian dates year-to-year and produce school-dependent results; civil months ignore classical convention; solar-term months align with Thẩm Thị Huyền Không Học.
- Decision:
  1. Monthly Phi Tinh uses **solar-term month boundaries** (tháng tiết khí) per *Thẩm Thị Huyền Không Học*.
  2. Boundary resolver: the v1.1.2 Tiết Khí scanner `get_all_tiet_khi_for_year(year: i32, time_zone: f64) -> Vec<SolarTermWithDate>` at `crates/amlich-core/src/tietkhi.rs:227`. No new term-scanning code in v1.5.
  3. List the 12 solar-month opening terms in a table (use the table from the `<interfaces>` block above).
  4. Year-branch group rule (groups start at 8/5/2, descend mod-9) is applied to these solar months, not lunar months.
- Consequences: Phase 13 wires `compute_monthly_flying_stars` against this resolver. Annual Phi Tinh (Niên Tử Bạch) also anchors year boundaries at Lập Xuân via the same scanner (consistent with ADR-0002 even though Niên is yearly). Future revisions require a superseding ADR.

**ADR 0003 — Niên Tử Bạch Direction Polarity Matrix**

- Title: `# ADR-0003: Niên Tử Bạch Direction Polarity Matrix`
- Status: `Accepted`  / Date: `2026-05-26`
- Context: Annual flying star direction (thuận hành = forward; nghịch hành = retrograde) varies by Tam Nguyên yuan period crossed with year polarity (dương/âm). PITFALLS MOD-3 warns that a single `is_retrograde: bool` flag is wrong; the rule needs a matrix.
- Decision:
  1. Direction is determined by `(Tam_Nguyên_yuan, year_polarity) -> (starting_star, direction)`.
  2. Tam Nguyên structure (use the bullet list from the `<interfaces>` block).
  3. Year polarity rule (use the description from the `<interfaces>` block — explicit Giáp/Bính/Mậu/Canh/Nhâm = dương / Ất/Đinh/Kỷ/Tân/Quý = âm).
  4. Polarity matrix:

| Yuan | Vận Range | Starting Star | Dương Year Direction | Âm Year Direction |
|------|-----------|---------------|---------------------|-------------------|
| Thượng Nguyên | 1864–1923 (Vận 1–3) | 1 (Nhất Bạch) | Nghịch hành (retrograde) | Thuận hành (forward) |
| Trung Nguyên  | 1924–1983 (Vận 4–6) | 4 (Tứ Lục)    | Nghịch hành | Thuận hành |
| Hạ Nguyên     | 1984–2043 (Vận 7–9) | 7 (Thất Xích) | Nghịch hành | Thuận hành |

  5. Worked examples (year polarity check):
     - 2024 = Giáp Thìn → dương; Vận 9 → Hạ Nguyên → starting star 7, nghịch hành.
     - 2025 = Ất Tỵ → âm; Vận 9 → Hạ Nguyên → starting star 7, thuận hành.
  6. **Confidence acknowledgment**: The Thượng Nguyên and Trung Nguyên rows are MEDIUM confidence — sourced from phongthuycaivan.org (single Vietnamese-language source). Phase 13 cross-checks against *Thẩm Thị Huyền Không Học* during implementation; any divergence is logged as `KnownDivergence` per EXPANSION_FRAMEWORK §7. If a revision is needed, ADR-0003a will supersede this one. Hạ Nguyên row is HIGH confidence (the practical era, multiple sources cross-checked).
- Consequences:
  - Phase 13 implements a `(van_number, year_polarity) -> (starting_star, direction)` lookup, not a `bool` flag.
  - Year polarity computed from can-chi stem (already available via existing `canchi.rs`).
  - Year boundaries anchor at Lập Xuân via `tietkhi.rs` (consistent with ADR-0002).

Both ADRs land with `Status: Accepted` to unblock Phase 13. Superseding ADRs are the revision path.
  </action>
  <verify>
    <automated>test -f .planning/adrs/0002-phi-tinh-monthly-anchor.md && test -f .planning/adrs/0003-nien-tu-bach-polarity.md && grep -q "Status: Accepted" .planning/adrs/0002-phi-tinh-monthly-anchor.md && grep -q "Status: Accepted" .planning/adrs/0003-nien-tu-bach-polarity.md && grep -q "get_all_tiet_khi_for_year" .planning/adrs/0002-phi-tinh-monthly-anchor.md && grep -q "Tam Nguyên\|Thượng Nguyên\|Hạ Nguyên" .planning/adrs/0003-nien-tu-bach-polarity.md && grep -q "MEDIUM" .planning/adrs/0003-nien-tu-bach-polarity.md</automated>
  </verify>
  <done>Both ADR files exist with Status: Accepted; ADR 0002 names the v1.1.2 Tiết Khí scanner by function signature and lists all 12 solar-month opening terms; ADR 0003 contains the Tam Nguyên × polarity matrix table, the year-polarity rule, worked 2024/2025 examples, and an explicit MEDIUM-confidence acknowledgment for Thượng/Trung Nguyên rows naming Phase 13 as the validation phase.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Land almanac/fengshui/types.rs stubs and register module</name>
  <files>crates/amlich-core/src/almanac/fengshui/mod.rs, crates/amlich-core/src/almanac/fengshui/types.rs, crates/amlich-core/src/almanac/mod.rs</files>
  <behavior>
- Test 1: `Palace::N as u8 == 1`, `Palace::Center as u8 == 5`, `Palace::S as u8 == 9` (Lo Shu numbering correct).
- Test 2: `FlyingStar::NhatBach as u8 == 1`, `FlyingStar::CuuTu as u8 == 9`.
- Test 3: `palace_to_direction(Palace::N)` returns a non-empty string slice (stub returns `"N"` or similar — Phase 13 may refine; Phase 10 only locks the function signature).
- Test 4: A `FlyingStarLayout { period: FlyingStarPeriod::Yearly { year: 2024 }, palaces: [FlyingStar::CuuTu; 9], center_star: FlyingStar::CuuTu, evidence: <minimal envelope> }` value can be constructed (compile-only proof that the field set is frozen).
- Test 5: `FlyingStarPeriod` discriminator round-trips Van/Yearly/Monthly via serde JSON.
  </behavior>
  <action>
1. Locate the existing `ReasoningEvidenceEnvelope` type:
```
grep -rn "pub struct ReasoningEvidenceEnvelope\|pub type ReasoningEvidenceEnvelope" crates/amlich-core/src/
```
Use the existing type as the `evidence` field type. If the exact name differs (e.g., `EvidenceEnvelope`, `ReasoningEvidence`), use whatever the existing convention is — the FND-02 success criterion names `evidence` as a slot, not a specific type.

2. Create `crates/amlich-core/src/almanac/fengshui/mod.rs`:

```rust
//! Phi Tinh (Flying Stars) — Huyền Không thời gian.
//!
//! Phase 10 lands frozen type stubs only (this module's `types` sub-module).
//! Phase 13 will add `period.rs`, `annual.rs`, `monthly.rs`, `combined.rs` with the
//! algorithm implementations and Vận-table JSON loaders.
//! Phase 14 will add `aspects.rs` (81-cell star-pair aspects) and `safety.rs`.
//!
//! Schemas locked by ADR-0002 (monthly anchor) and ADR-0003 (Niên polarity matrix).
//!
//! NOTE: per PITFALLS CRIT-3 and CONTEXT.md, `FlyingStar` is a palace-layout descriptor
//! and is NEVER wired into `interaction/direction_merge.rs` in v1.5.

pub mod types;
```

3. Create `crates/amlich-core/src/almanac/fengshui/types.rs`:

```rust
//! Locked v1 Phi Tinh types. See ADR-0002 and ADR-0003.

use serde::{Deserialize, Serialize};

// Bring in existing evidence envelope (path may differ — adjust import after grep)
use crate::analysis_envelope::ReasoningEvidenceEnvelope; // <-- adjust if path differs

/// Canonical Lo Shu palace numbering (FS-02).
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[repr(u8)]
pub enum Palace {
    N = 1,
    SW = 2,
    E = 3,
    SE = 4,
    Center = 5,
    NW = 6,
    W = 7,
    NE = 8,
    S = 9,
}

impl Palace {
    /// Iteration order for `[FlyingStar; 9]` indexing follows the variant numbers 1..=9
    /// where index i corresponds to the palace with Lo Shu number i+1.
    pub const ALL: [Palace; 9] = [
        Palace::N, Palace::SW, Palace::E, Palace::SE,
        Palace::Center, Palace::NW, Palace::W, Palace::NE, Palace::S,
    ];
}

/// Direction-string slug for a palace. Phase 10 stub; Phase 13 may refine localization.
pub fn palace_to_direction(p: Palace) -> &'static str {
    match p {
        Palace::N => "N",
        Palace::SW => "SW",
        Palace::E => "E",
        Palace::SE => "SE",
        Palace::Center => "Center",
        Palace::NW => "NW",
        Palace::W => "W",
        Palace::NE => "NE",
        Palace::S => "S",
    }
}

/// Nine canonical Phi Tinh stars (FS-03).
/// Metadata (element / polarity / auspice) loaded by Phase 13 from
/// `data/almanac/flying_stars.json`. Phase 10 declares the enum only.
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[repr(u8)]
pub enum FlyingStar {
    NhatBach = 1,
    NhiHac = 2,
    TamBich = 3,
    TuLuc = 4,
    NguHoang = 5,
    LucBach = 6,
    ThatXich = 7,
    BatBach = 8,
    CuuTu = 9,
}

/// Period discriminator for FlyingStarLayout (FND-02).
/// `Van` = era-level base palace (Vận 7/8/9).
/// `Yearly` = annual Niên Tử Bạch (anchored at Lập Xuân per ADR-0003).
/// `Monthly` = monthly Nguyệt Tử Bạch (anchored at solar terms per ADR-0002).
#[derive(Debug, Clone, Copy, PartialEq, Eq, Serialize, Deserialize)]
#[serde(tag = "kind", rename_all = "snake_case")]
pub enum FlyingStarPeriod {
    Van { van: u8 },
    Yearly { year: i32 },
    Monthly { year: i32, month: u8 },
}

/// Frozen v1 Phi Tinh layout shape (FND-02).
///
/// FIELD SET LOCKED — any change requires a superseding ADR.
#[derive(Debug, Clone, Serialize, Deserialize)]
pub struct FlyingStarLayout {
    pub period: FlyingStarPeriod,
    /// Indexed in `Palace::ALL` order (N=1, SW=2, E=3, SE=4, Center=5, NW=6, W=7, NE=8, S=9).
    pub palaces: [FlyingStar; 9],
    pub center_star: FlyingStar,
    pub evidence: ReasoningEvidenceEnvelope,
}

#[cfg(test)]
mod tests {
    // The 5 behaviors from the <behavior> block.
}
```

If the existing `ReasoningEvidenceEnvelope` does not derive `Serialize`/`Deserialize`, adjust `FlyingStarLayout` to derive only `Debug, Clone` (drop `Serialize, Deserialize`) — Phase 15 will deal with DTO serialization. FND-02 only requires the value can be CONSTRUCTED, not serialized.

4. Add `pub mod fengshui;` to `crates/amlich-core/src/almanac/mod.rs`, inserted alphabetically. Do NOT also add `pub mod rituals` here (rituals is at the crate root, not under almanac).

5. Write the five tests in the `tests` module per the behavior list:
   - Use `assert_eq!(Palace::N as u8, 1)` etc. for tests 1-2.
   - Use a helper or `Default::default()` to construct a minimal `ReasoningEvidenceEnvelope`; if the type has no `Default` impl, look at existing usage (`grep -rn "ReasoningEvidenceEnvelope {" crates/amlich-core/src/`) and copy a known-minimal construction pattern.
   - Test 5 may be `cargo test serde_round_trip_period`.

6. Run `cargo test --package amlich-core --lib almanac::fengshui`.

7. Verify Phi Tinh does NOT leak into `interaction/`: `grep -rn "fengshui\|FlyingStar\|Phi Tinh" crates/amlich-core/src/interaction/` should return ZERO matches.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --lib almanac::fengshui::types::tests</automated>
  </verify>
  <done>`almanac/fengshui/mod.rs` and `types.rs` exist; types stubs compile; all five tests pass; `cargo clippy --package amlich-core -- -D warnings` clean; `grep -rn 'fengshui\|FlyingStar' crates/amlich-core/src/interaction/` returns nothing (PITFALLS CRIT-3 verified).</done>
</task>

</tasks>

<verification>
- Both ADRs exist with `Status: Accepted`.
- ADR 0002 names the v1.1.2 Tiết Khí scanner by signature and lists 12 solar-month opening terms.
- ADR 0003 carries the Tam Nguyên × polarity matrix table with MEDIUM-confidence acknowledgment.
- `cargo test --package amlich-core --lib almanac::fengshui` passes.
- `cargo build --package amlich-core` succeeds.
- `cargo clippy --package amlich-core -- -D warnings` clean.
- `grep -rn 'fengshui\|FlyingStar\|fengshui' crates/amlich-core/src/interaction/` returns 0 lines (no Phi Tinh leak into KHCBPPT direction modules — PITFALLS CRIT-3).
</verification>

<success_criteria>
- FND-02 satisfied: `FlyingStarLayout { period, palaces: [FlyingStar; 9], center_star, evidence }` can be constructed; the field set is frozen by ADR-0002 (anchor convention) + the locked type in `types.rs`.
- FND-04 satisfied: ADR 0002 exists, names the v1.1.2 Tiết Khí scanner as boundary resolver, enumerates the 12 solar-month opening terms.
- FND-05 satisfied: ADR 0003 exists with the Tam Nguyên × polarity matrix and MEDIUM-confidence acknowledgment for Thượng/Trung Nguyên rows.
- Phi Tinh module is isolated from `interaction/direction_merge.rs` per PITFALLS CRIT-3.
- Phase 13 has unambiguous types to implement against.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-schema-lock-adrs-source-id-registration/10-04-SUMMARY.md` documenting:
- The discretionary choice on FlyingStarLayout shape (parameterized single struct vs three distinct types)
- The exact path resolved for `ReasoningEvidenceEnvelope` (file:line)
- Confirmation that `interaction/` contains no Phi Tinh references
</output>
