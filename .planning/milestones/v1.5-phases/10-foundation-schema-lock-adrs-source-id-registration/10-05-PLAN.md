---
phase: 10-foundation-schema-lock-adrs-source-id-registration
plan: 05
type: execute
wave: 3
depends_on:
  - 10-03
  - 10-04
files_modified:
  - .planning/MILESTONES.md
autonomous: true
requirements:
  - FND-01
  - FND-02
  - FND-04
  - FND-05

must_haves:
  truths:
    - "A reader of .planning/MILESTONES.md can find an ADR Cross-References subsection (immediately after Key Decisions) listing all three v1.5 ADRs"
    - "Each row carries a DEC-NNNN id starting at DEC-0023, the 2026-05-26 date, a one-line description, and a relative link to the ADR file"
    - "The pre-existing numbered Key Decisions list (1-6) is UNCHANGED — Phase 10 appends a new subsection, does not reformat history"
    - "All three ADR files (0001, 0002, 0003) are reachable from MILESTONES.md via the table links"
  artifacts:
    - path: ".planning/MILESTONES.md"
      provides: "ADR Cross-References subsection registering DEC-0023, DEC-0024, DEC-0025 with links to .planning/adrs/000X-*.md"
      contains: "ADR Cross-References"
  key_links:
    - from: ".planning/MILESTONES.md"
      to: ".planning/adrs/0001-ritual-schema-v1.md"
      via: "DEC-0023 row markdown link"
      pattern: "adrs/0001-ritual-schema-v1.md"
    - from: ".planning/MILESTONES.md"
      to: ".planning/adrs/0002-phi-tinh-monthly-anchor.md"
      via: "DEC-0024 row markdown link"
      pattern: "adrs/0002-phi-tinh-monthly-anchor.md"
    - from: ".planning/MILESTONES.md"
      to: ".planning/adrs/0003-nien-tu-bach-polarity.md"
      via: "DEC-0025 row markdown link"
      pattern: "adrs/0003-nien-tu-bach-polarity.md"
---

<objective>
Register the three Phase 10 ADRs in `.planning/MILESTONES.md` via a new `### ADR Cross-References` subsection appended immediately after the existing `### Key Decisions` block (line ~212). Use a markdown TABLE format (per CONTEXT.md §specifics example). Do NOT reformat the existing numbered Key Decisions list — that's v1.0–v1.4 history.

Purpose: Single index for project-level discoverability of architectural decisions. Without this, ADRs live in an orphan directory that has no entry point from the milestone-level documentation. CONTEXT.md locked the cross-ref requirement explicitly.

Output:
- One new subsection in MILESTONES.md immediately after `### Key Decisions`, with three table rows (DEC-0023, DEC-0024, DEC-0025) linking to the three ADR files.
- Existing content untouched.

DEC-NNNN selection: Research confirmed DEC-0015, 0016, 0022 are referenced in planning docs; DEC-0017–0021 are unreferenced; DEC-0023 is the next safe number. CONTEXT.md §specifics example also uses DEC-0023 as the starting id.

MILESTONES.md format choice (per RESEARCH.md open question 3): Existing Key Decisions section uses a narrative numbered list. CONTEXT.md §specifics provides a TABLE row example. Recommendation (now executed): introduce a new TABLE subsection AFTER the existing list — keeps history intact, gives the ADR registry its own markdown shape.
</objective>

<execution_context>
@./.claude/agents/gsd-executor.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/phases/10-foundation-schema-lock-adrs-source-id-registration/10-CONTEXT.md
@.planning/phases/10-foundation-schema-lock-adrs-source-id-registration/10-RESEARCH.md

<interfaces>
Current MILESTONES.md `### Key Decisions` section (line ~212-219):
```markdown
### Key Decisions

1. **KHCBPPT as sole reference** — Most authoritative classical text for Vietnamese almanac
2. **2020-2030 date range** — Practical daily use coverage with cyclical rule pattern completeness
3. **Golden dataset approach** — Enables automated regression testing, not just one-time audit
4. **Set-based comparison** — Avoids false failures due to ordering differences (taboos, xung hop)
5. **Collect-then-assert pattern** — Provides comprehensive divergence reports with clear actionability
6. **Metadata correction** — Updated `star_meta.source_id` for proper KHCBPPT attribution
```

The next section begins with `### Files Created/Modified` (line ~221). Insert the new subsection BETWEEN them.

Column shape from CONTEXT.md §specifics:
```
| DEC-NNNN | YYYY-MM-DD | Short description | [link text](relative/path.md) |
```

ADRs landed by plans 10-03 and 10-04:
- `.planning/adrs/0001-ritual-schema-v1.md` (plan 10-03)
- `.planning/adrs/0002-phi-tinh-monthly-anchor.md` (plan 10-04)
- `.planning/adrs/0003-nien-tu-bach-polarity.md` (plan 10-04)

This plan runs in Wave 2 BECAUSE it depends on plans 10-03 and 10-04 producing the three ADR files. If executed before those plans land, the markdown links would point at non-existent files.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Append ADR Cross-References subsection to MILESTONES.md</name>
  <files>.planning/MILESTONES.md</files>
  <action>
1. Verify all three ADR files exist before editing:
```
test -f .planning/adrs/0001-ritual-schema-v1.md \
 && test -f .planning/adrs/0002-phi-tinh-monthly-anchor.md \
 && test -f .planning/adrs/0003-nien-tu-bach-polarity.md
```
If any is missing, abort — depends_on order was violated; run plans 10-03 and 10-04 first.

2. Read `.planning/MILESTONES.md` and locate the exact line immediately AFTER the numbered Key Decisions list (item 6) and BEFORE the next `### Files Created/Modified` heading.

3. Insert the following block exactly there, preserving a blank line before and after:

```markdown
### ADR Cross-References

Architectural decisions recorded in `.planning/adrs/` using the Nygard short-form. Each decision below carries a stable DEC-NNNN id for project-wide reference.

| Decision ID | Date | Description | ADR |
|-------------|------|-------------|-----|
| DEC-0023 | 2026-05-26 | Ritual JSON schema v1 locked (typed `event_keys[]`, structured `offerings[]`/`preparation_steps[]`, closed `RitualVariantTag`, `#[serde(deny_unknown_fields)]`) | [adrs/0001-ritual-schema-v1.md](adrs/0001-ritual-schema-v1.md) |
| DEC-0024 | 2026-05-26 | Phi Tinh monthly anchor uses solar-term boundaries per *Thẩm Thị Huyền Không Học*, resolved by the v1.1.2 Tiết Khí scanner | [adrs/0002-phi-tinh-monthly-anchor.md](adrs/0002-phi-tinh-monthly-anchor.md) |
| DEC-0025 | 2026-05-26 | Niên Tử Bạch direction rule is a Tam Nguyên × year-polarity matrix (dương → nghịch hành; âm → thuận hành); Thượng/Trung Nguyên rows MEDIUM confidence pending Phase 13 cross-check | [adrs/0003-nien-tu-bach-polarity.md](adrs/0003-nien-tu-bach-polarity.md) |
```

CRITICAL:
- Do NOT modify the existing numbered list (items 1–6 — v1.0–v1.4 history).
- Do NOT renumber existing items to "DEC-NNNN" — the existing list uses descriptive titles by design; the new table is a SEPARATE registry for ADR-style decisions going forward.
- Do NOT change any other content in MILESTONES.md (test counts, file paths, etc.).

4. Verify the relative links resolve. From `.planning/MILESTONES.md`, `adrs/0001-ritual-schema-v1.md` resolves to `.planning/adrs/0001-ritual-schema-v1.md` — correct.

5. Run a quick sanity grep:
```
grep -c "### ADR Cross-References" .planning/MILESTONES.md   # should be 1
grep -c "DEC-0023\|DEC-0024\|DEC-0025" .planning/MILESTONES.md  # should be 3
```
  </action>
  <verify>
    <automated>grep -q "### ADR Cross-References" .planning/MILESTONES.md && grep -q "DEC-0023" .planning/MILESTONES.md && grep -q "DEC-0024" .planning/MILESTONES.md && grep -q "DEC-0025" .planning/MILESTONES.md && grep -q "adrs/0001-ritual-schema-v1.md" .planning/MILESTONES.md && grep -q "adrs/0002-phi-tinh-monthly-anchor.md" .planning/MILESTONES.md && grep -q "adrs/0003-nien-tu-bach-polarity.md" .planning/MILESTONES.md && grep -q "^6\. \*\*Metadata correction\*\*" .planning/MILESTONES.md</automated>
  </verify>
  <done>MILESTONES.md contains a new `### ADR Cross-References` subsection with three table rows linking to the three ADR files; the existing numbered Key Decisions list is preserved (item 6 still present and unchanged); the three relative links resolve to existing files.</done>
</task>

</tasks>

<verification>
- `grep -c '### ADR Cross-References' .planning/MILESTONES.md` returns 1.
- `grep -c 'DEC-002[345]' .planning/MILESTONES.md` returns at least 3.
- All three `adrs/000X-*.md` links exist and are reachable.
- The existing 6-item numbered list under `### Key Decisions` is unchanged.
- No other lines in MILESTONES.md are modified (run `git diff .planning/MILESTONES.md` to confirm — the diff should be a single localized insertion).
</verification>

<success_criteria>
- FND-01 / FND-02 / FND-04 / FND-05 success criterion #4 satisfied: a reader can find the three ADRs cross-referenced in MILESTONES.md.
- The ADR Cross-References subsection is the canonical project-level entry point for the `.planning/adrs/` directory.
- Future Phase 11+ ADRs will append rows to this same table starting at DEC-0026.
</success_criteria>

<output>
After completion, create `.planning/phases/10-foundation-schema-lock-adrs-source-id-registration/10-05-SUMMARY.md` documenting:
- The exact line numbers where the new subsection was inserted
- The DEC-NNNN assignments (DEC-0023 → ADR-0001, DEC-0024 → ADR-0002, DEC-0025 → ADR-0003)
- Confirmation that the existing Key Decisions list (items 1-6) was not modified
</output>
