---
phase: 12-van-khan-corpus-authoring
plan: 04
type: execute
wave: 2
depends_on: ["12-01", "12-02"]
files_modified:
  - crates/amlich-core/data/rituals/provenance_audit.md
autonomous: true
requirements: [RIT-11]

must_haves:
  truths:
    - "A reviewer can open data/rituals/provenance_audit.md and find one ledger row per ritual entry."
    - "Each row records the entry's ritual_id, its classical reference, the citation page, confidence tier, and a reviewer field."
    - "Every ritual_id present in the corpus files appears in the ledger (no orphans)."
    - "The ledger lists the classical references it draws from."
  artifacts:
    - path: "crates/amlich-core/data/rituals/provenance_audit.md"
      provides: "RIT-11 provenance ledger — classical reference + reviewer per entry"
      contains: "ritual_id"
      min_lines: 70
  key_links:
    - from: "data/rituals/provenance_audit.md ledger rows"
      to: "ritual_ids authored in 12-01 and 12-02 corpus files"
      via: "one ledger row per ritual_id, citation/page copied from each entry's original_citation"
      pattern: "van-khan-"
---

<objective>
Write the `provenance_audit.md` ledger (RIT-11): a markdown file in `data/rituals/` recording, for
every ritual entry in the corpus, its classical reference, citation page, confidence tier, and the
independent reviewer (which may be "pending" for v1.5 per research Q4). This mitigates the documented
single-author risk for the Văn khấn corpus.

Purpose: Satisfy RIT-11 so a reviewer can audit the provenance of every entry from one ledger. Pure
documentation — no Rust change. Depends on plans 12-01 and 12-02 because the ledger must enumerate the
exact ritual_ids and citations they authored. Runs in parallel with plan 12-03 (different file, no
conflict).

Output: `crates/amlich-core/data/rituals/provenance_audit.md`.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-van-khan-corpus-authoring/12-RESEARCH.md
@.planning/phases/12-van-khan-corpus-authoring/12-01-SUMMARY.md
@.planning/phases/12-van-khan-corpus-authoring/12-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write provenance_audit.md ledger for every corpus entry</name>
  <files>crates/amlich-core/data/rituals/provenance_audit.md</files>
  <action>
Enumerate every ritual entry across all corpus files. Authoritatively derive the list by parsing the
JSON (do NOT rely on memory):
```bash
python3 -c "import json,glob; [print(e['ritual_id'], '|', e['original_citation'].get('title',''), '|', e['original_citation'].get('page',''), '|', e['confidence']) for f in sorted(glob.glob('crates/amlich-core/data/rituals/*.json')) if f.split('/')[-1] not in ('manifest.json',) for e in json.load(open(f))['entries']]"
```
Then create `data/rituals/provenance_audit.md` with:

1. A header: title, **Last updated** date, and total **Entries** count.
2. An `## Audit Ledger` markdown table with one row per entry, columns:
   `| ritual_id | classical_reference | page | confidence | reviewer |`. Fill classical_reference and
   page from each entry's `original_citation` (e.g. "Văn Khấn Cổ Truyền Việt Nam (NXB VHTT, 2003)").
   Set `reviewer` to `pending` (RIT-11 only requires the field be recorded; peer review is deferred
   per research Q4). Group rows by event category with a sub-heading per category file for readability.
3. A `## References` section listing the classical works cited (e.g. "Văn Khấn Cổ Truyền Việt Nam —
   NXB Văn Hóa Thông Tin, 2003 ed." as the primary source) and a one-line note on the confidence-tier
   meaning (primary / regional-variant / synthesized).

CRITICAL: the ledger row set MUST be 1:1 with the corpus ritual_ids — every ritual_id from the JSON
appears exactly once, and no ledger row references a non-existent id. NO Hán characters (this is a .md
file, so the Hán guard does not scan it, but keep it pure Quốc-ngữ for consistency).
  </action>
  <verify>
    <automated>python3 -c "import json,glob,re; ids={e['ritual_id'] for f in glob.glob('crates/amlich-core/data/rituals/*.json') if not f.endswith('manifest.json') for e in json.load(open(f))['entries']}; led=open('crates/amlich-core/data/rituals/provenance_audit.md').read(); missing=[i for i in ids if i not in led]; assert not missing, ('missing from ledger: '+str(missing)); print('all', len(ids), 'ids in ledger')"</automated>
  </verify>
  <done>provenance_audit.md exists; has a header with entry count, an Audit Ledger table with one row per ritual_id (ritual_id + classical_reference + page + confidence + reviewer), and a References section; every corpus ritual_id appears in the ledger.</done>
</task>

</tasks>

<verification>
- File exists: `test -f crates/amlich-core/data/rituals/provenance_audit.md`
- Every corpus ritual_id appears in the ledger (1:1 coverage): see Task 1 automated check.
- Ledger has the required columns (ritual_id, classical_reference, page, confidence, reviewer): `grep -c 'ritual_id' crates/amlich-core/data/rituals/provenance_audit.md`
</verification>

<success_criteria>
- `data/rituals/provenance_audit.md` exists with a header (date + entry count), an Audit Ledger table, and a References section.
- One ledger row per ritual entry, recording classical reference, page, confidence tier, and reviewer.
- 1:1 coverage: every corpus ritual_id is in the ledger; no orphan rows.
</success_criteria>

<output>
After completion, create `.planning/phases/12-van-khan-corpus-authoring/12-04-SUMMARY.md`
recording the entry count covered and the references cited.
</output>
