---
phase: 07-hour-pillar-parity-core
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/amlich-core/src/almanac/hour_pillar.rs
  - crates/amlich-core/tests/hour_pillar_parity.rs
autonomous: true
requirements: [HP-03, HP-05, PAR-02]
must_haves:
  truths:
    - "All 12 two-hour window boundaries are tested with no overlap/gap behavior"
    - "Fixture matrix covers every day-stem seed group with representative rollover cases"
    - "Parity validators assert canonical hour-pillar expectations independent of implementation internals"
  artifacts:
    - path: "crates/amlich-core/tests/hour_pillar_parity.rs"
      provides: "Parity and fixture validators for hour-pillar boundary and group coverage"
      contains: "validate_hour_pillar_parity_matrix"
      min_lines: 180
    - path: "crates/amlich-core/src/almanac/hour_pillar.rs"
      provides: "Test fixtures and helper exposure needed by integration validators"
      exports: ["compute_hour_pillar", "resolve_hour_branch_slot"]
  key_links:
    - from: "crates/amlich-core/tests/hour_pillar_parity.rs"
      to: "crates/amlich-core/src/almanac/hour_pillar.rs"
      via: "Integration tests call public APIs across boundary and matrix fixtures"
      pattern: "compute_hour_pillar|resolve_hour_branch_slot"
    - from: "crates/amlich-core/tests/hour_pillar_parity.rs"
      to: ".planning/REQUIREMENTS.md"
      via: "Fixture groups mapped to HP-03/HP-05/PAR-02 expectations"
      pattern: "HP-03|HP-05|PAR-02"
---

<objective>
Add boundary fixtures and parity validators that lock hour-pillar contracts for all stem groups and transition edges.

Purpose: Convert Phase 7 parity requirements into executable regression checks so future changes cannot reintroduce boundary or seed-group drift.
Output: Dedicated integration validator suite and expanded fixture coverage for hour-pillar parity.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-hour-pillar-parity-core/07-RESEARCH.md
@.planning/phases/07-hour-pillar-parity-core/07-01-PLAN.md
@crates/amlich-core/src/almanac/hour_pillar.rs
@crates/amlich-core/src/gio_hoang_dao.rs
@crates/amlich-core/src/almanac/golden_loader.rs

<interfaces>
From `crates/amlich-core/src/almanac/hour_pillar.rs`:
```rust
pub fn resolve_hour_branch_slot(local_hour: u8, local_minute: u8) -> Option<HourBranchSlot>;
pub fn compute_hour_pillar(
    day_stem: HeavenlyStem,
    local_hour: u8,
    local_minute: u8,
) -> Option<HourPillarResult>;
```

From `crates/amlich-core/src/almanac/golden_loader.rs`:
```rust
pub fn load_golden_dataset() -> &'static GoldenDataset;
```

From `crates/amlich-core/src/gio_hoang_dao.rs`:
```rust
pub fn get_hour_time_range(chi_index: usize) -> &'static str;
```
</interfaces>

# Implementation constraints:
# - Validator expectations must be explicit fixture data, not derived from the function under test
# - Boundary suite must include both sides of every slot transition minute
# - Preserve deterministic, no-IO test design suitable for `cargo test --package amlich-core`
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create parity fixture matrix covering all day-stem groups and rollover cases</name>
  <files>crates/amlich-core/tests/hour_pillar_parity.rs</files>
  <behavior>
    - Test 1: Matrix includes at least one representative day stem per seed group and validates canonical slot outputs
    - Test 2: Rollover fixtures verify Tý split behavior across 23:xx and 00:xx inputs
    - Test 3: Invalid time inputs are rejected predictably
  </behavior>
  <action>
    Add a new integration test file `hour_pillar_parity.rs` with a static fixture matrix that includes:
    - Day stems: Giáp, Ất, Bính, Đinh, Mậu (group representatives)
    - Representative slots: Tý, Sửu, Mão, Ngọ, Dậu, Hợi
    - Rollover cases: 23:00, 23:59, 00:00, 00:59, 01:00

    For each fixture, assert full expected hour `can_chi.full` and slot index/branch token.
    Include negative tests for invalid hour/minute values (`24`, `60`) returning `None`.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --test hour_pillar_parity parity_fixture_matrix</automated>
  </verify>
  <done>
    Fixture matrix test provides broad seed-group coverage and rollover checks with explicit expected outcomes.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add exhaustive boundary and parity validator checks with metadata assertions</name>
  <files>crates/amlich-core/tests/hour_pillar_parity.rs, crates/amlich-core/src/almanac/hour_pillar.rs</files>
  <behavior>
    - Test 1: Every transition boundary pair (`xx:59` -> `xx+1:00`) maps to adjacent slots with no overlap/gap
    - Test 2: A full 12-slot progression check for one day stem validates canonical stem walk end-to-end
    - Test 3: Every computed result in validator paths contains populated evidence metadata fields
  </behavior>
  <action>
    Extend `hour_pillar_parity.rs` with:
    - `validate_hour_slot_boundaries_all_transitions`: enumerate all 11 transitions plus Hợi->Tý rollover
    - `validate_hour_pillar_parity_matrix`: full 12-slot expected sequence for one canonical day stem
    - metadata assertion helper checking `source_id`, `method`, and `profile` are non-empty and stable

    If needed, make minimal non-breaking adjustments in `hour_pillar.rs` to expose stable slot fields required by tests.
    Keep validator outputs deterministic and independent from runtime locale/timezone state.
  </action>
  <verify>
    <automated>cargo test --package amlich-core --test hour_pillar_parity -- --nocapture</automated>
  </verify>
  <done>
    Boundary transitions, full-slot parity progression, and metadata contracts are locked by integration tests.
    Phase 7 validation requirements are test-backed and regression-resistant.
  </done>
</task>

</tasks>

<verification>
```bash
cargo test --package amlich-core --test hour_pillar_parity -- --nocapture
cargo test --package amlich-core --lib hour_pillar::tests
```
</verification>

<success_criteria>
1. HP-03 satisfied with complete transition-boundary coverage and no overlap/gap semantics
2. HP-05 satisfied via day-stem group fixture matrix plus representative rollover cases
3. PAR-02 satisfied with explicit parity fixtures and metadata assertions in deterministic validator tests
</success_criteria>

<output>
After completion, create `.planning/phases/07-hour-pillar-parity-core/07-02-SUMMARY.md`
</output>
